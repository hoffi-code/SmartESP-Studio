{
  "ln882h_ble_tracker": {
    "action": {
      "start_scan": {
        "schema": {
          "config_vars": {
            "continuous": {
              "key": "Optional",
              "templatable": true,
              "type": "boolean",
              "docs": "Override the scan mode until the next `start_scan` or `stop_scan` (the YAML-configured value itself is untouched). When omitted, the mode configured under `scan_parameters` is restored \u2014 a previous `stop_scan` does not stick as \"one-shot\".\n\n*See also: [LN882H Bluetooth Low Energy Tracker Hub](https://esphome.io/components/ln882h_ble_tracker#configuration-variables)*"
            },
            "id": {
              "key": "GeneratedID",
              "type": "use_id",
              "use_id_type": "ln882h_ble_tracker::LN882HBLETracker"
            }
          }
        },
        "type": "schema",
        "docs": "Start a Bluetooth scan. The optional [templatable](https://esphome.io/automations/templates#config-templatable) `continuous` overrides the scan mode until the next `start_scan` or `stop_scan` (the YAML-configured value itself is untouched); without it the mode configured under `scan_parameters` is restored (a previous `stop_scan` does not stick as \"one-shot\"). This differs from `esp32_ble_tracker.start_scan`, where an omitted `continuous` always forces a one-shot scan. Invoked while a scan is already running, a mode switch re-anchors the running scan's `duration` window instead of restarting the radio; a same-mode call is a no-op."
      },
      "stop_scan": {
        "maybe": "id",
        "schema": {
          "config_vars": {
            "id": {
              "key": "GeneratedID",
              "type": "use_id",
              "use_id_type": "ln882h_ble_tracker::LN882HBLETracker"
            }
          }
        },
        "type": "schema",
        "docs": "Stop the running Bluetooth scan; also cancels a start latched before setup (an `on_boot` `start_scan`). Accepts the bare-id shorthand (`ln882h_ble_tracker.stop_scan: my_tracker`). It can be restarted with `ln882h_ble_tracker.start_scan`."
      }
    },
    "schemas": {
      "CONFIG_SCHEMA": {
        "schema": {
          "config_vars": {
            "id": {
              "id_type": {
                "class": "ln882h_ble_tracker::LN882HBLETracker",
                "parents": [
                  "ble_device_base::BLEHub",
                  "Component"
                ]
              },
              "key": "GeneratedID",
              "docs": "**[ID](https://esphome.io/guides/configuration-types#id)**: Manually specify the ID used to reference this component in lambdas.\n\n*See also: [LN882H Bluetooth Low Energy Tracker Hub](https://esphome.io/components/ln882h_ble_tracker#configuration-variables)*"
            },
            "ln882h_ble_id": {
              "key": "GeneratedID",
              "type": "use_id",
              "use_id_type": "ln882h_ble::LN882HBLE"
            },
            "on_ble_advertise": {
              "key": "Optional",
              "schema": {
                "config_vars": {
                  "mac_address": {
                    "is_list": true,
                    "key": "Optional",
                    "docs": "**list of MAC Address**: The MAC address to filter for this automation.\n\n*See also: [LN882H Bluetooth Low Energy Tracker Hub](https://esphome.io/components/ln882h_ble_tracker#configuration-variables)*"
                  },
                  "then": {
                    "type": "trigger"
                  }
                }
              },
              "type": "trigger",
              "docs": "**[Automation](https://esphome.io/automations)**: An automation to perform when a BLE advertising packet is received. Optionally filtered to a list of `mac_address`es. See [`on_ble_advertise` Trigger](https://esphome.io/components/ln882h_ble_tracker#ln882h_ble_tracker-on_ble_advertise).\n\n*See also: [LN882H Bluetooth Low Energy Tracker Hub](https://esphome.io/components/ln882h_ble_tracker#configuration-variables)*"
            },
            "on_ble_manufacturer_data_advertise": {
              "has_required_var": true,
              "key": "Optional",
              "schema": {
                "config_vars": {
                  "mac_address": {
                    "key": "Optional"
                  },
                  "manufacturer_id": {
                    "key": "Required"
                  },
                  "then": {
                    "type": "trigger"
                  }
                }
              },
              "type": "trigger",
              "docs": "**[Automation](https://esphome.io/automations)**: An automation to perform when a BLE advertising packet with matching manufacturer data is received. See [`on_ble_manufacturer_data_advertise` Trigger](https://esphome.io/components/ln882h_ble_tracker#ln882h_ble_tracker-on_ble_manufacturer_data_advertise).\n\n*See also: [LN882H Bluetooth Low Energy Tracker Hub](https://esphome.io/components/ln882h_ble_tracker#configuration-variables)*"
            },
            "on_ble_service_data_advertise": {
              "has_required_var": true,
              "key": "Optional",
              "schema": {
                "config_vars": {
                  "mac_address": {
                    "key": "Optional",
                    "docs": "**MAC Address**: The MAC address to filter for this automation.\n\n*See also: [LN882H Bluetooth Low Energy Tracker Hub](https://esphome.io/components/ln882h_ble_tracker#configuration-variables)*"
                  },
                  "service_uuid": {
                    "key": "Required",
                    "docs": "**string**: 16-bit, 32-bit, or 128-bit BLE service UUID in hex (e.g. `FCD2` for BTHome v2, `181A` for Environmental Sensing).\n\n*See also: [LN882H Bluetooth Low Energy Tracker Hub](https://esphome.io/components/ln882h_ble_tracker#configuration-variables)*"
                  },
                  "then": {
                    "type": "trigger"
                  }
                }
              },
              "type": "trigger",
              "docs": "**[Automation](https://esphome.io/automations)**: An automation to perform when a BLE advertising packet with service data for `service_uuid` is received. See [`on_ble_service_data_advertise` Trigger](https://esphome.io/components/ln882h_ble_tracker#ln882h_ble_tracker-on_ble_service_data_advertise).\n\n*See also: [LN882H Bluetooth Low Energy Tracker Hub](https://esphome.io/components/ln882h_ble_tracker#configuration-variables)*"
            },
            "on_scan_end": {
              "key": "Optional",
              "type": "trigger",
              "docs": "**[Automation](https://esphome.io/automations)**: An automation to perform when a scan session ends (its `duration` elapsed, or `stop_scan` was called). Fires on every cycle when `continuous` is `true`. See [`on_scan_end` Trigger](https://esphome.io/components/ln882h_ble_tracker#ln882h_ble_tracker-on_scan_end).\n\n*See also: [LN882H Bluetooth Low Energy Tracker Hub](https://esphome.io/components/ln882h_ble_tracker#configuration-variables)*"
            },
            "scan_parameters": {
              "default": "{}",
              "key": "Optional",
              "schema": {
                "config_vars": {
                  "active": {
                    "default": "True",
                    "key": "Optional",
                    "type": "boolean",
                    "docs": "**boolean**: When `true` the scanner sends scan-request packets to request additional advertisement data (scan response). Defaults to `true`, matching [ESP32 BLE Tracker](https://esphome.io/components/esp32_ble_tracker/). > [!NOTE] > As on ESP32 (where ESP-IDF performs the merge), an advertisement and its scan > response are combined into a single result, so automations and sensor platforms > receive one merged frame containing both payloads \u2014 this is where device names come from. > The merge is best-effort: a scannable advertisement is held briefly (up to 300 ms) waiting > for its scan response, and a device that does not answer the scan request is delivered > unmerged (without a name) after that timeout.\n\n*See also: [LN882H Bluetooth Low Energy Tracker Hub](https://esphome.io/components/ln882h_ble_tracker#configuration-variables)*"
                  },
                  "continuous": {
                    "default": "True",
                    "key": "Optional",
                    "type": "boolean",
                    "docs": "**boolean**: When `true` the scanner restarts automatically after each `duration` period. When `false` a scan runs once for `duration` after `ln882h_ble_tracker.start_scan`; nothing scans on boot unless an automation starts it. Defaults to `true`.\n\n*See also: [LN882H Bluetooth Low Energy Tracker Hub](https://esphome.io/components/ln882h_ble_tracker#configuration-variables)*"
                  },
                  "duration": {
                    "default": "5min",
                    "key": "Optional",
                    "schema": {
                      "extends": [
                        "core.positive_time_period_seconds"
                      ]
                    },
                    "type": "schema",
                    "docs": "**[Time](https://esphome.io/guides/configuration-types#time)**: The length of each complete scan session. When `continuous` is `true` the scanner restarts immediately after each session ends. Defaults to `5min`.\n\n*See also: [LN882H Bluetooth Low Energy Tracker Hub](https://esphome.io/components/ln882h_ble_tracker#configuration-variables)*"
                  },
                  "interval": {
                    "default": "100ms",
                    "key": "Optional",
                    "schema": {
                      "extends": [
                        "core.positive_time_period"
                      ]
                    },
                    "type": "schema",
                    "docs": "**[Time](https://esphome.io/guides/configuration-types#time)**: The interval between each consecutive scan window. The scanner cycles through the three BLE advertising channels at this rate. Defaults to `100ms` \u2014 the LN882H SDK's recommended `SCAN_INTERVAL_DEF` (`0xA0`), which together with the default `window` gives a 50 % duty cycle (the value the chip is designed to scan at). A larger interval lowers the duty cycle and frees more CPU time for WiFi on the single-core SoC, at the cost of catching fewer advertisements.\n\n*See also: [LN882H Bluetooth Low Energy Tracker Hub](https://esphome.io/components/ln882h_ble_tracker#configuration-variables)*"
                  },
                  "window": {
                    "default": "50ms",
                    "key": "Optional",
                    "schema": {
                      "extends": [
                        "core.positive_time_period"
                      ]
                    },
                    "type": "schema",
                    "docs": "**[Time](https://esphome.io/guides/configuration-types#time)**: The time the receiver is actively listening for packets on a channel during each scan interval. Must be \u2264 `interval`. Defaults to `50ms` \u2014 the LN882H SDK's recommended `SCAN_WINDOW_DEF` (`0x50`).\n\n*See also: [LN882H Bluetooth Low Energy Tracker Hub](https://esphome.io/components/ln882h_ble_tracker#configuration-variables)*"
                  }
                }
              },
              "type": "schema",
              "docs": "Advanced parameters for configuring the scan behaviour. See also [this guide by Texas Instruments](https://dev.ti.com/tirex/explore/content/simplelink_academy_cc2640r2sdk_5_10_02_00/modules/blestack/ble_scan_adv_basic/ble_scan_adv_basic.html#scanning-basics) for background reading.\n\n*See also: [LN882H Bluetooth Low Energy Tracker Hub](https://esphome.io/components/ln882h_ble_tracker#configuration-variables)*"
            }
          },
          "extends": [
            "core.COMPONENT_SCHEMA"
          ]
        },
        "type": "schema"
      }
    }
  }
}