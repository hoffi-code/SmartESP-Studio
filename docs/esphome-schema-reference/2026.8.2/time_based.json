{
  "time_based.cover": {
    "schemas": {
      "CONFIG_SCHEMA": {
        "schema": {
          "config_vars": {
            "assumed_state": {
              "default": "True",
              "key": "Optional",
              "type": "boolean",
              "docs": "**boolean**: Whether the true state of the cover is not known. This will make the Home Assistant frontend show buttons for both OPEN and CLOSE actions, instead of hiding or disabling one of them. Defaults to `true`.\n\n*See also: [Time Based Cover](https://esphome.io/components/cover/time_based#configuration-variables)*"
            },
            "close_action": {
              "key": "Required",
              "type": "trigger",
              "docs": "**[Action](https://esphome.io/automations/actions#all-actions)**: The action that should be performed when the remote requests the cover to be closed.\n\n*See also: [Time Based Cover](https://esphome.io/components/cover/time_based#configuration-variables)*"
            },
            "close_duration": {
              "key": "Required",
              "schema": {
                "extends": [
                  "core.positive_time_period_milliseconds"
                ]
              },
              "type": "schema",
              "docs": "**[Time](https://esphome.io/guides/configuration-types#time)**: The amount of time it takes the cover to close from the fully-open state.\n\n*See also: [Time Based Cover](https://esphome.io/components/cover/time_based#configuration-variables)*"
            },
            "has_built_in_endstop": {
              "default": "False",
              "key": "Optional",
              "type": "boolean",
              "docs": "**boolean**: Indicates that the cover has built in end stop detectors. In this configuration the `stop_action` is not performed when the open or close time is completed and if the cover is commanded to open or close the corresponding actions will be performed without checking current state. Defaults to `false`.\n\n*See also: [Time Based Cover](https://esphome.io/components/cover/time_based#configuration-variables)*"
            },
            "id": {
              "id_type": {
                "class": "time_based::TimeBasedCover",
                "parents": [
                  "cover::Cover",
                  "EntityBase",
                  "Component"
                ]
              },
              "key": "GeneratedID"
            },
            "manual_control": {
              "default": "False",
              "key": "Optional",
              "type": "boolean",
              "docs": "**boolean**: For covers with manual external controls. With this configuration if the cover is commanded to open or close the corresponding actions will be performed even if the current state fully open or fully closed matches desired state, then `stop_action` will be called after the full duration of the action elapses. The current state will then be relearned upon completion. It's recommended to set `assumed_state` to true so the cover control button aren't disabled in the interface. Defaults to `false`.\n\n*See also: [Time Based Cover](https://esphome.io/components/cover/time_based#configuration-variables)*"
            },
            "open_action": {
              "key": "Required",
              "type": "trigger",
              "docs": "**[Action](https://esphome.io/automations/actions#all-actions)**: The action that should be performed when the remote requests the cover to be opened.\n\n*See also: [Time Based Cover](https://esphome.io/components/cover/time_based#configuration-variables)*"
            },
            "open_duration": {
              "key": "Required",
              "schema": {
                "extends": [
                  "core.positive_time_period_milliseconds"
                ]
              },
              "type": "schema",
              "docs": "**[Time](https://esphome.io/guides/configuration-types#time)**: The amount of time it takes the cover to open up from the fully-closed state.\n\n*See also: [Time Based Cover](https://esphome.io/components/cover/time_based#configuration-variables)*"
            },
            "stop_action": {
              "key": "Required",
              "type": "trigger",
              "docs": "**[Action](https://esphome.io/automations/actions#all-actions)**: The action that should be performed to stop the cover when the remote requests the cover to be stopped or when the cover has been opening/closing for the given durations.\n\n*See also: [Time Based Cover](https://esphome.io/components/cover/time_based#configuration-variables)*"
            }
          },
          "extends": [
            "cover._COVER_SCHEMA",
            "core.COMPONENT_SCHEMA"
          ]
        },
        "type": "schema"
      }
    }
  }
}