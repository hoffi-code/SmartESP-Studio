{
  "esp32_can.canbus": {
    "schemas": {
      "CONFIG_SCHEMA": {
        "schema": {
          "config_vars": {
            "bit_rate": {},
            "id": {
              "id_type": {
                "class": "esp32_can::ESP32Can",
                "parents": [
                  "canbus::CanbusComponent",
                  "Component"
                ]
              }
            },
            "mode": {
              "default": "NORMAL",
              "key": "Optional",
              "type": "enum",
              "values": {
                "NORMAL": {
                  "docs": "Normal operation, sends ACK signals. *(default)*"
                },
                "LISTENONLY": {
                  "docs": "Receive data only, no ACK signals sent."
                }
              },
              "docs": "**enum**: Operating mode. One of:\n\n*See also: [ESP32 CAN](https://esphome.io/components/canbus/esp32_can#configuration-variables)*"
            },
            "rx_pin": {
              "internal": true,
              "key": "Required",
              "modes": [
                "input"
              ],
              "type": "pin",
              "docs": "**[Pin](https://esphome.io/guides/configuration-types#pin)**: Receive pin.\n\n*See also: [ESP32 CAN](https://esphome.io/components/canbus/esp32_can#configuration-variables)*"
            },
            "rx_queue_len": {
              "data_type": "uint32_t",
              "key": "Optional",
              "type": "integer",
              "docs": "**int**: Length of RX queue.\n\n*See also: [ESP32 CAN](https://esphome.io/components/canbus/esp32_can#configuration-variables)*"
            },
            "tx_enqueue_timeout": {
              "key": "Optional",
              "schema": {
                "extends": [
                  "core.positive_time_period_milliseconds"
                ]
              },
              "type": "schema",
              "docs": "**[Time](https://esphome.io/guides/configuration-types#time)**: Maximum time to wait when the TX queue is full before dropping the message (by default, this is set to the time it takes to send 10 CAN messages at the given bit rate).\n\n*See also: [ESP32 CAN](https://esphome.io/components/canbus/esp32_can#configuration-variables)*"
            },
            "tx_pin": {
              "internal": true,
              "key": "Required",
              "modes": [
                "output"
              ],
              "type": "pin",
              "docs": "**[Pin](https://esphome.io/guides/configuration-types#pin)**: Transmit pin.\n\n*See also: [ESP32 CAN](https://esphome.io/components/canbus/esp32_can#configuration-variables)*"
            },
            "tx_queue_len": {
              "data_type": "uint32_t",
              "key": "Optional",
              "type": "integer",
              "docs": "**int**: Length of TX queue, 0 to disable.\n\n*See also: [ESP32 CAN](https://esphome.io/components/canbus/esp32_can#configuration-variables)*"
            }
          },
          "extends": [
            "canbus.CANBUS_SCHEMA"
          ]
        },
        "type": "schema"
      }
    }
  }
}