{
  "ble_scanner.text_sensor": {
    "schemas": {
      "CONFIG_SCHEMA": {
        "schema": {
          "config_vars": {
            "id": {
              "id_type": {
                "class": "ble_scanner::BLEScanner",
                "parents": [
                  "text_sensor::TextSensor",
                  "EntityBase",
                  "Component",
                  "ble_device_base::ESPBTDeviceListener"
                ]
              }
            },
            "ble_hub_id": {
              "key": "GeneratedID",
              "docs": "**[ID](https://esphome.io/guides/configuration-types#id)**: The BLE tracker hub to receive advertisements from. Auto-generated when only one tracker is configured; set it explicitly to disambiguate multiple trackers. Replaces the former `esp32_ble_id` key, which still works as a deprecated alias until ESPHome 2027.2.0.\n\n*See also: [Bluetooth Low Energy Scanner](https://esphome.io/components/text_sensor/ble_scanner#configuration-variables)*"
            }
          },
          "extends": [
            "text_sensor._TEXT_SENSOR_SCHEMA",
            "core.COMPONENT_SCHEMA",
            "ble_device_base.BLE_DEVICE_SCHEMA"
          ]
        },
        "type": "schema"
      }
    }
  }
}