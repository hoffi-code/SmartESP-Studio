{
  "audio": {
    "schemas": {
      "CONFIG_SCHEMA": {
        "schema": {
          "config_vars": {
            "codecs": {
              "key": "Optional",
              "docs": "**mapping**: A mapping that enables and configures individual audio codecs. Each codec key may be left empty to enable the codec with default settings, or populated with the per-codec options described below.\n\n*See also: [Audio Codec Configuration](https://esphome.io/components/audio#configuration-variables)*"
            }
          }
        },
        "type": "schema"
      }
    }
  }
}