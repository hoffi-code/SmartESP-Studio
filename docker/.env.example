TZ=Europe/Warsaw
SES_PORT=8099
SES_USE_ESPHOME_SHARED_PATH=false

# Recommended for standalone deployments exposed on a LAN.
# Set SES_AUTH_MODE=none only if another trusted layer handles access.
SES_AUTH_MODE=basic
SES_AUTH_USERNAME=admin
SES_AUTH_PASSWORD=change-me
