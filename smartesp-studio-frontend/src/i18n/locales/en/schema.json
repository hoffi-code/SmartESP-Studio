{
  "idRef": {
    "noMatch": "No matching identifiers available",
    "create": "Define a new one"
  },
  "assetRef": {
    "manage": "Manage assets"
  },
  "fields": {
    "a_pin": {
      "hint": "Row address bit 0."
    },
    "abbwelcome": {
      "hint": "Trigger on a decoded ABB-Welcome code with the given data, see the transmitter description for more info."
    },
    "above": {
      "hint": "The minimum for the condition."
    },
    "ac_charging_status": {
      "hint": "ac charging status"
    },
    "ac_output_active_power": {
      "hint": "ac output active power"
    },
    "ac_output_apparent_power": {
      "hint": "ac output apparent power"
    },
    "ac_output_frequency": {
      "hint": "ac output frequency"
    },
    "ac_output_rating_active_power": {
      "hint": "AC output rating active power"
    },
    "ac_output_rating_apparent_power": {
      "hint": "AC output rating apparent power"
    },
    "ac_output_rating_current": {
      "hint": "AC output rating current"
    },
    "ac_output_rating_frequency": {
      "hint": "AC output rating frequency"
    },
    "ac_output_rating_voltage": {
      "hint": "AC output rating voltage"
    },
    "ac_output_voltage": {
      "hint": "ac output voltage"
    },
    "ac_power": {
      "hint": "The inverter's output power."
    },
    "ac_voltage": {
      "hint": "The grid voltage measured by the inverter."
    },
    "acc": {
      "hint": "Amount of rain since last message (see update_interval ), in mm."
    },
    "accel_x": {
      "hint": "Use the X-Axis of the Accelerometer."
    },
    "accel_y": {
      "hint": "Use the Y-Axis of the Accelerometer."
    },
    "accel_z": {
      "hint": "Use the Z-Axis of the Accelerometer."
    },
    "acceleration": {
      "hint": "The information for the acceleration sensor."
    },
    "acceleration_mode": {
      "hint": "Allowed value are low, medium and high."
    },
    "acceleration_wait_time": {
      "hint": "Considers a wait time needed by the cover to actually start moving after command is issued and takes it into account when computing cover position (useful for heavy covers with large inertia)."
    },
    "acceleration_x": {
      "hint": "Use the X-Axis of the Accelerometer."
    },
    "acceleration_y": {
      "hint": "Use the Y-Axis of the Accelerometer."
    },
    "acceleration_z": {
      "hint": "Use the Z-Axis of the Accelerometer."
    },
    "accelerometer_odr": {
      "hint": "The output data rate of the accelerometer."
    },
    "accelerometer_range": {
      "hint": "The full-scale range of the accelerometer."
    },
    "accept": {
      "hint": "Should be true if the passkeys displayed on both BLE devices are matching."
    },
    "accumulated_flow": {
      "hint": "The accumulated flow (volume) sensor."
    },
    "accuracy_decimals": {
      "hint": "Set the number of digits after the decimal point that data consumers should use."
    },
    "action": {
      "hint": "The Home Assistant Action to perform."
    },
    "action_state_topic": {
      "hint": "The topic to publish climate device action changes to."
    },
    "actions": {
      "hint": "A list of user-defined actions."
    },
    "active": {
      "hint": "Whether to send scan requests and receive scan responses (device names and other data some devices only put in the scan response)."
    },
    "active_power": {
      "hint": "Report the active (consumed) power value of this phase in watts (W)."
    },
    "active_power_1": {
      "hint": "Use the (active) power value of the channel 1 in watts."
    },
    "active_power_2": {
      "hint": "Use the (active) power value of the channel 2 in watts."
    },
    "active_power_a": {
      "hint": "Use the power value of the A channel in watts."
    },
    "active_power_b": {
      "hint": "Use the power value of the B channel in watts."
    },
    "active_power_gain_a": {
      "hint": "Set the active power amplification of the A channel."
    },
    "active_power_gain_b": {
      "hint": "Set the active power amplification of the B channel."
    },
    "active_pullup_resistance": {
      "hint": "The active pullup resistance."
    },
    "actual_gain": {
      "hint": "Gain value used to measure data, multiplier."
    },
    "actual_integration_time": {
      "hint": "Integration time used to measure data, ms."
    },
    "adc_averaging": {
      "hint": "Selects ADC sample averaging count."
    },
    "adc_input_mic": {
      "hint": "Control the ADC Mic Input."
    },
    "adc_range": {
      "hint": "0 or 1: Selects the range for differential input across shunt resistor."
    },
    "adc_time": {
      "hint": "The time in microseconds to perform a single ADC conversion."
    },
    "adc_voltage_multiplier": {
      "hint": "The voltage multiplier of the ADC sensor used for calibration."
    },
    "adc_voltage_offset": {
      "hint": "The voltage offset of the ADC sensor used for calibration."
    },
    "adc128s102_id": {
      "hint": "The id of the parent ADC128S102 component."
    },
    "add_sbu_priority_version": {
      "hint": "add sbu priority version"
    },
    "address": {
      "hint": "Manually specify the I²C address of the sensor."
    },
    "addressable_light_id": {
      "hint": "The id of the addressable light component to use as a display."
    },
    "addresses": {
      "hint": "One or more IP addresses to broadcast data to."
    },
    "adjustable": {
      "hint": "If true, the output voltage can be adjusted at runtime using the esp_ldo.voltage.adjust action."
    },
    "ads1115_id": {
      "hint": "Manually specify the ID of the"
    },
    "ads1118_id": {
      "hint": "Manually specify the ID of the"
    },
    "advertising": {
      "hint": "Manually enable BLE advertising support."
    },
    "advertising_cycle_time": {
      "hint": "The time interval for cycling through multiple advertisements."
    },
    "ae_level": {
      "hint": "The auto exposure level to apply to the picture (when aec_mode is set to auto ), from -2 to 2."
    },
    "aec_mode": {
      "hint": "The mode of exposure module."
    },
    "aec_value": {
      "hint": "The Exposure value to apply to the picture (when aec_mode is set to manual ), from 0 to 1200."
    },
    "aec2": {
      "hint": "Whether to enable Auto Exposure Control 2."
    },
    "aeha": {
      "hint": "Trigger on a decoded AEHA remote code with the given data."
    },
    "agc_gain_ceiling": {
      "hint": "The maximum gain allowed, when agc_mode is set to auto."
    },
    "agc_mode": {
      "hint": "The mode of gain control module."
    },
    "agc_value": {
      "hint": "The gain value to apply to the picture (when aec_mode is set to manual ), from 0 to 30."
    },
    "alarm_on_when_primary_source_interrupt": {
      "hint": "alarm on when primary source interrupt"
    },
    "alert": {
      "hint": "Configure the alert pin behaviour."
    },
    "algorithm_tuning": {
      "hint": "The NOx algorithm can be customized by tuning 5 different parameters."
    },
    "align": {
      "hint": "Anchor point of the widget relative to its parent (e.g. CENTER, TOP_LEFT)."
    },
    "all": {
      "hint": "Condition: Takes a list of conditions, all of which must be true (and is therefore equivalent to condition .)"
    },
    "allow_internal_channel": {
      "hint": "Must be set to true to use channels 1 or 2."
    },
    "allow_multiple_touches": {
      "hint": "Whether to allow multitouch."
    },
    "allow_partial_read": {
      "hint": "For a multi-register value type (U_DWORD, S_QWORD, FP32, …), allow a read request to cover only part of the value — starting inside it or stopping short of its end — and return just the requested reg…"
    },
    "allowed_keys": {
      "hint": "Keys allowed to be used."
    },
    "allowed_origins": {
      "hint": "A list of origins that browsers are allowed to make cross-origin requests from."
    },
    "alpha": {
      "hint": "The forget factor/alpha value of the filter."
    },
    "alternative_swing_control": {
      "hint": "(supported by smartAir2 only) If true - use alternative values to control swing mode."
    },
    "altitude": {
      "hint": "Include the measured altitude as a sensor"
    },
    "altitude_compensation": {
      "hint": "Enable compensating deviations due to current altitude (in metres)."
    },
    "always_on_top": {
      "hint": "Whether to always draw the display window above other windows or not"
    },
    "ambient": {
      "hint": "Enables sensor entity for ambient temperature reading."
    },
    "ambient_light": {
      "hint": "Sensor counts for the Ambient light sensor (#)."
    },
    "ambient_light_counts": {
      "hint": "The reading for visible light (_ALS channel_), counts."
    },
    "ambient_light_gain": {
      "hint": "The ambient light gain level."
    },
    "ambient_pressure_compensation": {
      "hint": "Enable compensation of measured CO₂ values based on given ambient pressure in mBar."
    },
    "ambient_pressure_compensation_source": {
      "hint": "Set an external pressure sensor ID used for ambient pressure compensation."
    },
    "ambient_temperature": {
      "hint": "If specified, the temperature of the room the BedJet is in will be reported as a sensor."
    },
    "ammonia": {
      "hint": "All options from Sensor"
    },
    "analog_gain": {
      "hint": "The analog output gain applied by the PCM5122."
    },
    "anim_duration": {
      "hint": "Animation duration in milliseconds."
    },
    "animation": {
      "hint": "Animate page changes as specified."
    },
    "animations": {
      "hint": "A list of Animations that change widget style properties over time."
    },
    "announcement": {
      "hint": "Whether to play back the file as an announcement or media stream."
    },
    "announcement_pipeline": {
      "hint": "Pipeline Schema: Configuration settings for the announcement pipeline."
    },
    "answer_timeout": {
      "hint": "Response timeout."
    },
    "any": {
      "hint": "Condition: Takes a list of conditions; if at least one is true, the condition will be true."
    },
    "ap": {
      "hint": "Enable an access point mode on the node."
    },
    "apparent_energy": {
      "hint": "Use the apparent energy value of the sensor in kilo volt amps hours."
    },
    "apparent_power": {
      "hint": "Report the apparent (voltage multiplied by current) power value of this phase in volt-amperes (VA)."
    },
    "apparent_power_a": {
      "hint": "Use the apparent power value of the A channel in volt amperes."
    },
    "apparent_power_b": {
      "hint": "Use the apparent power value of the B channel in volt amperes."
    },
    "appearance": {
      "hint": "Sets the appearance of the device (included in advertising data.)"
    },
    "appliance_name": {
      "hint": "A text sensor that indicates Haier appliance name."
    },
    "apply_area": {
      "hint": "Send the configured area_config to the selected area_id."
    },
    "apply_config": {
      "hint": "Saves both manual config tuning or the auto calibrate still and move threshold config settings."
    },
    "area": {
      "hint": "The area configuration for this device."
    },
    "area_config": {
      "hint": "Coordinate values (x_min/x_max/y_min/y_max/z_min/z_max, each range -10–10 m, step 0.1) for the selected area ID."
    },
    "area_id": {
      "hint": "Area to edit with area_config."
    },
    "areas": {
      "hint": "Additional areas that can be referenced by devices."
    },
    "args": {
      "hint": "The optional arguments for the"
    },
    "arming_away_time": {
      "hint": "The exit delay before the alarm is armed to away mode."
    },
    "arming_home_time": {
      "hint": "The exit delay before the alarm is armed to home mode."
    },
    "arming_night_time": {
      "hint": "The exit delay before the alarm is armed to night mode."
    },
    "as5600_id": {
      "hint": "Manually specify the ID of the AS5600 Hub you want to use this sensor."
    },
    "assumed_state": {
      "hint": "Whether the true state of the cover is not known."
    },
    "astep": {
      "hint": "The number of integration steps."
    },
    "at581x_id": {
      "hint": "The ID of the AT581x component defined above."
    },
    "atime": {
      "hint": "The integration time per step in increments of 2.78µs."
    },
    "attach_attempts": {
      "hint": "Total MLE attach attempts since boot."
    },
    "attenuation": {
      "hint": "Only on ESP32."
    },
    "attribute": {
      "hint": "The name of the state attribute to import from the specified entity."
    },
    "audio_dac": {
      "hint": "The audio DAC to use for volume control."
    },
    "auth": {
      "hint": "Enables HTTP authentication with a username and password."
    },
    "auth_key": {
      "hint": "Authentication key."
    },
    "auth_req_mode": {
      "hint": "The authentication request mode."
    },
    "authorized_duration": {
      "hint": "The amount of time until authorization times out and needs to be re-authorized."
    },
    "authorizer": {
      "hint": "A binary sensor to authorize with."
    },
    "auto_add_peer": {
      "hint": "When set to true, the ESP-NOW component will automatically add any new incoming device as a peer."
    },
    "auto_advance_switch": {
      "hint": "Required with more than one valve."
    },
    "auto_cal": {
      "hint": "Enable automatic image calibration on temperature changes."
    },
    "auto_cleaning_interval": {
      "hint": "Reads/Writes the interval in seconds of the periodic fan-cleaning."
    },
    "auto_clear_enabled": {
      "hint": "If the display should be cleared before each update."
    },
    "auto_connect": {
      "hint": "If true the device will be automatically connected when found by the Esp32 Ble Tracker."
    },
    "auto_detach_time": {
      "hint": "The time after reaching the target value when the servo will be detached, if set to zero, servo will not be detached."
    },
    "auto_gain": {
      "hint": "When set to true (the default), gain, digital_gain and integration_time will be adjusted automatically to avoid saturating the sensor and allow use of the maximum possible resolution; if these values…"
    },
    "auto_gain_threshold_high": {
      "hint": "percentage: A percentage of the maximum possible lux measurement given the current gains and integration time; when the lux measurement climbs above this value, the gain, digital_gain and integration…"
    },
    "auto_gain_threshold_low": {
      "hint": "percentage: A percentage of the maximum possible lux measurement given the current gains and integration time; when the lux measurement falls below this value, the gain, digital_gain and integration_…"
    },
    "auto_interference": {
      "hint": "Generate interference areas from the current environment."
    },
    "auto_mode": {
      "hint": "Automatic gain and integration time selection."
    },
    "auto_set_reset": {
      "hint": "Enables the sensor's automatic SET/RESET feature (Auto_SR_en)."
    },
    "auto_wake": {
      "hint": "Pulse wakeup_pin before sending a command."
    },
    "auto_wake_on_touch": {
      "hint": "If set to true, the Nextion will be configured to wake from sleep when touched."
    },
    "autoconf": {
      "hint": "Get capabilities automatically."
    },
    "automatic_baseline_calibration": {
      "hint": "MH-Z19 has automatic calibration procedure."
    },
    "automatic_self_calibration": {
      "hint": "Whether to enable automatic self calibration (ASC)."
    },
    "away": {
      "hint": "The away mode state to publish."
    },
    "away_config": {
      "hint": "Additionally specify target temperature range settings for away mode."
    },
    "b_constant": {
      "hint": "The B-constant of the thermistor you are using."
    },
    "b_pin": {
      "hint": "Row address bit 1."
    },
    "b1_pin": {
      "hint": "Blue data pin for top half."
    },
    "b2_pin": {
      "hint": "Blue data pin for bottom half."
    },
    "back_keys": {
      "hint": "Keys used to delete the last pressed key."
    },
    "background_color": {
      "hint": "Specifies the background color to use."
    },
    "background_pressed_color": {
      "hint": "The background color when pressed"
    },
    "backlight_on": {
      "hint": "backlight on"
    },
    "backlight_pin": {
      "hint": "The display's backlight pin."
    },
    "backspace": {
      "hint": "The ID of a Binary Sensor , to be used as BACKSPACE key."
    },
    "band_mode": {
      "hint": "Only on esp32-c5."
    },
    "bandwidth": {
      "hint": "Bandwidth can be 4_8kHz, 5_8kHz, 7_3kHz, 9_7kHz, 11_7kHz, 14_6kHz, 19_5kHz, 23_4kHz, 29_3kHz, 39_0kHz, 46_9kHz, 58_6kHz, 78_2kHz, 93_8kHz, 117_3kHz, 156_2kHz, 187_2kHz, 234_3kHz, 312_0kHz, 373_6kHz o…"
    },
    "base_dir": {
      "hint": "Set base direction of widget."
    },
    "baseline": {
      "hint": "The baseline value for the unit, for calibration purposes."
    },
    "batch_delay": {
      "hint": "The delay time for batching multiple state update messages together to reduce network overhead."
    },
    "battery": {
      "hint": "Which battery to monitor."
    },
    "battery_bulk_voltage": {
      "hint": "battery bulk voltage"
    },
    "battery_capacity_percent": {
      "hint": "battery capacity percent"
    },
    "battery_charging_current": {
      "hint": "battery charging current"
    },
    "battery_discharge_current": {
      "hint": "battery discharge current"
    },
    "battery_float_voltage": {
      "hint": "battery float voltage;"
    },
    "battery_level": {
      "hint": "The information for the battery level sensor."
    },
    "battery_rating_voltage": {
      "hint": "battery rating voltage"
    },
    "battery_recharge_voltage": {
      "hint": "battery recharge voltage;"
    },
    "battery_redischarge_voltage": {
      "hint": "battery redischarge voltage;"
    },
    "battery_type": {
      "hint": "battery type;"
    },
    "battery_under_voltage": {
      "hint": "battery under voltage;"
    },
    "battery_voltage": {
      "hint": "The information for the battery voltage sensor."
    },
    "battery_voltage_offset_for_fans_on": {
      "hint": "battery voltage offset for fans on"
    },
    "battery_voltage_scc": {
      "hint": "battery voltage from scc"
    },
    "battery_voltage_to_steady_while_charging": {
      "hint": "battery voltage to steady while charging"
    },
    "baud_rate": {
      "hint": "Control the serial port baud rate."
    },
    "bedjet_id": {
      "hint": "The ID of the Bedjet component."
    },
    "beeper": {
      "hint": "(supported only by hOn) A switch that enables or disables Haier climate sound feedback."
    },
    "below": {
      "hint": "The maximum for the condition."
    },
    "beo4": {
      "hint": "Trigger on a decoded B&O Beo4 infrared remote code with the given data."
    },
    "bg_color": {
      "hint": "Background fill color of this part."
    },
    "bg_grad": {
      "hint": "A gradient to apply to the background."
    },
    "bg_grad_color": {
      "hint": "Second color for a background gradient, blended from bg_color along bg_grad_dir."
    },
    "bg_grad_dir": {
      "hint": "Choose the direction of the background gradient: NONE , HOR , VER ."
    },
    "bg_grad_opa": {
      "hint": "Set opacity of the second gradient color."
    },
    "bg_grad_stop": {
      "hint": "Specify where the gradient should stop: 0 = upper left, 128 = in the center, 255 = lower right."
    },
    "bg_image_opa": {
      "hint": "Opacity of the background image of the widget."
    },
    "bg_image_recolor": {
      "hint": "Color to mix with every pixel of the background image of the widget."
    },
    "bg_image_recolor_opa": {
      "hint": "Opacity of the recoloring of the background image of the widget."
    },
    "bg_image_src": {
      "hint": "The ID of an existing image configuration, to show as the background of the widget."
    },
    "bg_main_opa": {
      "hint": "Set opacity of the first gradient color."
    },
    "bg_main_stop": {
      "hint": "Specify where the gradient should start: 0 = upper left, 128 = in the center, 255 = lower right."
    },
    "bg_opa": {
      "hint": "Background opacity, 0-255 or a percentage. TRANSP hides the fill."
    },
    "binary_sensor": {
      "hint": "The id of the binary sensor to add as an observation."
    },
    "binary_sensors": {
      "hint": "A list of binary sensors the panel should use."
    },
    "bindkey": {
      "hint": "32-character hex string: The encryption key for devices that broadcast encrypted BTHome advertisements."
    },
    "birth_message": {
      "hint": "MQTTMessage: The message to send when a connection to the broker is established."
    },
    "bit_depth": {
      "hint": "Color bit depth (4-12)."
    },
    "bit_width": {
      "hint": "The bit width/resolution of the ambient light sensor."
    },
    "bit0_high": {
      "hint": "The time to hold the data line high for a 0 bit."
    },
    "bit0_low": {
      "hint": "The time to hold the data line low for a 0 bit."
    },
    "bit1_high": {
      "hint": "The time to hold the data line high for a 1 bit."
    },
    "bit1_low": {
      "hint": "The time to hold the data line low for a 1 bit."
    },
    "bitrate": {
      "hint": "Bit rate of the signal."
    },
    "bits": {
      "hint": "A list of bits that are responded to when acting as a server."
    },
    "bits_per_sample": {
      "hint": "The bit depth of the audio samples."
    },
    "bitsync": {
      "hint": "Enables the receive bit synchronizer."
    },
    "ble_client_id": {
      "hint": "The id of the ble_client entry associated with the device."
    },
    "ble_hub_id": {
      "hint": "The BLE tracker hub to receive advertisements from."
    },
    "blend_mode": {
      "hint": "Describes how to blend the colors to the background."
    },
    "blue": {
      "hint": "percentage: The blue channel of the light."
    },
    "blue_channel": {
      "hint": "Value of the blue color channel relative to the clear channel, as a percentage."
    },
    "bluetooth": {
      "hint": "Turn on/off the bluetooth adapter."
    },
    "blur_backdrop": {
      "hint": "If true the background of the widget will be blurred."
    },
    "blur_quality": {
      "hint": "One of AUTO , SPEED , PRECISION ."
    },
    "blur_radius": {
      "hint": "Sets the intensity of blurring."
    },
    "bme680_bsec_id": {
      "hint": "Sets the ID of the bme680_bsec component to refer to."
    },
    "bme68x_bsec2_id": {
      "hint": "The ID of the bme68x_bsec2_i2c component sensors will refer to."
    },
    "board": {
      "hint": "The PlatformIO board ID that should be used."
    },
    "board_flash_mode": {
      "hint": "The SPI mode of the flash chip."
    },
    "body": {
      "hint": "A HTTP body string to send with request."
    },
    "boot_is_good_after": {
      "hint": "The amount of time after which the boot is considered successful."
    },
    "boot_is_good_on_shutdown": {
      "hint": "Whether an orderly shutdown (entering deep sleep, a restart or"
    },
    "boot0_pin": {
      "hint": "Pin connected with \"BOOT0\" of STM32."
    },
    "bootloader": {
      "hint": "Bootloader type."
    },
    "border": {
      "hint": "Draw a border around the legend."
    },
    "border_color": {
      "hint": "Border color."
    },
    "border_opa": {
      "hint": "Opacity of the borders of the widget."
    },
    "border_post": {
      "hint": "If true the border will be drawn after all children of the widget have been drawn."
    },
    "border_side": {
      "hint": "Select which borders of the widgets to show (multiple can be specified as a YAML list,"
    },
    "border_width": {
      "hint": "Border thickness in pixels. 0 disables the border."
    },
    "borderless": {
      "hint": "Whether to draw the display window with or without borders"
    },
    "bottom_layer": {
      "hint": "A special layer placed behind all pages."
    },
    "bp1658cj_id": {
      "hint": "Manually specify the ID of the Component/Hub."
    },
    "bp5758d_id": {
      "hint": "Manually specify the ID of the Component/Hub."
    },
    "bpp": {
      "hint": "The bit depth of the rendered font from OpenType/TrueType, for anti-aliasing."
    },
    "breath_rate": {
      "hint": "Radar-detected respiratory rate during the first 60 seconds."
    },
    "breath_voc_equivalent": {
      "hint": "Configuration for the Breath VOC equivalent humidity sensor."
    },
    "brennenstuhl": {
      "hint": "Trigger on a decoded brennenstuhl RF code with the given data."
    },
    "brightness": {
      "hint": "The id of the float Output Component to use for the brightness."
    },
    "brightness_global": {
      "hint": "Set the value of the current to be sink by all pins configured as led driver."
    },
    "brightness_mode": {
      "hint": "Define if the current to be sink will be confgured globaly or per pin configured as led driver."
    },
    "broker": {
      "hint": "The host of your MQTT broker."
    },
    "bs_limit": {
      "hint": "Saturation point for data rate offset compensation."
    },
    "bs_post_ki": {
      "hint": "Clock recovery integral gain after sync word detection."
    },
    "bs_post_kp": {
      "hint": "Clock recovery proportional gain after sync word detection."
    },
    "bs_pre_ki": {
      "hint": "Clock recovery integral gain before sync word detection."
    },
    "bs_pre_kp": {
      "hint": "Clock recovery proportional gain before sync word detection."
    },
    "bssid": {
      "hint": "Expose the BSSID of the currently connected WiFi network as a text sensor."
    },
    "buffer_duration": {
      "hint": "The duration of the internal ring buffer."
    },
    "buffer_expand_size": {
      "hint": "Number of bytes to expand the output buffer if it is too small to hold the JPEG-encoded image."
    },
    "buffer_size": {
      "hint": "The percentage of screen size to allocate buffer memory."
    },
    "buffer_size_rx": {
      "hint": "Change HTTP receive buffer size."
    },
    "buffer_size_tx": {
      "hint": "Change HTTP transmit buffer size."
    },
    "build_flags": {
      "hint": "Extra compiler flags to add to the build."
    },
    "build_path": {
      "hint": "Customize where ESPHome will store the build files for your node."
    },
    "bus_id": {
      "hint": "The id to use for this virtual I2C Bus."
    },
    "bus_mode": {
      "hint": "Select the SPI bus mode for the display driver."
    },
    "bus_sleep": {
      "hint": "Put the 1-Wire bus to sleep when not in use."
    },
    "bus_voltage": {
      "hint": "Use the bus voltage (voltage of the high side contact) value of the sensor in V."
    },
    "busy_pin": {
      "hint": "The BUSY pin, if used."
    },
    "button": {
      "hint": "The 4-bit button to listen for."
    },
    "bypass_armed_home": {
      "hint": "This binary sensor will not trigger the alarm when in armed_home state."
    },
    "bypass_armed_night": {
      "hint": "This binary sensor will not trigger the alarm when in armed_night state."
    },
    "bypass_auto": {
      "hint": "This binary sensor will be automatically bypassed if left on/open at the time of arming."
    },
    "byronsx": {
      "hint": "Trigger on a decoded Byron SX Doorbell RF remote code with the given data."
    },
    "byte_order": {
      "hint": "The byte order of the data LVGL outputs; either big_endian or little_endian ."
    },
    "c_pin": {
      "hint": "Row address bit 2."
    },
    "ca_certificate_path": {
      "hint": "file path: Path to a CA certificate bundle."
    },
    "calculated_lux": {
      "hint": "The value of the calculated lux."
    },
    "calculation_type": {
      "hint": "The AQI calculation standard to use."
    },
    "calibration": {
      "hint": "The calibration values necessary for this phase's sensors to report correct values."
    },
    "canalsat": {
      "hint": "Trigger on a decoded CanalSat remote code with the given data."
    },
    "canalsatld": {
      "hint": "Trigger on a decoded CanalSatLD remote code with the given data."
    },
    "cap1188_id": {
      "hint": "The ID of the CAP1188 defined above."
    },
    "capacitance": {
      "hint": "This setting will add capacitance to the series RLC antenna on the product to help tune its resonance."
    },
    "capture_response": {
      "hint": "Enable capturing the response from the Home Assistant action call."
    },
    "carbon_monoxide": {
      "hint": "The Carbon Monoxide sensor data."
    },
    "carrier_duty_percent": {
      "hint": "The carrier duty cycle for signal demodulation in the RMT peripheral in Hz."
    },
    "carrier_frequency": {
      "hint": "The carrier frequency for signal demodulation in the RMT peripheral in Hz."
    },
    "carrier_sense_above_threshold": {
      "hint": "Only accept packets when carrier sense indicates a signal is present."
    },
    "carrier_sense_abs_thr": {
      "hint": "Absolute RSSI threshold for carrier sense."
    },
    "carrier_sense_rel_thr": {
      "hint": "Relative RSSI threshold for carrier sense, compared to the current noise floor."
    },
    "cell_1_voltage": {
      "hint": "The voltage of cell number 1."
    },
    "cells_number": {
      "hint": "The number of cells in series in the battery pack."
    },
    "centered_on_display": {
      "hint": "For use with multiple display screens."
    },
    "certificate_authority": {
      "hint": "Only on ESP32."
    },
    "cf_pin": {
      "hint": "The pin CF is connected to."
    },
    "cf1_pin": {
      "hint": "The pin CF1 is connected to."
    },
    "change_mode_every": {
      "hint": "After how many updates to cycle between the current/voltage measurement mode."
    },
    "channel": {
      "hint": "The channel of the ADC128S102 to use."
    },
    "channel_1": {
      "hint": "Use channel 1."
    },
    "channel_2": {
      "hint": "Use channel 2."
    },
    "channel_3": {
      "hint": "Use channel 3."
    },
    "channel_4": {
      "hint": "Use channel 4."
    },
    "channel_5": {
      "hint": "Use channel 5."
    },
    "channel_6": {
      "hint": "Use channel 6."
    },
    "channel_colors": {
      "hint": "The order in which the strip expects its color channels."
    },
    "channel_mix": {
      "hint": "How audio channel data is routed to the left and right outputs."
    },
    "channel_spacing": {
      "hint": "frequency: Spacing between channels."
    },
    "channels": {
      "hint": "A list of channels that are mapped to certain values."
    },
    "characteristic_uuid": {
      "hint": "UUID: UUID of the service's characteristic to write to."
    },
    "charge": {
      "hint": "Calculated charge output, Ah (INA228/229 only)"
    },
    "charge_coulombs": {
      "hint": "Calculated charge output, Coulombs (INA228/229 only)"
    },
    "charge_enabled": {
      "hint": "Enable or disable charging."
    },
    "charge_voltage": {
      "hint": "Charge voltage in millivolts."
    },
    "charger_source_priority": {
      "hint": "charger source priority;"
    },
    "charging_mos_enabled": {
      "hint": "The BMS charging MOS status to enable the recharge of the battery."
    },
    "charging_status": {
      "hint": "charging status"
    },
    "charging_to_floating_mode": {
      "hint": "charging to floating mode"
    },
    "check": {
      "hint": "The 4-bit check to listen for."
    },
    "chime": {
      "hint": "When set true, the chime callback will be called whenever the sensor goes from closed to open."
    },
    "chip_lines_style": {
      "hint": "How are the lines in Multiline Mode connected?"
    },
    "chip_temperature": {
      "hint": "Use the chip temperature value."
    },
    "chipset": {
      "hint": "The chipset to apply known timings from."
    },
    "ckv_pin": {
      "hint": "The CKV pin for the Inkplate display."
    },
    "cl_pin": {
      "hint": "The CL pin for the Inkplate display."
    },
    "clean_session": {
      "hint": "Whether the broker will clean the MQTT session after disconnect."
    },
    "cleaning_status": {
      "hint": "A text sensor that indicates cleaning status."
    },
    "clear": {
      "hint": "The reading for the clear channel."
    },
    "clear_channel": {
      "hint": "Value of the clear (without a color filter) channel, relative to the maximum value for the chosen integration time."
    },
    "clear_gain_calibration": {
      "hint": "A button to clear the gain calibration from flash memory and use default values, or values defined under gain_voltage and gain_ct."
    },
    "clear_impedance": {
      "hint": "Clear the impedance information if a weight reading without impedance is received."
    },
    "clear_interference": {
      "hint": "Clear interference areas."
    },
    "clear_keys": {
      "hint": "Keys used to cancel building the key sequence."
    },
    "clear_offset_calibration": {
      "hint": "A button to clear the gain calibration from flash memory and use default values, or values defined under offset_voltage and offset_current."
    },
    "clear_power_offset_calibration": {
      "hint": "A button to clear the gain calibration from flash memory and use default values, or values defined under offset_active_power and offset_reactive_power."
    },
    "client_certificate": {
      "hint": "Only on esp32."
    },
    "client_certificate_key": {
      "hint": "Only on esp32."
    },
    "client_id": {
      "hint": "The client id to use for opening connections."
    },
    "climate_id": {
      "hint": "The ID of the pid climate to get the values from."
    },
    "clip_corner": {
      "hint": "If set to true , overflowing content will be clipped off by the widget’s rounded corners ( radius > 0 )."
    },
    "clk_pin": {
      "hint": "The SPI clock pin."
    },
    "clock": {
      "hint": "The frequency of the clock crystal used on the MCP2515 device."
    },
    "clock_inhibit_pin": {
      "hint": "Pin connected to SN74HC165 Clock Inhibit (CLK INH) pin."
    },
    "clock_phase": {
      "hint": "Invert clock phase."
    },
    "clock_pin": {
      "hint": "The pin which CLK is"
    },
    "clock_resolution": {
      "hint": "The clock resolution used by the RMT peripheral in Hz."
    },
    "clock_speed": {
      "hint": "The SPI clock speed."
    },
    "close_action": {
      "hint": "The action that should be performed when the remote requests the cover to be closed."
    },
    "close_duration": {
      "hint": "The amount of time it takes the cover to close from the fully-open state."
    },
    "close_endstop": {
      "hint": "The ID of the Binary Sensor that turns on when the closed position is reached."
    },
    "close_moving_current_threshold": {
      "hint": "The amount of current in Amps the motor should drain to consider the cover is closing."
    },
    "close_obstacle_current_threshold": {
      "hint": "The amount of current in Amps the motor should drain to consider the cover is blocked during closing."
    },
    "close_obstacle_sensor": {
      "hint": "The ID of the Binary Sensor that turns on when an obstacle that blocks the close direction is detected."
    },
    "close_sensor": {
      "hint": "The close current sensor."
    },
    "co2": {
      "hint": "The CO2 data from the sensor in parts per million (ppm)."
    },
    "co2_equivalent": {
      "hint": "Configuration for the CO₂ equivalent sensor."
    },
    "code": {
      "hint": "The raw dump in an array of hex"
    },
    "codecs": {
      "hint": "A mapping that enables and configures individual audio codecs."
    },
    "codes": {
      "hint": "A list of codes for disarming the alarm, if requires_code_to_arm set to true then for arming the alarm too."
    },
    "coding_rate": {
      "hint": "Coding rate, values can be CR_4_5, CR_4_6, CR_4_7 or CR_4_8."
    },
    "coefficient": {
      "hint": "only for LINEAR type: The coefficient to multiply the sensor's state by before summing all source sensor states."
    },
    "col": {
      "hint": "The column of the key."
    },
    "col_start": {
      "hint": "The starting column offset."
    },
    "cold": {
      "hint": "If true, enter cold sleep instead of warm sleep."
    },
    "cold_junction": {
      "hint": "The information for the Cold Junction temperature sensor."
    },
    "cold_white": {
      "hint": "The id of the float Output Component to use for the cold white channel."
    },
    "cold_white_color_temperature": {
      "hint": "The coldest color temperature supported by this light."
    },
    "collect_headers": {
      "hint": "List of the names of HTTP headers to collect from the response."
    },
    "color": {
      "hint": "The color of the stop point."
    },
    "color_brightness": {
      "hint": "percentage: The brightness of the color lights."
    },
    "color_correct": {
      "hint": "Apply a color correction to each color channel."
    },
    "color_depth": {
      "label": "Color depth",
      "hint": "Bits per pixel of the LVGL draw buffer. Must match the connected display."
    },
    "color_filter_opa": {
      "hint": "Opacity of the color filter."
    },
    "color_interlock": {
      "hint": "When enabled, this will prevent white leds being on at the same time as RGB leds."
    },
    "color_mode": {
      "hint": "For lights that support more than one color mode, the color mode that will be activated."
    },
    "color_order": {
      "hint": "Should be one of bgr (default) or rgb."
    },
    "color_palette": {
      "hint": "When using 8 bit colors, this controls the type of color palette that will be used in the ESP's internal 8-bits-per-pixel buffer."
    },
    "color_palette_images": {
      "hint": "A list of image files that will be used to generate the color palette for the display."
    },
    "color_temperature": {
      "hint": "The id of the float Output Component to use for the color temperature."
    },
    "columns": {
      "hint": "A list of pins where the vertical matrix lines are connected, in order from left to right."
    },
    "command": {
      "hint": "2-byte hex: The KMP command code (e.g."
    },
    "command_repeats": {
      "hint": "The number of times the command bytes are sent in one transmission."
    },
    "command_spacing": {
      "hint": "Sets the minimum time between commands sent to the Nextion display."
    },
    "comment": {
      "hint": "Additional text information about this node."
    },
    "compensation": {
      "hint": "The block containing sensors used for compensation."
    },
    "compile_process_limit": {
      "hint": "The maximum number of simultaneous compile processes to run."
    },
    "component_id": {
      "hint": "The ID (the number, not name!) of the component to track."
    },
    "component_name": {
      "hint": "The name of the Nextion component."
    },
    "compression": {
      "hint": "The compression algorithm used for the embedded web assets."
    },
    "compressor_current": {
      "hint": "Sensor for climate compressor current."
    },
    "compressor_frequency": {
      "hint": "Sensor for climate compressor frequency."
    },
    "compressor_status": {
      "hint": "A binary sensor that indicates Haier climate compressor activity."
    },
    "condition": {
      "hint": "Condition: The condition to check to determine which branch to take."
    },
    "conductivity": {
      "hint": "The conductivity sensor."
    },
    "configuration": {
      "hint": "The type of circuit, one of DOWNSTREAM or UPSTREAM."
    },
    "configuration_status": {
      "hint": "configuration status"
    },
    "connection_scan_window": {
      "hint": "The scan window used while a BLE connection is active, for example while a Bluetooth proxy is connected to a device."
    },
    "connection_timeout": {
      "hint": "The maximum time to wait for a BLE connection to be established."
    },
    "constant_brightness": {
      "hint": "When enabled, this will keep the overall brightness of the cold and warm white channels constant by limiting the combined output to 100% of a single channel."
    },
    "continue_on_error": {
      "hint": "If set to false, the surrounding automation will be aborted if the send fails."
    },
    "continuous": {
      "hint": "When true the scanner runs permanently, with the duration period used only for per-session bookkeeping."
    },
    "continuous_mode": {
      "hint": "Set if the ADS1115 should continuously measure voltages or only measure them when an update is called."
    },
    "contrast": {
      "hint": "The contrast to apply to the picture, from -2 to 2."
    },
    "control_method": {
      "hint": "(supported only by hOn) Defines control method (should be supported by AC)."
    },
    "control_packet_size": {
      "hint": "(supported only by hOn) Define the size of the control packet."
    },
    "control_parameters": {
      "hint": "Control parameters of the PID controller."
    },
    "controller_configuration": {
      "hint": "byte [0-255]: Controller configuration"
    },
    "controller_id": {
      "hint": "byte [0-255]: Controller ID code"
    },
    "controller_product_type": {
      "hint": "byte [0-255]: Controller product type"
    },
    "controller_product_version": {
      "hint": "byte [0-255]: Controller product version"
    },
    "conversation_timeout": {
      "hint": "How long to wait before resetting the conversation_id sent to the voice assist pipeline, which contains the context of the current assist pipeline."
    },
    "conversion_rate": {
      "hint": "The number of digital to analog conversions perfomed per second."
    },
    "cool_action": {
      "hint": "The action to call when the climate device wants to cooling mode and decrease the current temperature."
    },
    "cool_deadband": {
      "hint": "The minimum temperature differential (temperature above the set point) before calling the cooling action."
    },
    "cool_mode": {
      "hint": "The action to call when the climate device is placed into cool mode (it may cool as required, but not heat)."
    },
    "cool_output": {
      "hint": "The ID of a float output that decreases the current temperature."
    },
    "cool_overrun": {
      "hint": "The minimum temperature differential (cooling beyond the set point) before calling the idle action."
    },
    "coolix": {
      "hint": "Trigger on a decoded Coolix remote code with the given data."
    },
    "coulomb": {
      "hint": "State of Charge in percent."
    },
    "count": {
      "hint": "The number of times the action should be repeated."
    },
    "count_mode": {
      "hint": "Configure how the counter should behave on a detected rising edge/falling edge."
    },
    "counts": {
      "hint": "Raw measurement data from the sensor electronics (dimensionless quantity)."
    },
    "course": {
      "hint": "Include the measured course as a sensor"
    },
    "courtesy_response": {
      "hint": "Configuration block to enable the courtesy response feature when the device is acting as a Modbus server."
    },
    "cpu_frequency": {
      "hint": "The CPU frequency to use."
    },
    "crc_enable": {
      "hint": "Enable CRC-16 calculation and checking."
    },
    "crc_initial": {
      "hint": "Initial CRC value as a hex integer."
    },
    "crc_inverted": {
      "hint": "Inverts the CRC if enabled."
    },
    "crc_polynomial": {
      "hint": "CRC polynomial as a hex integer."
    },
    "crc_size": {
      "hint": "Size of the CRC in bytes, either 1 or 2."
    },
    "cs_pin": {
      "hint": "The SPI cable select pin to use"
    },
    "css_include": {
      "hint": "local file: Path to local file to be included in web server index page."
    },
    "css_url": {
      "hint": "url: The URL that should be used for the CSS stylesheet."
    },
    "cst226_id": {
      "hint": "Manually specify the ID of the touchscreen."
    },
    "cst328_id": {
      "hint": "Manually specify the ID of the touchscreen."
    },
    "current": {
      "hint": "Report the RMS current value of the neutral in amperes (A)."
    },
    "current_1": {
      "hint": "Use the current value of the channel 1 in amperes."
    },
    "current_2": {
      "hint": "Use the current value of the channel 2 in amperes."
    },
    "current_a": {
      "hint": "Use the current value of the A channel in amperes."
    },
    "current_b": {
      "hint": "Use the current value of the B channel in amperes."
    },
    "current_calibration": {
      "hint": "Enables current calibration configuration."
    },
    "current_gain": {
      "hint": "The value for the NIGAIN calibration register."
    },
    "current_gain_a": {
      "hint": "Set the current amplification of the A channel."
    },
    "current_gain_b": {
      "hint": "Set the current amplification of the B channel."
    },
    "current_hpf": {
      "hint": "Enables and disables the High-pass Filter on the current processing path in the CS5460A."
    },
    "current_humidity_state_topic": {
      "hint": "The topic to publish current humidity changes to."
    },
    "current_max_ac_charging_current": {
      "hint": "current max ac charging current;"
    },
    "current_max_charging_current": {
      "hint": "current max charging current;"
    },
    "current_operation": {
      "hint": "The current operation mode to publish."
    },
    "current_pga_gain_a": {
      "hint": "Set the input PGA current amplification of the A channel."
    },
    "current_pga_gain_b": {
      "hint": "Set input PGA current amplification of the B channel."
    },
    "current_phases": {
      "hint": "The number of phases the meter has, 2 or, 3 The 6 Channel Expandable Energy Meter should be set to 3, and the Split Single Phase meter should be set to 2."
    },
    "current_reference": {
      "hint": "The calibration parameter for current readings."
    },
    "current_resistor": {
      "hint": "The value of the shunt resistor for current measurement."
    },
    "current_state": {
      "hint": "Current state."
    },
    "current_temperature": {
      "hint": "The current measured temperature to publish."
    },
    "current_temperature_state_topic": {
      "hint": "The topic to publish current temperature changes to."
    },
    "custom": {
      "hint": "[Kamstrup Meter Protocol [KMP]](https://esphome.io/components/sensor/kamstrup_kmp#example-configuration-entry)"
    },
    "custom_distance_empty": {
      "hint": "distance sensor will read when it should be considered empty (0%)."
    },
    "custom_distance_full": {
      "hint": "distance sensor will read when it should be considered full (100%)."
    },
    "custom_fan_mode": {
      "hint": "Set one of the supported custom_fan_modes of the climate device."
    },
    "custom_fan_modes": {
      "hint": "List of supported custom fan modes."
    },
    "custom_mode": {
      "hint": "Settings and go to the Custom Mode option."
    },
    "custom_mode_end": {
      "hint": "Used to indicate whether or not the current radar is in a customised mode amongst the setup functions."
    },
    "custom_mode_num": {
      "hint": "The custom mode number that the radar is currently in."
    },
    "custom_motion_distance": {
      "hint": "Valid only for underlying open functions."
    },
    "custom_motion_speed": {
      "hint": "Valid only for underlying open functions."
    },
    "custom_patterns": {
      "hint": "While the dlms_meter natively supports most devices, some meters use"
    },
    "custom_presence_of_detection": {
      "hint": "Valid only for underlying open functions."
    },
    "custom_preset": {
      "hint": "Set one of the supported custom_presets of the climate device."
    },
    "custom_presets": {
      "hint": "List of supported custom presets."
    },
    "custom_services": {
      "hint": "Enable compilation of custom API services for external components that use the C++ CustomAPIDevice class."
    },
    "custom_set_end": {
      "hint": "Valid only in custom mode settings."
    },
    "custom_spatial_motion_value": {
      "hint": "Valid only for underlying open functions."
    },
    "custom_spatial_static_value": {
      "hint": "Valid only for underlying open functions."
    },
    "custom_unman_time": {
      "hint": "Valid only in custom mode settings."
    },
    "custom_waveform": {
      "hint": "Sets a custom predefined waveform for the display."
    },
    "cw_current": {
      "hint": "current: The current used for the white channel."
    },
    "cycle": {
      "hint": "Whether or not to set the number to its maximum value when the decrement pushes the value below its minimum value."
    },
    "d_pin": {
      "hint": "Row address bit 3."
    },
    "d0": {
      "hint": "The pin where the D0 output of the Wiegand's interface connects."
    },
    "d1": {
      "hint": "The pin where the D1 output of the Wiegand's interface connects."
    },
    "dac": {
      "hint": "Enable DAC output."
    },
    "dac_output": {
      "hint": "Control the DAC Audio output."
    },
    "dac7678_id": {
      "hint": "Manually specify the ID of the DAC7678 hub."
    },
    "dark_mode": {
      "hint": "Enables LVGL’s built-in dark default theme, which applies a dark color scheme (dark backgrounds, light text) to all widgets automatically."
    },
    "data": {
      "hint": "Raw data payload for this entry."
    },
    "data_bits": {
      "hint": "The number of data bits used on the UART bus."
    },
    "data_pin": {
      "hint": "The pin used for DATA."
    },
    "data_pins": {
      "hint": "The data lanes of the camera, this must be a list of 8 GPIO pins."
    },
    "data_rate": {
      "hint": "The SPI data rate."
    },
    "data_template": {
      "hint": "Optional template data to perform the action with."
    },
    "datapoint_type": {
      "hint": "The datapoint type one of raw, string, bool, int, uint, enum, bitmask or any."
    },
    "datapoints": {
      "hint": "The list of datapoints."
    },
    "date": {
      "hint": "The value to set the datetime to."
    },
    "datetime": {
      "hint": "The value to set the datetime to."
    },
    "days": {
      "hint": "The string to use for the days element."
    },
    "dc_blocking_filter": {
      "hint": "Enable the digital DC blocking filter."
    },
    "dc_power": {
      "hint": "The inverter's input power."
    },
    "dc_voltage": {
      "hint": "The voltage of your DC source."
    },
    "dcdc": {
      "hint": "Enable DC/DC converter for REG1 stage."
    },
    "dci_of_r": {
      "hint": "Use the DCI of R value of the sensor."
    },
    "dci_of_s": {
      "hint": "Use the DCI of S value of the sensor."
    },
    "dci_of_t": {
      "hint": "Use the DCI of T value of the sensor."
    },
    "deadband_output_averaging_samples": {
      "hint": "Typically when inside the deadband the PID Controller has reached a state of equilibrium, so it advantageous to use a higher number of output samples like 10-30 samples."
    },
    "deadband_parameters": {
      "hint": "Enables a deadband to stabilise and minimise changes in the output when the temperature is close to the target temperature."
    },
    "deassert_rts_dtr": {
      "hint": "Causes ESPHome to sequentially drive DTR and RTS false after opening"
    },
    "debounce": {
      "hint": "Only valid when using NATIVE debounce mode."
    },
    "debounce_count": {
      "hint": "Sets the debounce count; if the measured values continue to exceed the threshold for n + 1 times, the touch sensor state changes."
    },
    "debounce_mode": {
      "hint": "How the component debounces the motion sensor's signal."
    },
    "debounce_time": {
      "hint": "The debounce time is common to all IOs."
    },
    "debug": {
      "hint": "Options for debugging communication on the UART hub, see Debugging."
    },
    "debug_scheduler": {
      "hint": "If set, the scheduler will print debug information about scheduled tasks at log level DEBUG."
    },
    "decay_mode": {
      "hint": "The decay mode you want to use with the h-bridge."
    },
    "deceleration": {
      "hint": "The same as acceleration, but for when the motor is decelerating shortly before reaching the set position."
    },
    "decibel_reduction": {
      "hint": "The reduction of the media stream in decibels."
    },
    "decryption_key": {
      "hint": "Specify if your smart meter uses encryption."
    },
    "default": {
      "hint": "default run duration for timer wakeup and any unspecified wakeup reason."
    },
    "default_font": {
      "label": "Default font",
      "hint": "ID of a font: component used to render text and symbols where a widget does not set its own text_font. Defaults to LVGL's built-in montserrat_14."
    },
    "default_group": {
      "label": "Default group",
      "hint": "Name of the input group that new widgets join automatically, so an encoder or keypad can navigate them."
    },
    "default_preset": {
      "hint": "The name of the preset to use by default."
    },
    "default_target_temperature": {
      "hint": "The default target temperature (setpoint) for the control algorithm."
    },
    "default_target_temperature_high": {
      "hint": "The default high target temperature for the control algorithm."
    },
    "default_target_temperature_low": {
      "hint": "The default low target temperature for the control algorithm."
    },
    "default_transition_length": {
      "hint": "The default transition length to use when no transition length is set in the light call."
    },
    "defrost_status": {
      "hint": "A binary sensor that indicates defrost procedure activity."
    },
    "del": {
      "hint": "The ID of a Binary Sensor , to be used as DEL key."
    },
    "delay": {
      "hint": "Delay to proceed to the next timing."
    },
    "delta": {
      "hint": "This parameter allows you to manually specify the allowed difference between what Pronto code is specified, and what IR signal has been sent by the remote control."
    },
    "denoise_cap_level": {
      "hint": "Select internal reference capacitance of denoise channel."
    },
    "denoise_grade": {
      "hint": "Sets the denoise range of the denoise channel."
    },
    "derivative_averaging_samples": {
      "hint": "average the derivative term over this many samples."
    },
    "dest": {
      "hint": "The DFA value corresponding to your device (see below)."
    },
    "destination_address": {
      "hint": "The destination address to trigger on."
    },
    "detection_distance": {
      "hint": "Distance in cm of target."
    },
    "detection_range": {
      "hint": "The CO_2 sensor can be configured to detect different ranges: 0-2000ppm, 0-5000ppm, or 0-10000ppm."
    },
    "detection_segments": {
      "hint": "A list of detection segments."
    },
    "deviation": {
      "hint": "frequency: Transmitter FSK frequency deviation, values range from 0 to 100 kHz."
    },
    "device": {
      "hint": "The device to trigger on, see dumper output for more info."
    },
    "device_class": {
      "hint": "The device class for the sensor."
    },
    "device_factor": {
      "hint": "The default is 53.0."
    },
    "device_height": {
      "hint": "The device height."
    },
    "device_mode": {
      "hint": "device mode response"
    },
    "device_type": {
      "hint": "OpenThread Device Type, either FTD or MTD."
    },
    "device_width": {
      "hint": "The device width."
    },
    "deviceaddress": {
      "hint": "The address of the chip."
    },
    "devices": {
      "hint": "Sub-devices to group entities under."
    },
    "dfrobot_sen0395_id": {
      "hint": "The ID of the DFRobot mmWave component defined above."
    },
    "diff_mode": {
      "hint": "Enable differential mode measurement."
    },
    "digital_gain": {
      "hint": "Similar to gain ; provides an additional multipler, further increasing range."
    },
    "dimensions": {
      "hint": "Dimensions of the screen, specified either as width x height (e.g 320x240 ) or with separate config keys."
    },
    "dio_pin": {
      "hint": "The pin you have the DIO line hooked up to."
    },
    "dio0_pin": {
      "hint": "Digital IO pin 0."
    },
    "dio1_pin": {
      "hint": "Digital IO pin 1."
    },
    "dir_pin": {
      "hint": "The DIRECTION pin of the A4988 stepper driver."
    },
    "direction": {
      "hint": "The direction of the gradient: hor (or horizontal ) or ver (or vertical )."
    },
    "direction_change_wait_time": {
      "hint": "Stops cover and forces a wait time between changes in direction, and takes it into account when computing cover position (useful to protect motors)."
    },
    "direction_command_topic": {
      "hint": "The topic to receive fan direction commands on (options: forward, reverse, toggle)."
    },
    "direction_output": {
      "hint": "The id of the output to use for the direction state of the fan."
    },
    "direction_state_topic": {
      "hint": "The topic to publish fan direction state changes to (options: forward, reverse)."
    },
    "dis1": {
      "hint": "Measured DIS 1 value."
    },
    "dis2": {
      "hint": "Measured DIS 2 value."
    },
    "disable_bt_logs": {
      "hint": "When enabled, disables Bluetooth logging categories that are not used by the configured components."
    },
    "disable_led": {
      "hint": "Disables the on-board LED."
    },
    "disable_wiper_0": {
      "hint": "Disable wiper 0."
    },
    "disable_wiper_1": {
      "hint": "Disable wiper 1."
    },
    "disable_wiper_2": {
      "hint": "Disable wiper 2."
    },
    "disable_wiper_3": {
      "hint": "Disable wiper 3."
    },
    "disabled": {
      "hint": "Set to true to disable mDNS usage."
    },
    "disabled_by_default": {
      "hint": "If true, then this entity should not be added to any client's frontend, (usually Home Assistant) without the user manually enabling it (via the Home Assistant UI)."
    },
    "discharging_mos_enabled": {
      "hint": "The BMS discharging mos status to enable the load."
    },
    "disconnect_delay": {
      "hint": "The amount of time the BLE connection is maintained before being disconnected again."
    },
    "discover_ip": {
      "hint": "If Home Assistant automatic device discovery should be enabled."
    },
    "discovery": {
      "hint": "Manually enable/disable"
    },
    "discovery_object_id_generator": {
      "hint": "The object_id generator to use."
    },
    "discovery_prefix": {
      "hint": "The prefix to use for Home Assistant's MQTT discovery."
    },
    "discovery_retain": {
      "hint": "Whether to retain MQTT discovery messages so that entities are added automatically on Home Assistant restart."
    },
    "discovery_unique_id_generator": {
      "hint": "The unique_id generator to use."
    },
    "dish": {
      "hint": "Trigger on a decoded Dish Network remote code with the given data."
    },
    "disp_bg_color": {
      "label": "Display background color",
      "hint": "Fill color of the whole display behind all widgets."
    },
    "disp_bg_image": {
      "label": "Display background image",
      "hint": "ID of an image: component drawn as the display background."
    },
    "disp_bg_opa": {
      "label": "Display background opacity",
      "hint": "Opacity of the display background color/image, 0-255 or a percentage."
    },
    "display": {
      "hint": "ID of the display to render to."
    },
    "display_data_0_pin": {
      "hint": "The Data 0 pin for the Inkplate display."
    },
    "display_data_1_pin": {
      "hint": "The Data 1 pin for the Inkplate display."
    },
    "display_data_2_pin": {
      "hint": "The Data 2 pin for the Inkplate display."
    },
    "display_data_3_pin": {
      "hint": "The Data 3 pin for the Inkplate display."
    },
    "display_data_4_pin": {
      "hint": "The Data 4 pin for the Inkplate display."
    },
    "display_data_5_pin": {
      "hint": "The Data 5 pin for the Inkplate display."
    },
    "display_data_6_pin": {
      "hint": "The Data 6 pin for the Inkplate display."
    },
    "display_data_7_pin": {
      "hint": "The Data 7 pin for the Inkplate display."
    },
    "display_id": {
      "hint": "Manually specify the ID of the LCD display."
    },
    "displays": {
      "hint": "A list of display IDs where LVGL should perform rendering based on its configuration."
    },
    "distance": {
      "hint": "Distance in km to the front of the storm and not the distance to a lightning strike."
    },
    "distance_resolution": {
      "hint": "Control the gates distance resolution."
    },
    "dither": {
      "hint": "Specifies which dither method is used to process the image, only used for BINARY type images."
    },
    "div_ratio": {
      "hint": "The antenna is designed to resonate at 500kHz and so can be tuned with the following setting."
    },
    "divider": {
      "hint": "Value for the frequency divider."
    },
    "dns_address": {
      "hint": "Expose the DNS Address of the ESP as text sensor."
    },
    "dns1": {
      "hint": "IPv4 address: The main DNS server to use."
    },
    "dns2": {
      "hint": "IPv4 address: The backup DNS server to use."
    },
    "domain": {
      "hint": "Set the domain of the node hostname used for uploading."
    },
    "dooya": {
      "hint": "Trigger on a decoded Dooya RF remote code with the given data."
    },
    "double_buffer": {
      "hint": "Enable double buffering to prevent tearing."
    },
    "double_tap": {
      "hint": "Double tap detection."
    },
    "dout_pin": {
      "hint": "The DOUT (or DAT) pin."
    },
    "down": {
      "hint": "The ID of a Binary Sensor , to be used as DOWN key."
    },
    "draw_from_origin": {
      "hint": "When set, all partial display updates will start at the origin (0,0)."
    },
    "draw_rounding": {
      "hint": "An optional value to use for rounding draw areas to a specified boundary."
    },
    "drayton": {
      "hint": "Trigger on a decoded Drayton Digistat RF remote code with the given data."
    },
    "drdy_pin": {
      "hint": "Data-ready pin."
    },
    "drop_shadow_color": {
      "hint": "Set the color of the shadow."
    },
    "drop_shadow_offset_x": {
      "hint": "Set an offset on the shadow in pixels in X direction."
    },
    "drop_shadow_offset_y": {
      "hint": "Set an offset on the shadow in pixels in Y direction."
    },
    "drop_shadow_opa": {
      "hint": "Take a snapshot of a part of a widget and blur it."
    },
    "drop_shadow_quality": {
      "hint": "One of AUTO , SPEED , PRECISION ."
    },
    "drop_shadow_radius": {
      "hint": "Sets the intensity of blurring."
    },
    "dry_action": {
      "hint": "The action to call when the climate device should perform its drying (dehumidification) action."
    },
    "dry_mode": {
      "hint": "The action to call when the climate device is placed into dry mode (for dehumidification)."
    },
    "dtr_pin": {
      "hint": "GPIO pin to use as the DTR (Data Terminal Ready) modem control output."
    },
    "dump": {
      "hint": "Decode and dump these remote codes in the logs (at log.level=DEBUG)."
    },
    "duration": {
      "hint": "The length of each complete scan session."
    },
    "dustproof_installed": {
      "hint": "dustproof installed"
    },
    "dwl_req_pin": {
      "hint": "The pin connected to the PN7160's DWL_REQ line."
    },
    "dynamic_background_correction_status": {
      "hint": "True while the sensor is performing dynamic background correction."
    },
    "dyson": {
      "hint": "Trigger on a decoded dyson cool AM07 infrared remote code with the given data."
    },
    "e_pin": {
      "hint": "Row address bit 4."
    },
    "e25": {
      "hint": "Electrical Conductivity, reference at 25°C in dS/m."
    },
    "eap": {
      "hint": "See Enterprise Authentication."
    },
    "early_message": {
      "hint": "Displays early debug information, such as the boot reason."
    },
    "early_pin_init": {
      "hint": "Specifies whether pins should be initialised as early as possible to known values."
    },
    "ec": {
      "hint": "Measured EC value."
    },
    "ecc": {
      "hint": "The error correction code level you want to use."
    },
    "echo_pin": {
      "hint": "The input pin on which to wait for the echo."
    },
    "eco2": {
      "hint": "The eCO_2 data from the sensor in parts per million (ppm)."
    },
    "eco2_baseline": {
      "hint": "The information for the CO₂eq."
    },
    "eeprom_version": {
      "hint": "eeprom version"
    },
    "effects": {
      "hint": "A list of light effects to use for this light."
    },
    "eightbitcolor": {
      "hint": "Limits the supported color depth to eight bits."
    },
    "elevation": {
      "hint": "The elevation to cross."
    },
    "else": {
      "hint": "The action to perform if the condition evaluates to false."
    },
    "em_sat": {
      "hint": "true if the sensor reports the Emitter being saturated."
    },
    "emissivity": {
      "hint": "Emissivity coefficient is the measure of an object's ability to emit infrared energy compared to a theoretical perfect black body emitter."
    },
    "empty_tube": {
      "hint": "Reports if the tube is empty."
    },
    "emulation_message": {
      "hint": "When scanned by another NFC card/tag reader (such as a smartphone), this string is used as the content for an NDEF-formatted response."
    },
    "enable_adc": {
      "hint": "Enable or disable the ADC."
    },
    "enable_btm": {
      "hint": "Only on esp32."
    },
    "enable_ecc": {
      "hint": "For octal mode, enable ECC (Error Correction Code) for the PSRAM (default is off.) ECC is a method of detecting and correcting single-bit errors in memory."
    },
    "enable_full_printf": {
      "hint": "Enable full FILE-based printf support."
    },
    "enable_gain_calibration": {
      "hint": "If true this enables gain calibration for voltage, gain_voltage, and current, gain_ct, based on the values entered in the corresponding voltage, reference_voltage, and current, reference_current, ref…"
    },
    "enable_high_performance": {
      "hint": "Explicitly enables or disables high-performance networking optimizations."
    },
    "enable_hubs": {
      "hint": "Whether to include support for hubs."
    },
    "enable_ipv6": {
      "hint": "Enables IPv6 support."
    },
    "enable_offset_calibration": {
      "hint": "If true this enables fine grained offset noise 0 level calibration for voltage, offset_voltage, and current sensors, offset_current, & power offset for active, offset_active_power, and reactive power…"
    },
    "enable_on_boot": {
      "hint": "Bring the BLE stack up during boot."
    },
    "enable_pin": {
      "hint": "An optional pin to enable the display, if required."
    },
    "enable_private_network_access": {
      "hint": "Enables support for Private Network Access and the Private Network Access Permission Prompt."
    },
    "enable_rrm": {
      "hint": "Only on esp32."
    },
    "enable_rx_pin": {
      "hint": "Output pin to be used to switch the line between RX and TX."
    },
    "enable_scanf_float": {
      "hint": "Enable float support for scanf()/sscanf()."
    },
    "enable_serial": {
      "hint": "Force-enable the Arduino Serial object (UART0) for use in lambdas or external libraries."
    },
    "enable_serial1": {
      "hint": "Force-enable the Arduino Serial1 object (UART1) for use in lambdas or external libraries."
    },
    "enable_status_led": {
      "hint": "Enable or disable the status LED on the IC."
    },
    "enable_switch": {
      "hint": "The name for the switch component to be used to enable this valve to be run as a part of a full cycle of the system."
    },
    "enable_time": {
      "hint": "The time that the power supply needs for startup."
    },
    "enabled": {
      "hint": "Whether to enable the courtesy response feature."
    },
    "encoders": {
      "hint": "A list of rotary encoders interacting with the LVGL widgets on the display."
    },
    "encryption": {
      "hint": "If present, encryption will be enabled for the API."
    },
    "end": {
      "hint": "The ID of a Binary Sensor , to be used as END key."
    },
    "end_frame": {
      "hint": "The last frame to show in the loop; when this frame is reached it will loop back to start_frame."
    },
    "end_key_required": {
      "hint": "Only trigger on_result automation when one of the end_keys was pressed, and not when max_length characters have been entered."
    },
    "end_keys": {
      "hint": "Keys used to finish the key sequence and trigger the on_result automation."
    },
    "end_position": {
      "hint": "The raw position that should be considered the end (e.g."
    },
    "energy": {
      "hint": "The energy value of the channel 1 in kWh."
    },
    "energy_1": {
      "hint": "Use the energy counter value of the channel 1 in kWh."
    },
    "energy_2": {
      "hint": "Use the energy counter value of the channel 2 in kWh."
    },
    "energy_calibration": {
      "hint": "Enables energy calibration configuration."
    },
    "energy_joules": {
      "hint": "Calculated energy output, Joules (INA228/229 only)"
    },
    "energy_production_day": {
      "hint": "Use the export active energy value for same day of the sensor in kilo watt hours."
    },
    "energy_reference": {
      "hint": "The calibration parameter for cumulative energy readings."
    },
    "energy_total": {
      "hint": "Use the energy counter value of both channels in kWh."
    },
    "engineering_mode": {
      "hint": "enable/disable engineering mode."
    },
    "engineering_sample": {
      "hint": "ESP32-P4 only."
    },
    "enqueue": {
      "hint": "Whether to add the media file to the end of the pipeline's internal playlist."
    },
    "enter": {
      "hint": "The ID of a Binary Sensor , to be used as ENTER key."
    },
    "enter_button": {
      "hint": "The ID of a Binary Sensor , to be used as ENTER key."
    },
    "entity_category": {
      "hint": "The category of the entity."
    },
    "entity_id": {
      "hint": "The Home Assistant entity ID of the number to synchronize with."
    },
    "environment_variables": {
      "hint": "Environment variables to set during the build process."
    },
    "eot_level": {
      "hint": "Overrides the default end of transmit level."
    },
    "eq_preset": {
      "hint": "Eq Preset value."
    },
    "equation": {
      "hint": "The saturated vapor pressure equation to use (see below)."
    },
    "error": {
      "hint": "only for KALMAN type: The standard deviation of the sensor's measurements."
    },
    "error_message": {
      "hint": "An error message to include when success is false."
    },
    "es8388_id": {
      "hint": "Manually specify the ID for the Es8388 component."
    },
    "esc": {
      "hint": "The ID of a Binary Sensor , to be used as ESC key."
    },
    "esp32_ext1_wakeup": {
      "hint": "Use the EXT1 wakeup source of the ESP32 to wake from deep sleep to wake up on multiple pins."
    },
    "esp8266_disable_ssl_support": {
      "hint": "Determines whether to include HTTPS/SSL support in the firmware binary."
    },
    "esp8266_store_log_strings_in_flash": {
      "hint": "ESP8266 only."
    },
    "ethanol": {
      "hint": "The Ethanol (C2H5OH) sensor data."
    },
    "eui64": {
      "hint": "Expose the EUI-64 address as a text sensor."
    },
    "event": {
      "hint": "The event to create - must begin with esphome."
    },
    "event_acc": {
      "hint": "Amount of rain for this event (i.e."
    },
    "event_type": {
      "hint": "The type of event to trigger."
    },
    "event_types": {
      "hint": "A list of custom event identifiers that this template event is capable of triggering."
    },
    "existence_boundary": {
      "hint": "Valid only in custom mode settings."
    },
    "existence_threshold": {
      "hint": "Valid only in custom mode settings."
    },
    "exit_reparse_on_start": {
      "hint": "Request the Nextion exit Active Reparse Mode before setup of the display."
    },
    "expand": {
      "hint": "If set, the text will always include all elements, even if they are zero."
    },
    "expansion_valve_open_degree": {
      "hint": "Sensor for climate's expansion valve open degree."
    },
    "expire_after": {
      "hint": "Manually set the time in which the sensor values should be marked as “expired”/“unknown”."
    },
    "export_active_energy": {
      "hint": "Use the export active energy value of the sensor in kilowatt hours (kWh)."
    },
    "export_reactive_energy": {
      "hint": "Use the export reactive energy value of the sensor in kilovolt amps reactive hours (kVArh)."
    },
    "ext_addr": {
      "hint": "Expose the IEEE 802.15.4 Extended MAC address as a text sensor."
    },
    "ext_pan_id": {
      "hint": "8-byte Extended Personal Area Network ID (XPAN ID)."
    },
    "extended_range": {
      "hint": "Applies only to calculation_type: AQI."
    },
    "external_clock": {
      "hint": "The configuration of the external clock to drive the camera."
    },
    "external_clock_input": {
      "hint": "Enable external clock input."
    },
    "external_temperature": {
      "hint": "The external value of the sensor in °C."
    },
    "external_vcc": {
      "hint": "Set this to true if you have the VCC pin connected to an external power supply."
    },
    "extras": {
      "hint": "A list of font glyph configurations you'd like to include within this font, from other OpenType/TrueType files (eg."
    },
    "f1": {
      "hint": "The reading for the 415nm channel."
    },
    "f2": {
      "hint": "The reading for the 445nm channel."
    },
    "f3": {
      "hint": "The reading for the 480nm channel."
    },
    "f4": {
      "hint": "The reading for the 515nm channel."
    },
    "f5": {
      "hint": "The reading for the 555nm channel."
    },
    "f6": {
      "hint": "The reading for the 590nm channel."
    },
    "f7": {
      "hint": "The reading for the 630nm channel."
    },
    "f8": {
      "hint": "The reading for the 680nm channel."
    },
    "facility": {
      "hint": "The syslog facility to use."
    },
    "factory_reset": {
      "hint": "If set to true, a factory reset of the sensor will be performed (before changing other options if present)."
    },
    "fall_detected": {
      "hint": "An indication of whether the radar has detected a fall."
    },
    "falling_edge": {
      "hint": "What to do when a falling edge is detected."
    },
    "fan_mode": {
      "hint": "Set the fan mode of the climate device."
    },
    "fan_mode_auto_action": {
      "hint": "The action to call when the fan should be set to \"auto\" mode (the fan is controlled by the climate control system as required)."
    },
    "fan_mode_command_topic": {
      "hint": "The topic to receive fan mode commands on."
    },
    "fan_mode_diffuse_action": {
      "hint": "The action to call when the fan should direct its airflow over a broad area."
    },
    "fan_mode_focus_action": {
      "hint": "The action to call when the fan should direct its airflow at a specific area."
    },
    "fan_mode_high_action": {
      "hint": "The action to call when the fan should run at its maximum speed."
    },
    "fan_mode_low_action": {
      "hint": "The action to call when the fan should run at its minimum speed."
    },
    "fan_mode_medium_action": {
      "hint": "The action to call when the fan should run at an intermediate speed."
    },
    "fan_mode_middle_action": {
      "hint": "The action to call when the fan should direct its airflow at an intermediate area."
    },
    "fan_mode_off_action": {
      "hint": "The action to call when the fan should never run."
    },
    "fan_mode_on_action": {
      "hint": "The action to call when the fan should run continuously."
    },
    "fan_mode_quiet_action": {
      "hint": "The action to call when the fan should run at quiet speed."
    },
    "fan_mode_state_topic": {
      "hint": "The topic to publish fan mode changes to."
    },
    "fan_only_action": {
      "hint": "The action to call when the climate device should activate its fan only (but does not heat or cool)."
    },
    "fan_only_action_uses_fan_mode_timer": {
      "hint": "If set to true, the fan_only_action will share the same delay timer used for all fan_mode actions."
    },
    "fan_only_cooling": {
      "hint": "If set to true, when in the fan_only_mode climate mode, the fan_only_action will only be called when the observed temperature exceeds the upper set point plus cool_deadband."
    },
    "fan_only_mode": {
      "hint": "The action to call when the climate device is placed into fan-only mode (it may not heat or cool, but will activate its fan either immediately or, when fan_only_cooling is true, as needed based on th…"
    },
    "fan_with_cooling": {
      "hint": "If set to true, fan_only_action will be called whenever cool_action is called."
    },
    "fan_with_heating": {
      "hint": "If set to true, fan_only_action will be called whenever heat_action is called."
    },
    "fast_connect": {
      "hint": "If enabled, directly connects to WiFi network without doing a full scan first."
    },
    "fast_filter": {
      "hint": "See datasheet."
    },
    "fault_battery_open": {
      "hint": "fault battery open"
    },
    "fault_battery_short": {
      "hint": "fault battery short"
    },
    "fault_bus_over": {
      "hint": "fault bus over"
    },
    "fault_bus_soft_fail": {
      "hint": "fault bus soft fail"
    },
    "fault_bus_under": {
      "hint": "fault bus under"
    },
    "fault_code_record": {
      "hint": "fault code record"
    },
    "fault_count": {
      "hint": "Number of measurements."
    },
    "fault_current_sensor_failed": {
      "hint": "fault current sensor failed"
    },
    "fault_dc_dc_over_current": {
      "hint": "fault dc dc over current"
    },
    "fault_inverter_fault": {
      "hint": "fault inverter fault"
    },
    "fault_inverter_over_current": {
      "hint": "fault inverter over current"
    },
    "fault_inverter_soft_failed": {
      "hint": "fault inverter soft failed"
    },
    "fault_inverter_voltage_too_high": {
      "hint": "fault inverter voltage too high"
    },
    "fault_inverter_voltage_too_low": {
      "hint": "fault inverter voltage too low"
    },
    "fault_mppt_overload": {
      "hint": "fault mppt overload"
    },
    "fault_op_dc_voltage_over": {
      "hint": "fault op dc voltage over"
    },
    "fault_opvshort": {
      "hint": "fault opvshort"
    },
    "fault_self_test_failed": {
      "hint": "fault_self_test_failed"
    },
    "faults_present": {
      "hint": "faults present"
    },
    "field_strength_x": {
      "hint": "The field strength in microtesla along the X-Axis."
    },
    "field_strength_y": {
      "hint": "The field strength in microtesla along the Y-Axis."
    },
    "field_strength_z": {
      "hint": "The field strength in microtesla along the Z-Axis."
    },
    "file": {
      "hint": "Path to audio file."
    },
    "files": {
      "hint": "A list of media files to build into the firmware for on-device playback."
    },
    "filter": {
      "hint": "On-chip digital filter."
    },
    "filter_bandwidth": {
      "hint": "frequency: The receive filter bandwidth."
    },
    "filter_length_ask_ook": {
      "hint": "Averaging length for AGC in ASK/OOK modes."
    },
    "filter_length_fsk_msk": {
      "hint": "Averaging length for AGC in FSK/MSK modes."
    },
    "filter_mode": {
      "hint": "Sets the filter mode."
    },
    "filter_symbols": {
      "hint": "Filter out any data received with a length in symbols less than filter_symbols."
    },
    "filters": {
      "hint": "A list of filters to apply on the binary sensor values such as inverting signals."
    },
    "finger_id": {
      "hint": "The slot number of the enrolled fingerprint to delete."
    },
    "firmware_version": {
      "hint": "Value Configuration: The firmware version of the device."
    },
    "first": {
      "hint": "uint32_t: The first 24-bit Coolix code to trigger on, see dumper output for more info."
    },
    "flash_frequency": {
      "hint": "The SPI frequency used to access the flash chip, for example 80MHz or 40MHz."
    },
    "flash_mode": {
      "hint": "The SPI mode used to access the flash chip."
    },
    "flash_size": {
      "hint": "The amount of flash memory available on the ESP32 board/module."
    },
    "flash_transition_length": {
      "hint": "The transition length to use when flash is called."
    },
    "flash_write_interval": {
      "hint": "Customize the frequency in which data is flushed to the flash."
    },
    "flip_x": {
      "hint": "Flip the horizontal axis on the screen."
    },
    "flip_y": {
      "hint": "Flip the vertical axis on the screen."
    },
    "flow": {
      "hint": "The estimated volume flow of the medium in m³/h."
    },
    "flow_control_pin": {
      "hint": "The pin used to switch flow control."
    },
    "flow_direction_wrong": {
      "hint": "Reports if the flow direction is incorrect."
    },
    "flow_rate_out_of_range": {
      "hint": "Reports if the flow rate is out of the measurable range."
    },
    "foc_bs_cs_gate": {
      "hint": "Freeze frequency offset compensation and clock recovery until carrier sense is asserted."
    },
    "foc_limit": {
      "hint": "Saturation point for frequency offset compensation."
    },
    "foc_post_k": {
      "hint": "Frequency compensation loop gain after sync word detection."
    },
    "foc_pre_k": {
      "hint": "Frequency compensation loop gain before sync word detection."
    },
    "folder": {
      "hint": "The folder number."
    },
    "follow_redirects": {
      "hint": "Enable following HTTP redirects."
    },
    "font": {
      "hint": "Font: Specifies the font to use"
    },
    "font_id": {
      "hint": "The font id for the component"
    },
    "force_dataset": {
      "hint": "Forces ESPHome to override any OpenThread network dataset previously stored on the device, ensuring the configured dataset is applied at startup."
    },
    "force_update": {
      "hint": "If true, this option will force the frontend (usually Home Assistant) to create a state changed event when the sensor updates even if the value stayed the same."
    },
    "foreground_color": {
      "hint": "Specifies the foreground color to use."
    },
    "foreground_pressed_color": {
      "hint": "The foreground color when pressed"
    },
    "formaldehyde": {
      "hint": "Use the formaldehyde (HCHO) concentration in µg per cubic meter for the PMS5003S and PMS5003ST type sensors."
    },
    "format": {
      "hint": "The format for the message in printf-style."
    },
    "forward_active_energy": {
      "hint": "Report the forward active energy value of this phase in watt-hours (Wh)."
    },
    "four_way_valve_status": {
      "hint": "A binary sensor that indicates four way valve status."
    },
    "frame_buffer_count": {
      "hint": "The number of frame buffers to use when reading from the camera sensor."
    },
    "frame_buffer_location": {
      "hint": "The memory area used for storing the frame buffers."
    },
    "framework": {
      "hint": "Options for the underlying framework used by ESPHome."
    },
    "freeze": {
      "hint": "Controls when AGC gain is frozen (held constant)."
    },
    "frequency": {
      "hint": "frequency: The AC line frequency of the supply voltage."
    },
    "frequency_status": {
      "hint": "Reports status based on frequency thresholds."
    },
    "friendly_name": {
      "hint": "This name is sent to the frontend and used by Home Assistant as the integration and device name."
    },
    "from": {
      "hint": "The type of the keys in the mapping."
    },
    "fsk_deviation": {
      "hint": "frequency: Frequency deviation for FSK/GFSK modulation."
    },
    "full_refresh": {
      "label": "Full refresh",
      "hint": "Redraw the entire screen every frame instead of only changed areas. Needed for some displays, costs performance."
    },
    "full_spectrum": {
      "hint": "The reading for the full spectrum sensor."
    },
    "full_spectrum_counts": {
      "hint": "Sensor counts from the sensor sensitive to both visible light and IR (CH0), counts."
    },
    "full_update_every": {
      "hint": "On screens that support partial updates, this sets the number of updates before a full update is forced."
    },
    "fullscreen": {
      "hint": "Whether to draw the display window in fullscreen or not."
    },
    "function": {
      "hint": "Function of the alert pin, either comparator or interrupt."
    },
    "fw_version": {
      "hint": "Allows you to retrieve the Ld2420 firmware version."
    },
    "g1_pin": {
      "hint": "Green data pin for top half."
    },
    "g2_pin": {
      "hint": "Green data pin for bottom half."
    },
    "gain": {
      "hint": "The gain of this sensor."
    },
    "gain_calibration": {
      "hint": "Pre calibrated gain value, range 0 to 511.9999998807907"
    },
    "gain_ct": {
      "hint": "CT clamp calibration value."
    },
    "gain_factor": {
      "hint": "Gain factor to amplify or to attenuate the VOC index output."
    },
    "gain_metering": {
      "hint": "This value determines how quickly internal energy registers accumulate and hence defines the value of a \"pulse\"."
    },
    "gain_pga": {
      "hint": "The gain for the CT clamp."
    },
    "gain_voltage": {
      "hint": "Voltage gain to scale the low voltage AC power back to household mains feed."
    },
    "gamma_correct": {
      "hint": "Gamma correction mode."
    },
    "gas_resistance": {
      "hint": "The information for the gas sensor."
    },
    "gate_move_sensitivity": {
      "hint": "Gate move threshold calibration sensitivity factor."
    },
    "gate_pin": {
      "hint": "The pin used to control the Triac or Mosfet."
    },
    "gate_select": {
      "hint": "Selection of gate value to edit (gate # from 0 to 15)."
    },
    "gate_still_sensitivity": {
      "hint": "Gate still threshold calibration sensitivity factor."
    },
    "gateway": {
      "hint": "IPv4 address: The gateway of the local network."
    },
    "gating_max_duration_minutes": {
      "hint": "Maximum duration of gating in minutes (freeze of estimator during high NOx index signal)."
    },
    "gdo0_pin": {
      "hint": "The GDO0 pin."
    },
    "gesture_gain": {
      "hint": "The proximity gain level."
    },
    "gesture_led_drive": {
      "hint": "The gesture LED drive level in mA."
    },
    "gesture_wait_time": {
      "hint": "The gesture wait time in ms."
    },
    "get_areas": {
      "hint": "Request stored interference/detection areas from the radar."
    },
    "get_delay": {
      "hint": "Request hold delay."
    },
    "get_installation": {
      "hint": "Request installation mode from the radar and update the installation_mode select with top or side."
    },
    "get_low_power_mode": {
      "hint": "Request low power mode state."
    },
    "get_low_power_sleep_time": {
      "hint": "Request low power sleep time."
    },
    "get_radar_parameters": {
      "hint": "Get all the current setup parameters of the radar."
    },
    "get_sensitivity": {
      "hint": "Request sensitivity state."
    },
    "get_trigger_speed": {
      "hint": "Request trigger speed state."
    },
    "get_z_range": {
      "hint": "Request Z-axis range."
    },
    "gfci_value": {
      "hint": "Use the GFCI value of the sensor."
    },
    "glass_attenuation_factor": {
      "hint": "The attenuation factor of glass if it's behind some glass or plastic facia."
    },
    "glyphs": {
      "hint": "A list of characters you plan to use, in addition to the characters defined by the glyphsets option above."
    },
    "glyphsets": {
      "hint": "A list of glyphsets you plan to use."
    },
    "gmod_pin": {
      "hint": "The GMOD pin for the Inkplate display."
    },
    "gobox": {
      "hint": "Trigger on a decoded Go-Box remote code with the given data."
    },
    "gp8403_id": {
      "hint": "The ID of the GP8403 component."
    },
    "gpio_wakeup_reason": {
      "hint": "run duration if woken up by GPIO."
    },
    "gpio0_enable_pin": {
      "hint": "The GPIO0 Enable pin for the Inkplate display."
    },
    "gradients": {
      "hint": "A list of gradient definitions to use in bg_grad styles."
    },
    "green": {
      "hint": "percentage: The green channel of the light."
    },
    "green_channel": {
      "hint": "Value of the green color channel relative to the clear channel, as a percentage."
    },
    "greyscale": {
      "hint": "Makes the screen display 3 bit colors."
    },
    "grid_frequency": {
      "hint": "grid frequency"
    },
    "grid_rating_current": {
      "hint": "grid rating current"
    },
    "grid_rating_voltage": {
      "hint": "grid rating voltage"
    },
    "grid_voltage": {
      "hint": "grid voltage"
    },
    "group": {
      "hint": "Name of the input group this widget joins, so an encoder or keypad can navigate it."
    },
    "gt911_id": {
      "hint": "Manually specify the ID of the touchscreen."
    },
    "gyro_x": {
      "hint": "Use the X-Axis of the Gyroscope."
    },
    "gyro_y": {
      "hint": "Use the Y-Axis of the Gyroscope."
    },
    "gyro_z": {
      "hint": "Use the Z-Axis of the Gyroscope."
    },
    "gyroscope_odr": {
      "hint": "The output data rate of the gyroscope."
    },
    "gyroscope_range": {
      "hint": "The full-scale range of the gyroscope."
    },
    "gyroscope_x": {
      "hint": "Use the X-Axis of the Gyroscope."
    },
    "gyroscope_y": {
      "hint": "Use the Y-Axis of the Gyroscope."
    },
    "gyroscope_z": {
      "hint": "Use the Z-Axis of the Gyroscope."
    },
    "haier": {
      "hint": "Trigger on a Haier remote code with the given code."
    },
    "haier_id": {
      "hint": "The ID of the Haier climate component."
    },
    "hallconf": {
      "hint": "Modifies the hall plate spinning (2-phase vs 4-phase)."
    },
    "hardware_model": {
      "hint": "The hardware model."
    },
    "hardware_uart": {
      "hint": "The Hardware UART to use for logging."
    },
    "hardware_version": {
      "hint": "The hardware version."
    },
    "harmonic_power": {
      "hint": "Use the harmonic power value on this phase."
    },
    "has_built_in_endstop": {
      "hint": "Indicates that the cover has built in end stop detectors."
    },
    "has_diodes": {
      "hint": "For pads where row pins are outputs, and the keys are connected with diodes."
    },
    "has_direction": {
      "hint": "Indicates if there should be a control for direction."
    },
    "has_moving_target": {
      "hint": "If true a moving target is detected."
    },
    "has_oscillating": {
      "hint": "Indicates if there should be a control for oscillating."
    },
    "has_position": {
      "hint": "Whether this cover will publish its position as a floating point number."
    },
    "has_pulldowns": {
      "hint": "For pads where the column lines have external pulldowns."
    },
    "has_still_target": {
      "hint": "If true a still target is detected."
    },
    "has_target": {
      "hint": "If true target detect either still or in movement."
    },
    "hdop": {
      "hint": "Include the measured HDOP (Horizontal Dilution Of Precision) as a sensor"
    },
    "head": {
      "hint": "The estimated head (preasure difference generated by the pump) in m."
    },
    "heading": {
      "hint": "The heading of the sensor in degrees."
    },
    "health_mode": {
      "hint": "A switch that enables or disables Haier climate health mode (UV light sterilization)."
    },
    "heart_beat": {
      "hint": "Sensor operating status indicator."
    },
    "heart_rate": {
      "hint": "Heart rate during the first 60 seconds as detected by the radar."
    },
    "heat_action": {
      "hint": "The action to call when the climate device wants to heating mode and increase the current temperature."
    },
    "heat_cool_mode": {
      "hint": "The action to call when the climate device is placed into \"heat/cool\" mode (it may both cool and heat as required)."
    },
    "heat_deadband": {
      "hint": "The minimum temperature differential (temperature below the set point) before calling the heating action."
    },
    "heat_energy": {
      "hint": "Heat energy delivered."
    },
    "heat_mode": {
      "hint": "The primary heating mode to use for HVACMode.HEAT :"
    },
    "heat_output": {
      "hint": "The ID of a float output that increases the current temperature."
    },
    "heat_overrun": {
      "hint": "The minimum temperature differential (heating beyond the set point) before calling the idle action."
    },
    "heater": {
      "hint": "The settings for the internal heater for the gas sensor."
    },
    "heater_enabled": {
      "hint": "Turn on/off heater at boot."
    },
    "heater_max_duty": {
      "hint": "The maximum duty cycle of the heater (limited to 0.05 )."
    },
    "heater_power": {
      "hint": "The heater power, either High, Med or Low."
    },
    "heater_time": {
      "hint": "The length of time to run the heater, either Long (1000ms) or Short (100ms)."
    },
    "height": {
      "hint": "Height in pixels."
    },
    "height_threshold": {
      "hint": "To accurately distinguish between a person falling and sitting still in this area, you need to set the trigger height that triggers fall detection."
    },
    "hide_hash": {
      "hint": "Allows you to hide the config hash from the version string."
    },
    "hide_timestamp": {
      "hint": "Allows you to hide the compilation timestamp from the version string."
    },
    "high_voltage_reference": {
      "hint": "The high voltage reference to use for the charge cycles."
    },
    "historical_mode": {
      "hint": "Whether to use historical mode or standard mode."
    },
    "hoermann_hcp_id": {
      "hint": "Manually specify the ID of the hoermann_hcp hub."
    },
    "hold_delay": {
      "hint": "range 0–65535 s: Presence hold time, in seconds (default 30 s in the 2025 protocol spec)."
    },
    "home": {
      "hint": "The ID of a Binary Sensor , to be used as HOME key."
    },
    "homeassistant_services": {
      "hint": "Enable compilation of Home Assistant service call support for external components that use the C++ CustomAPIDevice::call_homeassistant_service() or CustomAPIDevice::fire_homeassistant_event() methods."
    },
    "homeassistant_states": {
      "hint": "Enable compilation of Home Assistant state subscription support for external components that use the C++ CustomAPIDevice::subscribe_homeassistant_state() method."
    },
    "horizontal_airflow": {
      "hint": "The horizontal airflow direction."
    },
    "horizontal_mirror": {
      "hint": "Whether to mirror the image horizontally."
    },
    "host": {
      "hint": "ip: The Host IP of your StatsD Server."
    },
    "hot_junction": {
      "hint": "The information for the Hot Junction temperature sensor."
    },
    "hours": {
      "hint": "The string to use for the hours element."
    },
    "href_pin": {
      "hint": "The pin the HREF line of the camera is connected to."
    },
    "hsync_back_porch": {
      "hint": "The horizontal back porch length."
    },
    "hsync_front_porch": {
      "hint": "The horizontal front porch length."
    },
    "hsync_pin": {
      "hint": "The Horizontal sync pin."
    },
    "hsync_pulse_width": {
      "hint": "The horizontal sync pulse width."
    },
    "hub_sleep": {
      "hint": "Put the bridge chip to sleep when not in use."
    },
    "humidity": {
      "hint": "The sensor that is used to measure the current relative humidity, in %."
    },
    "humidity_control_dehumidify_action": {
      "hint": "The action to call when dehumidification is required."
    },
    "humidity_control_humidify_action": {
      "hint": "The action to call when humidification is required."
    },
    "humidity_control_off_action": {
      "hint": "The action to call when (de)humidification should stop."
    },
    "humidity_hysteresis": {
      "hint": "The maximum humidity differential (above/below the set point) before calling the respective humidity control action."
    },
    "humidity_sensor": {
      "hint": "If specified, this sensor is used to measure the current humidity."
    },
    "humidity_setpoint": {
      "hint": "The information for the humidity indoor sensor (experimental)."
    },
    "humidity_source": {
      "hint": "Give an external relative humidity sensor ID here."
    },
    "hw_frontend_reset": {
      "hint": "If set to true, a reset of the analog frontend will be performed before changing other options."
    },
    "hw_version": {
      "hint": "Valid values are sx1261, sx1262, sx1268 or llcc68."
    },
    "hydreon_rgxx_id": {
      "hint": "The ID of the Hydreon Rain Sensor display."
    },
    "hydrogen": {
      "hint": "All options from Sensor"
    },
    "hyst_level": {
      "hint": "Hysteresis level to prevent gain oscillation on borderline signals."
    },
    "hysteresis": {
      "hint": "See datasheet."
    },
    "i2c_id": {
      "hint": "Specify the ID of the I2C Component if your configuration includes multiple I2C buses."
    },
    "i2s_bclk_pin": {
      "hint": "The GPIO pin to use for the I²S BCLK (Bit Clock) signal, also referred to as SCK (Serial Clock)."
    },
    "i2s_lrclk_pin": {
      "hint": "The GPIO pin to use for the I²S LRCLK (Left/Right Clock) signal, also referred to as WS (Word Select) or FS (Frame Sync)."
    },
    "i2s_mclk_pin": {
      "hint": "The GPIO pin to use for the I²S MCLK (Master Clock) signal."
    },
    "iaq": {
      "hint": "Configuration for the IAQ sensor."
    },
    "iaq_accuracy": {
      "hint": "The information for the IAQ accuracy sensor."
    },
    "iaq_mode": {
      "hint": "IAQ calculation mode."
    },
    "iaq_static": {
      "hint": "Configuration for the IAQ static sensor."
    },
    "ibeacon_major": {
      "hint": "The iBeacon major identifier of the beacon that needs"
    },
    "ibeacon_minor": {
      "hint": "The iBeacon minor identifier of the beacon that needs"
    },
    "ibeacon_uuid": {
      "hint": "The universally unique identifier"
    },
    "icon": {
      "hint": "Manually set the icon to use for the binary sensor in the frontend."
    },
    "id": {
      "hint": "Identifier used to reference this component elsewhere in the configuration."
    },
    "identify_duration": {
      "hint": "The amount of time to identify for."
    },
    "idf_send_async": {
      "hint": "Only on ESP32."
    },
    "idle": {
      "hint": "The amount of time that a signal should remain stable/unchanged for it to be considered complete."
    },
    "idle_action": {
      "hint": "The action to call when the climate device wants to enter idle mode."
    },
    "idle_framerate": {
      "hint": "The framerate to capture images at when no client is requesting a full stream."
    },
    "idle_interval": {
      "hint": "If specified, puts the sensor into idle mode between readings for the specified amount of time."
    },
    "idle_level": {
      "hint": "percentage: The PWM duty cycle the idle value (0%) will map to."
    },
    "idle_period_to_sleep": {
      "hint": "The sensor idle period to wait before powering it off (sleep)."
    },
    "ieee802154_vendor_oui": {
      "hint": "Sets the Vendor Organizationally Unique Identifier (OUI)."
    },
    "if_frequency": {
      "hint": "frequency: Intermediate Frequency."
    },
    "ignore_mcu_update_on_datapoints": {
      "hint": "A list of datapoints to ignore MCU updates for."
    },
    "ignore_missing_glyphs": {
      "hint": "By default, warnings are generated for any glyph that is included in the defined glyphsets but not present in the configured font."
    },
    "ignore_not_found": {
      "hint": "When true (default), the firmware ignores PSRAM initialisation failures and continues to boot."
    },
    "ignore_out_of_range": {
      "hint": "If true, ignores all sensor values out of the range."
    },
    "iir_filter": {
      "hint": "Set up an Infinite Impulse Response filter to increase accuracy."
    },
    "illuminance": {
      "hint": "The information for the light level sensor."
    },
    "image_opa": {
      "hint": "Set opacity of an image."
    },
    "image_recolor": {
      "hint": "Color to mix with every pixel of an image."
    },
    "image_recolor_opa": {
      "hint": "Opacity of the image recoloring."
    },
    "impedance": {
      "hint": "The information for the impedance sensor."
    },
    "import_active_energy": {
      "hint": "Use the import active energy value of the sensor in kilowatt hours (kWh)."
    },
    "import_reactive_energy": {
      "hint": "Use the import reactive energy value of the sensor in kilovolt amps reactive hours (kVArh)."
    },
    "in_pin": {
      "hint": "The pin of the OpenTherm hardware bridge which is usually labeled out on the board."
    },
    "inc_pin": {
      "hint": "Increment pin"
    },
    "include_internal": {
      "hint": "Whether internal entities should be displayed on the web interface."
    },
    "includes": {
      "hint": "A list of C/C++ files to include in the (auto-generated) main file."
    },
    "includes_c": {
      "hint": "The same as includes but for files that require C linkage."
    },
    "index": {
      "hint": "byte: The index (0-based) of the sensor."
    },
    "index_offset": {
      "hint": "NOx index representing typical (average) conditions."
    },
    "indoor": {
      "hint": "Indicates if the sensor is used indoor."
    },
    "indoor_coil_temperature": {
      "hint": "Temperature sensor for indoor coil temperature."
    },
    "indoor_electric_heating_status": {
      "hint": "A binary sensor that indicates electrical heating system activity."
    },
    "indoor_fan_status": {
      "hint": "A binary sensor that indicates indoor fan activity."
    },
    "infer_endstop_from_movement": {
      "hint": "Whether to infer endstop state from the movement sensor."
    },
    "infrared": {
      "hint": "The reading for the infrared sensor."
    },
    "infrared_counts": {
      "hint": "Sensor counts from the IR-sensitive sensor (CH1), counts."
    },
    "init_sequence": {
      "hint": "Allows custom initialisation sequences to be added."
    },
    "init_with_half_cycle": {
      "hint": "Will send the first full half AC cycle Try to use this for dimmable LED lights, it might help turning on at low brightness levels."
    },
    "initial_focus": {
      "hint": "An optional ID for a widget to be given focus on startup (especially useful if there is only one focusable widget.)"
    },
    "initial_level": {
      "hint": "The initial log level, which may be varied at run time."
    },
    "initial_mode": {
      "hint": "The initial measurement mode."
    },
    "initial_option": {
      "hint": "The option to set the option to on setup if not restored with restore_value."
    },
    "initial_state": {
      "hint": "The initial state the light should be set to on bootup."
    },
    "initial_value": {
      "hint": "The value with which to initialize this variable if the state can not be restored or if state restoration is not enabled."
    },
    "input": {
      "hint": "The id of the binary sensor component"
    },
    "input_current_limit": {
      "hint": "Input current in milliamps."
    },
    "input_voltage_range": {
      "hint": "input voltage range"
    },
    "install_height": {
      "hint": "Before using the MR60FDA2, please select the installation height of the radar according to the actual situation in order to obtain accurate identification results."
    },
    "installation_mode": {
      "hint": "top or side."
    },
    "insulation_of_p_to_ground": {
      "hint": "Use the insulation of photo voltiac's +ve terminal to ground value of the sensor in kilo ohms."
    },
    "insulation_of_pv_n_to_ground": {
      "hint": "Use the insulation of photo voltiacs's -ve terminal to ground value of the sensor in kilo ohms."
    },
    "integration_method": {
      "hint": "The integration method to use."
    },
    "integration_time": {
      "hint": "The amount of time sensors are exposed."
    },
    "intensity": {
      "hint": "The intensity with which the MAX7219 should drive the outputs."
    },
    "interface": {
      "hint": "Controls which ESP-IDF SPI implementation should be used."
    },
    "interfaces": {
      "hint": "List of CDC-ACM interface instances."
    },
    "interlock": {
      "hint": "A list of other GPIO switches in an interlock group."
    },
    "interlock_wait_time": {
      "hint": "For interlocking mode, set how long to wait after other items in an interlock group have been disabled before re-activating."
    },
    "internal": {
      "hint": "Mark this component as internal."
    },
    "internal_filter": {
      "hint": "If a pulse shorter than this time is detected, it's discarded and no pulse is counted."
    },
    "internal_filter_mode": {
      "hint": "Determines how the internal filter is applied."
    },
    "internal_reference": {
      "hint": "State of the internal reference."
    },
    "internal_temperature": {
      "hint": "The internal temperature value of the sensor in °C."
    },
    "interrupt_pin": {
      "hint": "The touch detection pin."
    },
    "interrupt_type": {
      "hint": "The type of interrupt to use."
    },
    "interval": {
      "hint": "The interval between each consecutive scan window."
    },
    "invalid_cooldown": {
      "hint": "If a multi click is started, but the timing set in timing does not match, a \"cool down\" period will be activated during which no timing will be matched."
    },
    "invert": {
      "hint": "Invert data output, for use with n-type transistors."
    },
    "invert_alpha": {
      "hint": "Applicable to binary and grayscale only, this will invert the colors, i.e."
    },
    "invert_colors": {
      "hint": "Specifies whether the display colors should be inverted."
    },
    "invert_position": {
      "hint": "Inverts the position value to and from the device."
    },
    "inverted": {
      "hint": "Inverts the output so 0 means 100% duty cycle and 1 means 0%."
    },
    "inverter_bus_voltage": {
      "hint": "Use the inverter bus voltage value of the sensor in volts."
    },
    "inverter_heat_sink_temperature": {
      "hint": "inverter heatsink temperature"
    },
    "inverter_inner_temp": {
      "hint": "Use the inverter inner temperature value of the sensor in degree celsius."
    },
    "inverter_module_temp": {
      "hint": "Use the inverter module temperature value of the sensor in degree celsius."
    },
    "inverter_status": {
      "hint": "Status code of the inverter (0: waiting, 1: normal, 3:fault)"
    },
    "io_capability": {
      "hint": "The IO capability of this ESP32, used for securely connecting to other BLE devices."
    },
    "ip_address": {
      "hint": "Expose the IP Address of the ESP as a text sensor."
    },
    "irk": {
      "hint": "16 byte hex string: The Identity Resolving Key (IRK) to track for this"
    },
    "irq_pin": {
      "hint": "The pin connected to the ADE7935 IRQ line (if connected)"
    },
    "irq0_pin": {
      "hint": "The GPIO pin that the ADE7880's IRQ0 output is connected to, if any."
    },
    "irq1_pin": {
      "hint": "The GPIO pin that the ADE7880's IRQ1 output is connected to."
    },
    "is_connected": {
      "hint": "true while the motor's bus controller is polling the emulated UAP1 module."
    },
    "is_cs_package": {
      "hint": "The \"CS\" package of this sensor has a slightly different formula for calculating the illuminance in lx."
    },
    "is_on": {
      "hint": "The boolean On/Off state to publish."
    },
    "jitter_step": {
      "hint": "Set jitter filter step size."
    },
    "jpeg_quality": {
      "hint": "The JPEG quality that the camera should encode images with."
    },
    "js_include": {
      "hint": "local file: Path to local file to be included in web server index page."
    },
    "js_url": {
      "hint": "url: The URL that should be used for the JS script."
    },
    "json": {
      "hint": "A HTTP body in JSON format."
    },
    "jvc": {
      "hint": "Trigger on a decoded JVC remote code with the given data."
    },
    "k_value": {
      "hint": "The K value comes from the list below."
    },
    "kd": {
      "hint": "The factor for the derivative term of the PID controller."
    },
    "kd_multiplier": {
      "hint": "Set the kd gain when inside the deadband."
    },
    "keeloq": {
      "hint": "Trigger on a decoded KeeLoq RF remote code with the given data."
    },
    "keep_away": {
      "hint": "Indicator for detecting objects approaching or moving away."
    },
    "keep_on_time": {
      "hint": "The time the power supply should be kept enabled after the last output that used it has been switch off."
    },
    "keepalive": {
      "hint": "The time to keep the MQTT socket alive, decreasing this can help with overall stability due to more WiFi traffic with more pings."
    },
    "key": {
      "hint": "The key from keys configuration entry above."
    },
    "key_columns": {
      "hint": "The number of keypad columns to use."
    },
    "key_rows": {
      "hint": "The number of keypad rows to use."
    },
    "keypad": {
      "hint": "Add this to enable the keypad."
    },
    "keypad_id": {
      "hint": "The ID of the keypad to process keypresses from."
    },
    "keypads": {
      "hint": "A list of keypads interacting with the LVGL widgets on the display."
    },
    "keys": {
      "hint": "The keys present on the matrix, from top left to bottom right, row by row."
    },
    "ki": {
      "hint": "The factor for the integral term of the PID controller."
    },
    "ki_multiplier": {
      "hint": "Set the ki gain when inside the deadband."
    },
    "kp": {
      "hint": "The factor for the proportional term of the PID controller."
    },
    "kp_multiplier": {
      "hint": "Set the kp gain when inside the deadband."
    },
    "lambda": {
      "hint": "lambda: The lambda to use for rendering the content on the display."
    },
    "lane_bit_rate": {
      "hint": "The bit rate of the serial data lanes."
    },
    "lanes": {
      "hint": "Number of serial data lanes to use - 1 or 2."
    },
    "last_qflag": {
      "hint": "last qflag reponse"
    },
    "last_qmn": {
      "hint": "last qmn reponse"
    },
    "last_qmod": {
      "hint": "last qmod reponse"
    },
    "last_qpigs": {
      "hint": "last qpigs reponse"
    },
    "last_qpiri": {
      "hint": "last qpiri reponse"
    },
    "last_qpiws": {
      "hint": "last qpiws reponse"
    },
    "last_qt": {
      "hint": "last qt reponse"
    },
    "last_time": {
      "hint": "Information of the last switch-on time sensor."
    },
    "lat_pin": {
      "hint": "Latch/strobe pin."
    },
    "latch_blanking": {
      "hint": "Number of clock cycles OE is blanked during LAT pulse."
    },
    "latch_pin": {
      "hint": "Pin connected to SN74HC595 RCLK (ST_CP) pin"
    },
    "latitude": {
      "hint": "Include the Latitude as a sensor"
    },
    "layout": {
      "hint": "See Layouts for details."
    },
    "layout_cols": {
      "hint": "Number of panels arranged horizontally."
    },
    "layout_rows": {
      "hint": "Number of panels arranged vertically."
    },
    "lcd_escape_to_default": {
      "hint": "lcd escape to default"
    },
    "ld2410_id": {
      "hint": "Manually specify the ID for the Ld2410 component if you are using multiple components."
    },
    "ld2412_id": {
      "hint": "Manually specify the ID for the component."
    },
    "ld2450_id": {
      "hint": "Manually specify the ID for the Ld2450 component."
    },
    "ld6002b_id": {
      "hint": "Manually specify the ID of the LD6002B component."
    },
    "ldo_voltage": {
      "hint": "Voltage used for reference, should be less than VCC"
    },
    "le_pin": {
      "hint": "The LE pin for the Inkplate display."
    },
    "leading_edge": {
      "hint": "Dimming mode: true means leading edge, false is trailing edge."
    },
    "learning_time_gain_hours": {
      "hint": "Time constant to estimate the NOx algorithm gain from the history in hours."
    },
    "learning_time_offset_hours": {
      "hint": "Time constant to estimate the NOx algorithm offset from the history in hours."
    },
    "led_drive": {
      "hint": "The LED drive level in mA."
    },
    "left": {
      "hint": "The ID of a Binary Sensor , to be used as LEFT key."
    },
    "left_button": {
      "hint": "The ID of a Binary Sensor , to be used as LEFT key."
    },
    "legacy_mode": {
      "hint": "Whether to use the legacy configuration from initial implementation."
    },
    "legend": {
      "hint": "Configures a legend for the graph traces."
    },
    "length": {
      "hint": "The amount of digits your TM1637 is driving."
    },
    "lens_bad": {
      "hint": "true if the sensor reports the lens being bad."
    },
    "level": {
      "hint": "The log level to print the message"
    },
    "level_percent": {
      "hint": "Level from 0 to 100"
    },
    "lg": {
      "hint": "Trigger on a decoded LG remote code with the given data."
    },
    "libraries": {
      "hint": "A list of libraries to include in the project."
    },
    "light": {
      "hint": "When in engineering mode, indicates the light sensitivity, otherwise unknown."
    },
    "light_function": {
      "hint": "If set, will affect the OUT pin value, based on light threshold."
    },
    "light_threshold": {
      "hint": "Sets the light threshold for the light function."
    },
    "lightning_energy": {
      "hint": "Lightning energy value."
    },
    "lightning_threshold": {
      "hint": "The number of lightnings that must appear in a 15-minute time window before a lightning storm is detected."
    },
    "limit_high": {
      "hint": "Higher temperature limit, in °C."
    },
    "limit_low": {
      "hint": "Lower temperature limit, in °C."
    },
    "limiter_power": {
      "hint": "The power measured by the inverter's \"Internal\" limiter."
    },
    "line_frequency": {
      "hint": "The AC line frequency of the supply voltage."
    },
    "line_type": {
      "hint": "Specifies the plot line-type."
    },
    "listen_address": {
      "hint": "IPv4 address: Changes to multicast, adding an address to listen to."
    },
    "listen_backlog": {
      "hint": "The maximum number of pending connections in the listen queue."
    },
    "lna_priority": {
      "hint": "If true, reduce LNA gain before DVGA gain when decreasing overall gain."
    },
    "load_pin": {
      "hint": "Pin connected to SN74HC165 Load input (SH/LD) pin."
    },
    "local": {
      "hint": "Include supporting javascript locally allowing it to work without internet access."
    },
    "lock_action": {
      "hint": "The action that should be performed when the remote (like Home Assistant's frontend) requests the lock to be locked."
    },
    "log": {
      "hint": "Turn on or off the log feature inside the web server."
    },
    "log_interval": {
      "hint": "How often to log the statistics."
    },
    "log_level": {
      "hint": "Set the logger level specifically for the messages of the LVGL library: VERBOSE , DEBUG , INFO , WARN , ERROR , NONE ."
    },
    "log_topic": {
      "hint": "MQTTMessage: The topic to send MQTT log messages to."
    },
    "logs": {
      "hint": "Manually set the log level for a"
    },
    "long_mode": {
      "hint": "How a label handles text longer than its width: WRAP, DOT, SCROLL, SCROLL_CIRCULAR or CLIP."
    },
    "long_press_repeat_time": {
      "hint": "For the touchscreen, repeated interval after long_press_time , when on_long_pressed_repeat interaction trigger will be called."
    },
    "long_press_time": {
      "hint": "For the touchscreen, delay after which the on_long_pressed interaction trigger will be called."
    },
    "long_range": {
      "hint": "Set the sensor in long range mode."
    },
    "longitude": {
      "hint": "Include the Longitude as a sensor"
    },
    "loop": {
      "hint": "If you want to loop over a subset of your animation (e.g."
    },
    "low_power": {
      "hint": "Enable or disable low power mode."
    },
    "low_power_mode": {
      "hint": "Enable the low-power (master only) I²C bus."
    },
    "low_power_sleep_time": {
      "hint": "range 0–60000 ms: Low power sleep time in milliseconds (default 500 ms)."
    },
    "low_voltage_reference": {
      "hint": "The low voltage reference to use for the charge cycles."
    },
    "lower": {
      "hint": "Lower threshold, that needs to be crossed to transition from high to low states."
    },
    "lux_compensation": {
      "hint": "Lux compensation formula is used as per manufacturer."
    },
    "lvgl_id": {
      "hint": "The ID of the LVGL instance to pause."
    },
    "mac_address": {
      "hint": "MAC Address: The MAC address of the device."
    },
    "machine_type": {
      "hint": "machine type"
    },
    "magiquest": {
      "hint": "Trigger on a decoded MagiQuest wand remote code with the given wand ID."
    },
    "magn_target": {
      "hint": "dB: Target signal amplitude for the AGC loop."
    },
    "magnitude": {
      "hint": "Information about the detected magnitude."
    },
    "main_switch": {
      "hint": "Required with more than one valve."
    },
    "mains_filter": {
      "hint": "The mains power frequency to reject (50 Hz or 60 Hz )."
    },
    "major": {
      "hint": "The iBeacon major identifier of this beacon."
    },
    "malfunction_action": {
      "hint": "The action that should be performed when relay malfunction is detected."
    },
    "malfunction_detection": {
      "hint": "Enable to detect malfunction detection (Typically welded relays)."
    },
    "manchester": {
      "hint": "Enable Manchester encoding."
    },
    "manual_control": {
      "hint": "For covers with manual external controls."
    },
    "manual_ip": {
      "hint": "Manually configure the static IP of the node."
    },
    "manual_selection_delay": {
      "hint": "The amount of time the controller should wait to activate a valve after the next_valve and previous_valve actions are called."
    },
    "manufacturer": {
      "hint": "Value Configuration: The name of the manufacturer/firmware creator."
    },
    "manufacturer_data": {
      "hint": "The manufacturer-specific data to include in the advertising packet."
    },
    "manufacturer_id": {
      "hint": "16-, 32- or 128-bit BLE manufacturer ID in hex."
    },
    "margin_bottom": {
      "hint": "Sets margin on the bottom."
    },
    "margin_left": {
      "hint": "Sets margin on the left."
    },
    "margin_right": {
      "hint": "Sets margin on the right."
    },
    "margin_top": {
      "hint": "Sets margin on the top."
    },
    "mark_back": {
      "hint": "0-255: Code of the character used to mark menu items going back one level."
    },
    "mark_editing": {
      "hint": "0-255: Code of the character used to mark menu item editing mode."
    },
    "mark_selected": {
      "hint": "0-255: Code of the character used to mark menu item selected."
    },
    "mark_submenu": {
      "hint": "0-255: Code of the character used to mark menu item leading to a submenu."
    },
    "mask_disturber": {
      "hint": "This setting will return whether or not disturbers trigger the IRQ Pin."
    },
    "master_sample_time": {
      "hint": "Adjust the master sample time (tMSP)."
    },
    "max_brightness": {
      "hint": "Maximum brightness value on a scale from 0..1000, the default is 1000."
    },
    "max_cell_voltage": {
      "hint": "The cell of the battery with the higher voltage."
    },
    "max_cell_voltage_number": {
      "hint": "The cell number of the battery with the higher voltage."
    },
    "max_clients": {
      "hint": "The maximum number of simultaneous BLE client connections the server will accept."
    },
    "max_cmd_retries": {
      "hint": "How many times a command will be retried if no response is received."
    },
    "max_commands_per_loop": {
      "hint": "Limits the number of commands processed per loop cycle."
    },
    "max_connections": {
      "hint": "The maximum number of simultaneous API connections allowed."
    },
    "max_cooling_run_time": {
      "hint": "Duration after which supplemental_cooling_action will be called when cooling is active."
    },
    "max_current": {
      "hint": "The maximum current you are expecting."
    },
    "max_delay": {
      "hint": "The maximum delay between power cycles."
    },
    "max_distance_gate": {
      "hint": "Maximum distance gate for still detection."
    },
    "max_duration": {
      "hint": "The maximum duration the cover should be opening or closing."
    },
    "max_dvga_gain": {
      "hint": "Limits the maximum DVGA (Digital Variable Gain Amplifier) gain."
    },
    "max_framerate": {
      "hint": "The maximum framerate the camera will generate images at."
    },
    "max_gate_distance": {
      "hint": "Maximum gate for movement detection."
    },
    "max_heating_run_time": {
      "hint": "Duration after which supplemental_heating_action will be called when heating is active."
    },
    "max_humidity": {
      "hint": "percentage: The maximum humidity the climate device can reach."
    },
    "max_integral": {
      "hint": "The maximum value of the integral term multiplied by ki to prevent windup."
    },
    "max_interval": {
      "hint": "The iBeacon maximum transmit interval in milliseconds from 20 to 10240."
    },
    "max_key_size": {
      "hint": "Maximum encryption key size to support."
    },
    "max_length": {
      "hint": "The maximum length of the desired key sequence."
    },
    "max_level": {
      "hint": "percentage: The PWM duty cycle the maximum value (100%) will map to."
    },
    "max_lna_gain": {
      "hint": "dB: Limits the maximum LNA (Low Noise Amplifier) gain."
    },
    "max_move_distance_gate": {
      "hint": "Maximum distance gate for movement detection."
    },
    "max_notifications": {
      "hint": "The maximum number of BLE characteristics that can have notifications enabled across all connections."
    },
    "max_packet_size": {
      "hint": "Maximum USB packet size in bytes."
    },
    "max_payload_size": {
      "hint": "The largest payload, in bytes, this device can send and receive in one frame."
    },
    "max_power_color_channels": {
      "hint": "Adjusts the current supplied to the"
    },
    "max_power_white_channels": {
      "hint": "Adjusts the current supplied to the"
    },
    "max_pressure": {
      "hint": "Maximum pressure for the pressure sensor, default unit psi."
    },
    "max_queue_age": {
      "hint": "Maximum age in milliseconds for queued commands before they are automatically removed."
    },
    "max_queue_size": {
      "hint": "Sets the maximum number of commands that can be queued at once."
    },
    "max_range": {
      "hint": "Specifies the maximum Y-axis range."
    },
    "max_refresh_rate": {
      "hint": "A time interval used to limit the number of commands a light can handle per second."
    },
    "max_response_buffer_size": {
      "hint": "The maximum buffer size to be used to store the response."
    },
    "max_restore_data_length": {
      "hint": "Only applies to variables of type std::string."
    },
    "max_runs": {
      "hint": "Allows limiting the maximum number of script instances."
    },
    "max_send_queue": {
      "hint": "The maximum number of messages that can be queued for sending per connection before the connection is dropped."
    },
    "max_speed": {
      "hint": "The maximum speed in steps/s (steps per seconds) to drive the stepper at."
    },
    "max_still_distance_gate": {
      "hint": "Maximum distance gate for still detection."
    },
    "max_temperature": {
      "hint": "The maximum temperature the climate device can reach."
    },
    "max_temperature_probe_number": {
      "hint": "The sensor number which has measured the higher temperature."
    },
    "max_transfer_requests": {
      "hint": "Maximum number of concurrent USB transfer requests."
    },
    "max_value": {
      "hint": "Highest value this control accepts."
    },
    "max_voltage": {
      "hint": "The maximum bus voltage you are expecting."
    },
    "maximum_demand_active_power": {
      "hint": "Use the maximum demand (active) power value of the sensor in watts."
    },
    "maximum_demand_apparent_power": {
      "hint": "Use the maximum demand apparent power value of the sensor in VA."
    },
    "maximum_demand_reactive_power": {
      "hint": "Use the maximum demand reactive power value of the sensor in VAR."
    },
    "mcp3008_id": {
      "hint": "The id of the parent MCP3008 component."
    },
    "mcp3204_id": {
      "hint": "The id of the parent MCP3204 component."
    },
    "mcp4461_id": {
      "hint": "Manually specify the ID of the MCP4461."
    },
    "mcp4728_id": {
      "hint": "Manually specify the ID of the MCP4728."
    },
    "md5": {
      "hint": "The MD5sum of the firmware file pointed to by url (below)."
    },
    "md5_url": {
      "hint": "The URL of the file containing an MD5sum of the firmware file pointed to by url (below)."
    },
    "mdc_pin": {
      "hint": "The MDC pin."
    },
    "mdio_pin": {
      "hint": "The MDIO pin."
    },
    "measured_power": {
      "hint": "The RSSI of the iBeacon as measured 1 meter from the device."
    },
    "measurement_duration": {
      "hint": "Set the conversion time for all touch pads."
    },
    "measurement_mode": {
      "hint": "Set measurement mode for scd4x."
    },
    "measurement_rate": {
      "hint": "The measurement rate of the ambient light sensor in milliseconds."
    },
    "measurement_sequence_number": {
      "hint": "The information for the measurement sequence number sensor"
    },
    "media_file": {
      "hint": "The ID of the media file."
    },
    "media_pipeline": {
      "hint": "Pipeline Schema: Configuration settings for the media pipeline."
    },
    "media_player": {
      "hint": "The media_player to use to output the response."
    },
    "menu_item_value": {
      "hint": "Specifies how to render values for menu items that have values (eg."
    },
    "merge_warnings": {
      "hint": "Whether to warn when a YAML merge key (<<:) silently drops a key."
    },
    "mesh_local_prefix": {
      "hint": "ipv6network: Used to build Mesh-Local IPv6 addresses (ML-EIDs), which are unique to each Thread device within the network partition."
    },
    "message": {
      "hint": "The message content."
    },
    "message_id": {
      "hint": "The random message ID to trigger on, see dumper output for more info."
    },
    "message_type": {
      "hint": "The message type to trigger on."
    },
    "meter_constant": {
      "hint": "The number of pulses per kWh."
    },
    "methane": {
      "hint": "All options from Sensor"
    },
    "method": {
      "hint": "Set the method for dimming, can be:"
    },
    "mic_gain": {
      "hint": "The gain applied to the ADC microphones."
    },
    "micro_wake_word": {
      "hint": "The micro_wake_word component used for wake word detection."
    },
    "microphone": {
      "hint": "Microphone Source Configuration: The microphone settings to use for audio input."
    },
    "midea": {
      "hint": "Trigger on a Midea remote code with the given code."
    },
    "min_auth_mode": {
      "hint": "Only on esp32 and esp8266."
    },
    "min_brightness": {
      "hint": "Minimum brightness value on a scale from 0..1000, the default is 0."
    },
    "min_cell_voltage": {
      "hint": "The cell of the battery with the lower voltage."
    },
    "min_cell_voltage_number": {
      "hint": "The cell number of the battery with the lower voltage."
    },
    "min_cooling_off_time": {
      "hint": "Minimum duration the cooling action must be disengaged before it may be engaged."
    },
    "min_cooling_run_time": {
      "hint": "Minimum duration the cooling action must be engaged before it may be disengaged."
    },
    "min_distance_gate": {
      "hint": "Maximum distance gate for movement detection."
    },
    "min_fan_mode_switching_time": {
      "hint": "Minimum duration any given fan mode must be active before it may be changed."
    },
    "min_fanning_off_time": {
      "hint": "Minimum duration the fanning action must be disengaged before it may be engaged."
    },
    "min_fanning_run_time": {
      "hint": "Minimum duration the fanning action must be engaged before it may be disengaged."
    },
    "min_gate_distance": {
      "hint": "Minimum distance for move or still energy detection."
    },
    "min_heating_off_time": {
      "hint": "Minimum duration the heating action must be disengaged before it may be engaged."
    },
    "min_heating_run_time": {
      "hint": "Minimum duration the heating action must be engaged before it may be disengaged."
    },
    "min_humidity": {
      "hint": "percentage: The minimum humidity the climate device can reach."
    },
    "min_idle_time": {
      "hint": "Minimum duration the idle action must be active before calling another climate action."
    },
    "min_integral": {
      "hint": "The minimum value of the integral term multiplied by ki to prevent windup."
    },
    "min_interval": {
      "hint": "The iBeacon minimum transmit interval in milliseconds from 20 to 10240."
    },
    "min_ipv6_addr_count": {
      "hint": "ESPHome considers the network to be connected when it has one IPv4 address and this number of IPv6 addresses."
    },
    "min_key_size": {
      "hint": "Minimum encryption key size requirement from peer."
    },
    "min_length": {
      "hint": "The minimum length of the desired key sequence."
    },
    "min_level": {
      "hint": "percentage: The PWM duty cycle the minimum value (-100%) will map to."
    },
    "min_pressure": {
      "hint": "Minimum pressure for the pressure sensor, default unit psi."
    },
    "min_range": {
      "hint": "Specifies the minimum Y-axis range."
    },
    "min_refresh_rate": {
      "hint": "Minimum panel refresh rate in Hz (40-200)."
    },
    "min_rssi": {
      "hint": "at which minimum RSSI level would the component report the device be present."
    },
    "min_temperature": {
      "hint": "The minimum temperature the climate device can reach."
    },
    "min_temperature_probe_number": {
      "hint": "The sensor number which has measured the lower temperature."
    },
    "min_value": {
      "hint": "Lowest value this control accepts."
    },
    "min_version": {
      "hint": "The minimum ESPHome version required to compile this configuration."
    },
    "minor": {
      "hint": "The iBeacon minor identifier of this beacon."
    },
    "minutes": {
      "hint": "The string to use for the minutes element."
    },
    "mirage": {
      "hint": "Trigger on a Mirage remote code with the given code."
    },
    "mirror_x": {
      "hint": "If true, mirror the x axis."
    },
    "mirror_y": {
      "hint": "If true, mirror the y axis."
    },
    "miso_pin": {
      "hint": "The SPI MISO pin."
    },
    "modbus_id": {
      "hint": "Manually specify the ID of the Modbus hub."
    },
    "mode": {
      "hint": "The auto-mute mode to be used; must be in the range of 0 (auto-mute disabled) to 7."
    },
    "mode_command_topic": {
      "hint": "The topic to receive climate device mode commands on."
    },
    "mode_state_topic": {
      "hint": "The topic to publish climate device mode changes to."
    },
    "model": {
      "hint": "Manually specify the DHT model, can be one of AUTO_DETECT, DHT11, DHT22, DHT22_TYPE2, AM2302, RHT03, SI7021, AM2120 and helps with some connection issues."
    },
    "models": {
      "hint": "The models to use."
    },
    "modulation": {
      "hint": "Modulation can be FSK or LORA."
    },
    "modulation_type": {
      "hint": "The modulation format."
    },
    "moisture": {
      "hint": "Soil moisture in %."
    },
    "mos_temperature": {
      "hint": "Temperature of the mosfets."
    },
    "mosi_pin": {
      "hint": "The SPI MOSI pin."
    },
    "motion_boundary": {
      "hint": "Valid only in custom mode settings."
    },
    "motion_status": {
      "hint": "An indicator that detects the movement or stationarity of an object."
    },
    "motion_threshold": {
      "hint": "Valid only in custom mode settings."
    },
    "motion_to_rest": {
      "hint": "Valid only in custom mode settings."
    },
    "motion_trigger": {
      "hint": "Valid only in custom mode settings."
    },
    "movement_counter": {
      "hint": "The information for the movement count sensor"
    },
    "movement_signs": {
      "hint": "A value calculated by a built-in algorithm to determine if someone is moving in the current environment."
    },
    "moving_distance": {
      "hint": "Distance in cm of detected moving target."
    },
    "moving_energy": {
      "hint": "Energy for moving target."
    },
    "moving_target_count": {
      "hint": "Count of moving targets."
    },
    "mpr121_id": {
      "hint": "The ID of the MPR121 defined above."
    },
    "mqtt_json_state_payload": {
      "hint": "When set to true, state changes will be published only to the state_topic as a single JSON object per state change."
    },
    "msk_deviation": {
      "hint": "Deviation index for MSK modulation."
    },
    "multi_target": {
      "hint": "Turn on/off the Multi Target Tracking option."
    },
    "multiplexer": {
      "hint": "The multiplexer channel of this sensor."
    },
    "multiplier_number": {
      "hint": "The name of the number component that should be presented to the front end (Home Assistant) to enable control of the sprinkler controller's multiplier value."
    },
    "my9231_id": {
      "hint": "Manually specify the ID of the Component/Hub."
    },
    "name": {
      "hint": "Display name shown in the frontend (e.g. Home Assistant)."
    },
    "name_add_mac_suffix": {
      "hint": "Appends the last 3 bytes of the mac address of the device to the name in the form <name>-aabbcc."
    },
    "nbits": {
      "hint": "The number of bits of the remote code."
    },
    "ndef_contains": {
      "hint": "A (sub)string that must appear in the tag's NDEF message."
    },
    "nec": {
      "hint": "Trigger on a decoded NEC remote code with the given data."
    },
    "negative_output": {
      "hint": "The negative output power to drive the cool output at."
    },
    "netmask": {
      "hint": "IPv4 address: The netmask for the configured address."
    },
    "network_key": {
      "hint": "OpenThread network key."
    },
    "network_name": {
      "hint": "A human-readable Network Name."
    },
    "networks": {
      "hint": "Configure multiple WiFi networks to connect to, the best one that is reachable will be connected to."
    },
    "neutral": {
      "hint": "The configuration variables for the 'neutral' phase of the chip."
    },
    "new_password": {
      "hint": "Sets a new password to use for authentication."
    },
    "nexa": {
      "hint": "Trigger on a decoded Nexa RF code with the given data."
    },
    "next": {
      "hint": "The ID of a Binary Sensor , to be used as NEXT key."
    },
    "next_prev_ignore_disabled": {
      "hint": "Set to true to cause sprinkler.next_valve action and sprinkler.previous_valve action to skip over valves that are not enabled."
    },
    "next_url": {
      "hint": "The URL to open after provisioning is complete."
    },
    "nextion_id": {
      "hint": "The ID of the Nextion display."
    },
    "nir": {
      "hint": "The reading for the NIR (near-infrared) channel."
    },
    "nitrogen_dioxide": {
      "hint": "The Nitrogen Dioxide sensor data."
    },
    "noise_level": {
      "hint": "Noise floor level is compared to known reference voltage."
    },
    "noise_suppression_level": {
      "hint": "The noise suppression level to apply to the assist pipeline."
    },
    "noise_threshold": {
      "hint": "Noise threshold coefficient."
    },
    "noiseband": {
      "hint": "The noiseband of the process (=sensor) variable."
    },
    "non_blocking": {
      "hint": "If enabled, any transmit will return immediately and the RMT will run in the background."
    },
    "nonvolatile": {
      "hint": "Only for the volatile channels A-D."
    },
    "nonvolatile_write_delay": {
      "hint": "How long a level must remain unchanged before it is persisted."
    },
    "normalized_offset_slope": {
      "hint": "Normalized temperature offset slope."
    },
    "nrst_pin": {
      "hint": "Pin connected with \"NRST\" of STM32."
    },
    "num_attempts": {
      "hint": "Number of request attempts between 1 and 5 inclusive."
    },
    "num_channels": {
      "hint": "positive integer: The number of audio channels to send to the output speaker."
    },
    "num_chip_lines": {
      "hint": "Number of lines if you want to use the displays in Multiline Mode."
    },
    "num_chips": {
      "hint": "The number of chips you wish to use for daisy chaining."
    },
    "num_leds": {
      "hint": "The number of LEDs in the strip."
    },
    "num_preamble": {
      "hint": "Number of preamble bytes to transmit."
    },
    "num_scans": {
      "hint": "Number of times to scan the finger to be enrolled."
    },
    "num_targets": {
      "hint": "The number of target detected by the radar."
    },
    "number": {
      "hint": "The pin number of the MCP3008."
    },
    "obis_code": {
      "hint": "Specify the OBIS code you want to retrieve data for from the device."
    },
    "object": {
      "hint": "Enables sensor entity for object temperature reading."
    },
    "observations": {
      "hint": "A list of observations that influence the Bayesian probability of the event."
    },
    "obstacle_rollback": {
      "hint": "percentage: The percentage of rollback the cover will perform in case of obstacle detection."
    },
    "oci": {
      "hint": "Measured OCI value."
    },
    "oe_pin": {
      "hint": "Output enable pin."
    },
    "off_mode": {
      "hint": "The action to call when the climate device is placed into \"off\" mode (it is completely disabled)."
    },
    "off_pin": {
      "hint": "The GPIO pin to pulse to turn off the switch."
    },
    "off_speed_cycle": {
      "hint": "Determines if the fan will cycle off after cycling though its highest speed."
    },
    "offline_skip_updates": {
      "hint": "How many update intervals to wait between retry attempts while a device is offline (see below)."
    },
    "offset": {
      "hint": "Temperature offset [°C]."
    },
    "offset_active_power": {
      "hint": "The active power offset obtained using the power offset calibration feature when current is 0 and a voltage transformer is plugged in and calibrated."
    },
    "offset_calibration": {
      "hint": "Pre calibrated offset value, range -8388608 to 8388607"
    },
    "offset_current": {
      "hint": "The current offset obtained using the offset calibration feature when current and voltage are 0"
    },
    "offset_height": {
      "hint": "Specify an offset for the y-direction of the display."
    },
    "offset_reactive_power": {
      "hint": "The reactive power offset obtained using the power offset calibration feature when current is 0 and a voltage transformer is plugged in and calibrated."
    },
    "offset_voltage": {
      "hint": "The voltage offset obtained using the offset calibration feature when current and voltage are 0"
    },
    "offset_width": {
      "hint": "Specify an offset for the x-direction of the display, typically used when an LCD is smaller than the maximum supported by the driver chip."
    },
    "offset_x": {
      "hint": "Set this option if some horizontal pixel is missing."
    },
    "offset_y": {
      "hint": "Set this option if some vertical pixel is missing."
    },
    "on_abbwelcome": {
      "hint": "An automation to perform when a ABB-Welcome code has been decoded."
    },
    "on_aeha": {
      "hint": "An automation to perform when a AEHA remote code has been decoded."
    },
    "on_alarm_end": {
      "hint": "(supported only by hOn) Automation to perform when AC deactivates a new alarm."
    },
    "on_alarm_start": {
      "hint": "(supported only by hOn) Automation to perform when AC activates a new alarm."
    },
    "on_anticlockwise": {
      "hint": "Actions to be performed when the knob is turned anticlockwise."
    },
    "on_armed_away": {
      "hint": "An automation to perform when the alarm state changes to armed_away."
    },
    "on_armed_home": {
      "hint": "An automation to perform when the alarm state changes to armed_home."
    },
    "on_armed_night": {
      "hint": "An automation to perform when the alarm state changes to armed_night."
    },
    "on_arming": {
      "hint": "An automation to perform when the alarm state changes to arming."
    },
    "on_beo4": {
      "hint": "An automation to perform when a B&O Beo4 infrared remote code has been decoded."
    },
    "on_ble_advertise": {
      "hint": "An automation to perform when a Bluetooth advertisement is received."
    },
    "on_ble_manufacturer_data_advertise": {
      "hint": "The same for manufacturer data."
    },
    "on_ble_service_data_advertise": {
      "hint": "An automation to perform when an advertisement carries matching service data."
    },
    "on_boot": {
      "hint": "An automation to perform when the node starts."
    },
    "on_boot_restore_from": {
      "hint": "(Optional, on_boot_restore_from): Controls what the thermostat will do when it first boots."
    },
    "on_brennenstuhl": {
      "hint": "An automation to perform when a brennenstuhl RF code has been decoded."
    },
    "on_broadcast": {
      "hint": "An automation to perform when a broadcast packet is received."
    },
    "on_buffer_overflow": {
      "hint": "An action to be performed when the Nextion reports a buffer overflow."
    },
    "on_byronsx": {
      "hint": "An automation to perform when a Byron SX doorbell RF code has been decoded."
    },
    "on_calibration": {
      "hint": "Triggered when the result of get_calibration() is ready."
    },
    "on_call_connected": {
      "hint": "An action to be performed when a call is connected, either because an outgoing call accepted is accepted or an incoming call answered."
    },
    "on_call_disconnected": {
      "hint": "An action to be performed when a call is disconnected."
    },
    "on_canalsat": {
      "hint": "An automation to perform when a CanalSat remote code has been decoded."
    },
    "on_canalsatld": {
      "hint": "An automation to perform when a CanalSatLD remote code has been decoded."
    },
    "on_chime": {
      "hint": "An automation to perform when a zone has been marked as chime in the configuration, and it changes from closed to open."
    },
    "on_cleared": {
      "hint": "An automation to perform when the alarm clears."
    },
    "on_click": {
      "hint": "An automation to perform when the button is held down for a specified period of time."
    },
    "on_client_connected": {
      "hint": "An automation to perform when a client connects to the API."
    },
    "on_client_disconnected": {
      "hint": "An automation to perform when a client disconnects from the API."
    },
    "on_clockwise": {
      "hint": "Actions to be performed when the knob is turned clockwise."
    },
    "on_command_sent": {
      "hint": "An automation to perform when a modbus command has been sent."
    },
    "on_complete": {
      "hint": "An automation to perform after data has been sent."
    },
    "on_connect": {
      "hint": "An automation to perform when the client connects to a device."
    },
    "on_coolix": {
      "hint": "An automation to perform when a Coolix remote code has been decoded."
    },
    "on_custom": {
      "hint": "Triggered when the result of get_custom() is ready."
    },
    "on_data": {
      "hint": "An automation that will be triggered for every serial line received from the EmonTx (both plain text and JSON)."
    },
    "on_datapoint_update": {
      "hint": "An automation to perform when a Tuya datapoint update is received."
    },
    "on_device_information": {
      "hint": "Triggered when the result of get_device_information() is ready."
    },
    "on_direction_set": {
      "hint": "An automation to perform when the fan direction is changed."
    },
    "on_disarmed": {
      "hint": "An automation to perform when the alarm state changes to disarmed."
    },
    "on_disconnect": {
      "hint": "An automation to perform when the client disconnects from a device."
    },
    "on_dish": {
      "hint": "An automation to perform when a dish network remote code has been decoded."
    },
    "on_dooya": {
      "hint": "An automation to perform when a Dooya RF remote code has been decoded."
    },
    "on_double_click": {
      "hint": "An automation to perform when the button is pressed twice for specified periods of time."
    },
    "on_download_finished": {
      "hint": "An automation to perform when the image has been successfully downloaded."
    },
    "on_drayton": {
      "hint": "An automation to perform when a Drayton Digistat RF code has been decoded."
    },
    "on_dyson": {
      "hint": "An automation to perform when a Dyson cool AM07 code has been decoded."
    },
    "on_emulated_tag_scan": {
      "hint": "An automation to perform when the PN7160 is scanned by another tag reader (such as a smartphone)."
    },
    "on_end": {
      "hint": "An automation to perform when the voice assistant is finished all tasks."
    },
    "on_enrollment_done": {
      "hint": "An action to be performed when a fingerprint is enrolled."
    },
    "on_enrollment_failed": {
      "hint": "An action to be performed when a fingerprint enrollment failed."
    },
    "on_enrollment_scan": {
      "hint": "An action to be performed when a fingerprint is scanned during enrollment."
    },
    "on_error": {
      "hint": "Optional automation to execute when the Home Assistant action call fails."
    },
    "on_event": {
      "hint": "An automation to perform when an event is triggered."
    },
    "on_exposure_notification": {
      "hint": "An automation to run when an exposure notification bluetooth message is received."
    },
    "on_face_info": {
      "hint": "An action to be performed when face information is available."
    },
    "on_face_scan_invalid": {
      "hint": "An action to be performed when the face scan failed."
    },
    "on_face_scan_matched": {
      "hint": "An action to be performed when an enrolled face is scanned and recognized."
    },
    "on_face_scan_unmatched": {
      "hint": "An action to be performed when an unknown face is scanned."
    },
    "on_finger_scan_invalid": {
      "hint": "An action to be performed when the scan of a fingerprint failed."
    },
    "on_finger_scan_matched": {
      "hint": "An action to be performed when an enrolled fingerprint is scanned."
    },
    "on_finger_scan_misplaced": {
      "hint": "An action to be performed when the finger is not entirely touching the sensor."
    },
    "on_finger_scan_start": {
      "hint": "An action to be performed when the finger touches the sensor."
    },
    "on_finger_scan_unmatched": {
      "hint": "An action to be performed when an unknown fingerprint is scanned."
    },
    "on_finished_playback": {
      "hint": "An action to be performed when playback is finished."
    },
    "on_gobox": {
      "hint": "An automation to perform when a Go-Box remote code has been decoded."
    },
    "on_haier": {
      "hint": "An automation to perform when a Haier remote code has been decoded."
    },
    "on_idle": {
      "hint": "An automation to perform when the voice assistant is idle (no other actions/states are in progress)."
    },
    "on_image": {
      "hint": "An automation called when image taken."
    },
    "on_incoming_call": {
      "hint": "An action to be performed when a call is received."
    },
    "on_intent_end": {
      "hint": "An automation to perform when intent processing ends."
    },
    "on_intent_progress": {
      "hint": "An automation to perform when intent progress happens."
    },
    "on_intent_start": {
      "hint": "An automation to perform when intent processing starts."
    },
    "on_join": {
      "hint": "Automation to run when the device joins the network."
    },
    "on_json": {
      "hint": "An automation that will be triggered whenever new JSON data is received from the EmonTx."
    },
    "on_json_message": {
      "hint": "An action to be performed when a JSON message on a specific MQTT topic is received."
    },
    "on_jvc": {
      "hint": "An automation to perform when a JVC remote code has been decoded."
    },
    "on_keeloq": {
      "hint": "An automation to perform when a KeeLoq RF code has been decoded."
    },
    "on_key": {
      "hint": "An automation to perform when a key has been pressed."
    },
    "on_led": {
      "hint": "Triggered when the result of get_led_state() is ready."
    },
    "on_lg": {
      "hint": "An automation to perform when a LG remote code has been decoded."
    },
    "on_listening": {
      "hint": "An automation to perform when the voice assistant microphone starts listening."
    },
    "on_lock": {
      "hint": "An automation to perform when the lock is locked."
    },
    "on_loop": {
      "hint": "An automation to perform on each loop() iteration."
    },
    "on_magiquest": {
      "hint": "An automation to perform when a MagiQuest wand remote code has been decoded."
    },
    "on_message": {
      "hint": "An action to be"
    },
    "on_midea": {
      "hint": "An automation to perform when a Midea remote code has been decoded."
    },
    "on_mirage": {
      "hint": "An automation to perform when a Mirage remote code has been decoded."
    },
    "on_multi_click": {
      "hint": "An automation to perform when the button is pressed in a specific sequence."
    },
    "on_mute": {
      "hint": "An automation to perform when muted."
    },
    "on_nec": {
      "hint": "An automation to perform when a NEC remote code has been decoded."
    },
    "on_nexa": {
      "hint": "An automation to perform when a Nexa RF code has been decoded."
    },
    "on_no_response": {
      "hint": "Fires when the device does not answer within the hub's send_wait_time."
    },
    "on_not_sent": {
      "hint": "Fires when the frame never reached the wire - a duplicate write already pending, a full send queue, an invalid request, or a queued frame cancelled by another device or automation clearing the queue…"
    },
    "on_numeric_comparison_request": {
      "hint": "An automation to compare the passkeys shown on the two BLE devices."
    },
    "on_offline": {
      "hint": "An automation to perform when a modbus controller goes offline."
    },
    "on_online": {
      "hint": "An automation to perform when a modbus controller goes online."
    },
    "on_oscillating_set": {
      "hint": "An automation to perform when the fan oscillating state is changed."
    },
    "on_packet": {
      "hint": "An automation to perform when a packet has been decoded."
    },
    "on_page": {
      "hint": "An action to be performed after a page change."
    },
    "on_panasonic": {
      "hint": "An automation to perform when a Panasonic remote code has been decoded."
    },
    "on_passkey_notification": {
      "hint": "An automation to display the passkey to the user."
    },
    "on_passkey_request": {
      "hint": "An automation to enter the passkey required by the other BLE device."
    },
    "on_pending": {
      "hint": "An automation to perform when the alarm state changes to pending."
    },
    "on_pin": {
      "hint": "The GPIO pin to pulse to turn on the switch."
    },
    "on_pioneer": {
      "hint": "An automation to perform when a pioneer remote code has been decoded."
    },
    "on_preset_set": {
      "hint": "An automation to perform when the fan preset mode is changed."
    },
    "on_press": {
      "hint": "An automation to perform when the button is pressed."
    },
    "on_progress": {
      "hint": "An automation to perform when keys are pressed."
    },
    "on_pronto": {
      "hint": "An automation to perform when a Pronto remote code has been decoded."
    },
    "on_provisioned": {
      "hint": "An action to be performed when provisioning has completed."
    },
    "on_provisioning": {
      "hint": "An action to be performed when the device begins the provisioning process."
    },
    "on_ps_high_threshold": {
      "hint": "Actions to perform when the proximity sensor is triggered on object getting closer."
    },
    "on_ps_low_threshold": {
      "hint": "Actions to perform when the proximity sensor is triggered on object getting further away."
    },
    "on_raw": {
      "hint": "An automation to perform when a raw remote code has been decoded."
    },
    "on_raw_value": {
      "hint": "An automation to perform when a raw value is received that hasn't passed through any filters."
    },
    "on_rc_switch": {
      "hint": "An automation to perform when a RCSwitch RF code has been decoded."
    },
    "on_rc5": {
      "hint": "An automation to perform when a RC5 remote code has been decoded."
    },
    "on_rc6": {
      "hint": "An automation to perform when a RC6 remote code has been decoded."
    },
    "on_ready": {
      "hint": "An automation to perform when the logical 'and' of all the zone sensors change state."
    },
    "on_receive": {
      "hint": "An automation to perform when a directed (non-broadcast) packet is received from a registered peer."
    },
    "on_redraw": {
      "hint": "An automation to perform when the menu needs to be redrawn."
    },
    "on_release": {
      "hint": "An automation to perform when the button is released."
    },
    "on_response": {
      "hint": "An automation to perform after the request is received."
    },
    "on_result": {
      "hint": "An automation to perform when the key sequence has been finished (eg."
    },
    "on_roomba": {
      "hint": "An automation to perform when a Roomba remote code has been decoded."
    },
    "on_safe_mode": {
      "hint": "An action to be performed once when safe mode is invoked."
    },
    "on_samsung": {
      "hint": "An automation to perform when a Samsung remote code has been decoded."
    },
    "on_samsung36": {
      "hint": "An automation to perform when a Samsung36 remote code has been decoded."
    },
    "on_scan_end": {
      "hint": "An automation to perform when a scan session ends (its duration elapsed, or stop_scan was called)."
    },
    "on_sent": {
      "hint": "An automation to perform when the data is sent successfully (the receiver has acknowledged the packet)."
    },
    "on_setup": {
      "hint": "An action to be performed after ESPHome connects to the Nextion."
    },
    "on_shutdown": {
      "hint": "An automation to perform right before the node shuts down."
    },
    "on_sleep": {
      "hint": "An action to be performed when the Nextion goes to sleep."
    },
    "on_slope": {
      "hint": "Triggered when the result of get_slope() is ready."
    },
    "on_sms_received": {
      "hint": "An action to be performed when an SMS is received."
    },
    "on_sony": {
      "hint": "An automation to perform when a Sony remote code has been decoded."
    },
    "on_speed_set": {
      "hint": "An automation to perform when the fan speed is changed."
    },
    "on_start": {
      "hint": "An action to be performed when Improv is waiting for authorization and/or upon authorization."
    },
    "on_state": {
      "hint": "An automation to perform when the alarm changes state."
    },
    "on_status_message": {
      "hint": "Automation to perform when status message received from AC."
    },
    "on_stop": {
      "hint": "An action to be performed when Improv has stopped."
    },
    "on_stream_start": {
      "hint": "An automation to perform when a stream starts."
    },
    "on_stream_stop": {
      "hint": "An automation to perform when a stream stops."
    },
    "on_stt_end": {
      "hint": "An automation to perform when the voice assistant has finished speech-to-text."
    },
    "on_stt_vad_end": {
      "hint": "An automation to perform when voice activity detection ends speech-to-text processing."
    },
    "on_stt_vad_start": {
      "hint": "An automation to perform when voice activity detection starts speech-to-text processing."
    },
    "on_success": {
      "hint": "Optional automation to execute when the Home Assistant action call succeeds."
    },
    "on_sunrise": {
      "hint": "An automation to perform at sunrise when the sun crosses a specified angle."
    },
    "on_sunset": {
      "hint": "An automation to perform at sunset when the sun crosses a specified angle."
    },
    "on_symphony": {
      "hint": "An automation to perform when a Symphony remote code has been decoded."
    },
    "on_t": {
      "hint": "Triggered when the result of get_t() is ready."
    },
    "on_tag": {
      "hint": "An automation to perform when a tag is read."
    },
    "on_tag_removed": {
      "hint": "An automation to perform when a tag is removed."
    },
    "on_time": {
      "hint": "Automation to run at specific intervals using a cron-like syntax."
    },
    "on_time_sync": {
      "hint": "Automation to run when the time source could be (re-)synchronized.."
    },
    "on_timeout": {
      "hint": "An automation to perform if the timeout happens."
    },
    "on_timer_cancelled": {
      "hint": "An automation to perform when a voice assistant timer has been cancelled."
    },
    "on_timer_finished": {
      "hint": "An automation to perform when a voice assistant timer has finished."
    },
    "on_timer_started": {
      "hint": "An automation to perform when a voice assistant timer has started."
    },
    "on_timer_tick": {
      "hint": "An automation to perform when the voice assistant timers tick is triggered."
    },
    "on_timer_updated": {
      "hint": "An automation to perform when a voice assistant timer has been updated (paused/resumed/duration changed)."
    },
    "on_toshiba_ac": {
      "hint": "An automation to perform when a Toshiba AC remote code has been decoded."
    },
    "on_toto": {
      "hint": "An automation to perform when a Toto remote code has been decoded."
    },
    "on_touch": {
      "hint": "An action to be performed after a touch event (press or release)."
    },
    "on_transmit": {
      "hint": "An automation to perform before data is sent."
    },
    "on_triggered": {
      "hint": "An automation to perform when the alarm triggers."
    },
    "on_tts_end": {
      "hint": "An automation to perform when the voice assistant has finished text-to-speech."
    },
    "on_tts_start": {
      "hint": "An automation to perform when the voice assistant has started text-to-speech."
    },
    "on_tts_stream_end": {
      "hint": "An automation to perform when audio stream (voice response) playback ends."
    },
    "on_tts_stream_start": {
      "hint": "An automation to perform when audio stream (voice response) playback starts."
    },
    "on_turn_off": {
      "hint": "An automation to perform when the fan is turned off."
    },
    "on_turn_on": {
      "hint": "An automation to perform when the fan is turned on."
    },
    "on_unknown_peer": {
      "hint": "An automation to perform when a packet is received from a device that is not registered as a peer."
    },
    "on_unlock": {
      "hint": "An automation to perform when the lock is unlocked."
    },
    "on_unmute": {
      "hint": "An automation to perform when unmuted."
    },
    "on_update": {
      "hint": "An automation to perform when the device firmware is updated."
    },
    "on_update_available": {
      "hint": "An automation to perform when an update is available."
    },
    "on_value": {
      "hint": "An automation to perform when a new value is published."
    },
    "on_value_range": {
      "hint": "An automation to perform when a published value transition from outside to a range to inside."
    },
    "on_volume": {
      "hint": "An automation to perform when the volume is changed."
    },
    "on_wake": {
      "hint": "An automation to run when the device boots after waking from deep sleep."
    },
    "on_wake_word_detected": {
      "hint": "An automation to perform when the assist pipeline has detected a wake word."
    },
    "one_wire_id": {
      "hint": "One Wire: The ID of the 1-Wire bus to use."
    },
    "opa": {
      "hint": "Overall opacity of the widget, 0-255 or a percentage."
    },
    "opa_layered": {
      "hint": "Opacity of the entire layer the widget is on."
    },
    "open_action": {
      "hint": "The action that should be performed when the remote requests the cover to be opened."
    },
    "open_drain_interrupt": {
      "hint": "Configure the interrupt pin to open-drain mode."
    },
    "open_duration": {
      "hint": "The amount of time it takes the cover to open up from the fully-closed state."
    },
    "open_endstop": {
      "hint": "The ID of the Binary Sensor that turns on when the open position is reached."
    },
    "open_moving_current_threshold": {
      "hint": "The amount of current in Amps the motor should drain to consider the cover is opening."
    },
    "open_obstacle_current_threshold": {
      "hint": "The amount of current in Amps the motor should drain to consider the cover is blocked during opening."
    },
    "open_obstacle_sensor": {
      "hint": "The ID of the Binary Sensor that turns on when an obstacle that blocks the open direction is detected."
    },
    "open_sensor": {
      "hint": "The open current sensor."
    },
    "opentherm_version_controller": {
      "hint": "Version of OpenTherm implemented by controller"
    },
    "operating_mode": {
      "hint": "Provisions the operating mode select component."
    },
    "operation": {
      "hint": "What operation to perform on the number component."
    },
    "optimistic": {
      "hint": "Whether to operate in optimistic mode - when in this mode, any command sent to the switch will immediately update the reported state."
    },
    "option": {
      "hint": "The option to set the select to."
    },
    "options": {
      "hint": "A string, or list of strings to compare with the current selection."
    },
    "orientation_xy": {
      "hint": "XY orientation."
    },
    "orientation_z": {
      "hint": "Z orientation."
    },
    "oscillating": {
      "hint": "Set the oscillation state of the fan."
    },
    "oscillation_command_topic": {
      "hint": "The topic to receive oscillation commands on."
    },
    "oscillation_output": {
      "hint": "The id of the output to use for the oscillation state of this fan."
    },
    "oscillation_state_topic": {
      "hint": "The topic to publish fan oscillation state changes to."
    },
    "ota": {
      "hint": "Explicitly disable OTA updates through the web server interface."
    },
    "ota_version": {
      "hint": "Firmware version string, if supported by your module firmware."
    },
    "out_of_range_mode": {
      "hint": "How to treat out of range values."
    },
    "out_pin": {
      "hint": "The pin of the OpenTherm hardware bridge which is usually labeled in on the board."
    },
    "out_pin_level": {
      "hint": "Control OUT pin away value."
    },
    "out_pin_presence_status": {
      "hint": "When in engineering mode, indicates the status of the OUT pin, otherwise false."
    },
    "outdoor_coil_temperature": {
      "hint": "Temperature sensor for outdoor coil temperature."
    },
    "outdoor_defrost_temperature": {
      "hint": "Temperature sensor for outdoor defrost temperature."
    },
    "outdoor_fan_status": {
      "hint": "A binary sensor that indicates outdoor fan activity."
    },
    "outdoor_in_air_temperature": {
      "hint": "Temperature sensor incoming air temperature."
    },
    "outdoor_out_air_temperature": {
      "hint": "Temperature sensor for outgoing air temperature."
    },
    "outdoor_temperature": {
      "hint": "Temperature sensor for outdoor temperature."
    },
    "outlet_temperature": {
      "hint": "If specified, the temperature of the air being discharged from the BedJet will be reported as a sensor."
    },
    "outline_color": {
      "hint": "Color used to draw the outline around the widget."
    },
    "outline_opa": {
      "hint": "Opacity of the outline of the widget."
    },
    "outline_pad": {
      "hint": "Distance between the outline and the widget itself."
    },
    "outline_width": {
      "hint": "Set the width of the outline in pixels."
    },
    "output": {
      "hint": "The id of the binary output component to use for this fan."
    },
    "output_averaging_samples": {
      "hint": "average the output over this many samples."
    },
    "output_load_percent": {
      "hint": "output load percentage"
    },
    "output_power": {
      "hint": "The transmission power in dBm."
    },
    "output_source_priority": {
      "hint": "output source priority;"
    },
    "output_source_priority_battery": {
      "hint": "output source priority battery"
    },
    "output_source_priority_hybrid": {
      "hint": "output source priority hybrid"
    },
    "output_source_priority_solar": {
      "hint": "output source priority solar"
    },
    "output_source_priority_utility": {
      "hint": "output source priority utility"
    },
    "output_speaker": {
      "hint": "The speaker to output the mixed audio."
    },
    "output_speakers": {
      "hint": "A list of speakers the router can output to."
    },
    "over_temperature_restart_function": {
      "hint": "over temperature restart function"
    },
    "overload_bypass_function": {
      "hint": "overload bypass function"
    },
    "overload_restart_function": {
      "hint": "overload restart function"
    },
    "oversampling": {
      "hint": "The oversampling parameter for the temperature sensor."
    },
    "pa_pin": {
      "hint": "Transmitter output pin, can be BOOST or RFO."
    },
    "pa_power": {
      "hint": "Transmitter power, range is from -3 to 15 dBm when hw_version is sx1261 and from -3 to 22 dBm otherwise."
    },
    "pa_ramp": {
      "hint": "Transmitter PA ramp, can be 10us, 20us, 40us, 80us, 200us, 800us, 1700us or 3400us."
    },
    "packet_length": {
      "hint": "Packet length in bytes."
    },
    "packet_mode": {
      "hint": "Enable FIFO packet mode."
    },
    "pad_all": {
      "hint": "Inner padding on all four sides in pixels."
    },
    "pad_bottom": {
      "hint": "Set the padding on the bottom, in pixels."
    },
    "pad_column": {
      "hint": "Set the padding between the columns of the children elements, in pixels."
    },
    "pad_height": {
      "hint": "Specify additional pixels recognized by the controller after the offset and height."
    },
    "pad_left": {
      "hint": "Set the padding on the left, in pixels."
    },
    "pad_radial": {
      "hint": "Pad text labels away from the scale ticks/remainder of the widget part."
    },
    "pad_right": {
      "hint": "Set the padding on the right, in pixels."
    },
    "pad_row": {
      "hint": "Set the padding between the rows of the children elements, in pixels."
    },
    "pad_top": {
      "hint": "Set the padding on the top, in pixels."
    },
    "pad_width": {
      "hint": "Specify additional pixels recognized by the controller after the offset and width."
    },
    "page_id": {
      "hint": "hex: (RTL8201 only) Register page number to select before writing (e.g."
    },
    "page_wrap": {
      "hint": "Wrap from the last to the first page when navigating between them with lvgl.page.next , lvgl.page.previous ."
    },
    "pages": {
      "hint": "A list of page IDs."
    },
    "pan_id": {
      "hint": "2-byte Personal Area Network ID (PAN ID)."
    },
    "panasonic": {
      "hint": "Trigger on a decoded Panasonic remote code with the given data."
    },
    "panel_height": {
      "hint": "Height of a single panel in pixels (e.g., 32)."
    },
    "panel_width": {
      "hint": "Width of a single panel in pixels (e.g., 64)."
    },
    "parallel_max_num": {
      "hint": "parallel max num"
    },
    "parameters": {
      "hint": "Script Parameters: A script can define one or more parameters that must be provided in order to execute."
    },
    "parent_average_rssi": {
      "hint": "The average RSSI of the parent, in dBm."
    },
    "parent_changes": {
      "hint": "Total MLE parent changes since boot."
    },
    "parent_last_rssi": {
      "hint": "The RSSI of the last frame received from the parent, in dBm."
    },
    "parent_link_quality_in": {
      "hint": "Incoming link quality from the parent (0-3)."
    },
    "parent_link_quality_out": {
      "hint": "Outgoing link quality to the parent (0-3)."
    },
    "parity": {
      "hint": "The parity used on the UART bus."
    },
    "partial_updating": {
      "hint": "Makes the screen update partially, which is faster, but leaves burnin."
    },
    "partition_id_changes": {
      "hint": "Total MLE partition ID changes since boot."
    },
    "partitions": {
      "hint": "filename or list: A partition table CSV file or a list of custom partitions to add."
    },
    "passive": {
      "hint": "Whether passive mode is enabled."
    },
    "passive_scan": {
      "hint": "If enabled, then the device will perform WiFi scans in a passive fashion."
    },
    "passkey": {
      "hint": "The 6-digit passkey."
    },
    "password": {
      "hint": "The password to set."
    },
    "path": {
      "hint": "Path inside the repo, if different from components or esphome/components"
    },
    "paused": {
      "hint": "When true , LVGL will be initially paused and the display will not be updated until lvgl.resume: is called."
    },
    "payload": {
      "hint": "The message content."
    },
    "payload_length": {
      "hint": "Length of the packet."
    },
    "pca9685_id": {
      "hint": "Manually specify the ID of the PCA9685 hub."
    },
    "pcf8575": {
      "hint": "Whether this is a 16-pin PCF8575."
    },
    "pclk_frequency": {
      "hint": "Set the pixel clock speed."
    },
    "pclk_inverted": {
      "hint": "If the pclk is active negative (default is True)"
    },
    "pclk_pin": {
      "hint": "The PCLK pin."
    },
    "pdu": {
      "hint": "The PDU to send: function code followed by data."
    },
    "peak": {
      "hint": "The information for the peak loudness sensor."
    },
    "peak_current": {
      "hint": "Use the peak current value on this phase in amperes."
    },
    "peak_current_signed": {
      "hint": "Control the peak current output as signed or absolute."
    },
    "peer_allowed_ips": {
      "hint": "A list of networks in CIDR notation (IP/mask) to be allowed through the tunnel."
    },
    "peer_endpoint": {
      "hint": "The hostname of the remote peer."
    },
    "peer_persistent_keepalive": {
      "hint": "The amount of time after which a keepalive packet is sent through the tunnel."
    },
    "peer_port": {
      "hint": "UDP port: The port where remote peer is listening on."
    },
    "peer_preshared_key": {
      "hint": "The chosen pre-shared key between local device and remote peer."
    },
    "peer_public_key": {
      "hint": "The public key of the remote peer."
    },
    "peers": {
      "hint": "The list of MAC addresses of the devices that this device is allowed to communicate with."
    },
    "pending_time": {
      "hint": "The entry delay before the alarm is triggered."
    },
    "people_exist": {
      "hint": "If true when target (person) is detected."
    },
    "period": {
      "hint": "Minimal period between requests to the appliance."
    },
    "permittivity": {
      "hint": "Dielectric coefficient or permittivity of the material to be measured (dimensionless quantity)."
    },
    "pga_gain": {
      "hint": "If set to 10X (default), the differential voltage at the current inputs must be between -250mV and +250mV."
    },
    "ph": {
      "hint": "Measured pH value."
    },
    "phase_a": {
      "hint": "The configuration variables for the 'A' phase inputs of the chip."
    },
    "phase_angle": {
      "hint": "The value for the APHCAL calibration register."
    },
    "phase_b": {
      "hint": "The configuration variables for the 'B' phase inputs of the chip."
    },
    "phase_balancer": {
      "hint": "The phase balancer algorithm to use."
    },
    "phase_c": {
      "hint": "The configuration variables for the 'C' phase inputs of the chip."
    },
    "phase_offset": {
      "hint": "This can be used to account for a phase offset between the voltage sensing circuit and the current sensing circuit to improve power measurement accuracy."
    },
    "phase_status": {
      "hint": "Enables per-phase status conditions:"
    },
    "phy_addr": {
      "hint": "The PHY address of the Ethernet controller."
    },
    "phy_mode": {
      "hint": "Only on esp8266."
    },
    "phy_registers": {
      "hint": "Arbitrary PHY register values to set after Ethernet initialization, with the same address/value/page_id sub-options as the RMII configuration variables above."
    },
    "pid": {
      "hint": "The product ID of the device."
    },
    "pin": {
      "hint": "The pin to measure the voltage on."
    },
    "pin_a": {
      "hint": "The id of the float output connected to Pin A (alternatively IN1, etc.) of the h-bridge."
    },
    "pin_b": {
      "hint": "The id of the float output connected to Pin B (alternatively IN2, etc.) of the h-bridge."
    },
    "pin_c": {
      "hint": "The pin c of the stepper control board."
    },
    "pin_count": {
      "hint": "The number of bits implemented in the expander."
    },
    "pin_d": {
      "hint": "The pin d of the stepper control board."
    },
    "pin_reset": {
      "hint": "An optional pin that resets the step value."
    },
    "pins": {
      "hint": "The pins to wake up on."
    },
    "pio": {
      "hint": "The PIO peripheral to use."
    },
    "pioneer": {
      "hint": "Trigger on a decoded Pioneer remote code with the given data."
    },
    "pipeline": {
      "hint": "Which pipeline to set the delay for."
    },
    "pixel_clock_pin": {
      "hint": "The pin the pixel clock line of the camera is connected to."
    },
    "pixel_format": {
      "hint": "The pixel format to use for the images."
    },
    "pixel_mapper": {
      "hint": "lambda: A lambda that returns the integer address of the LED given the supplied the x and y pixel coordinate."
    },
    "pixel_mode": {
      "hint": "Allows forcing the display into 18 or 16 bit mode."
    },
    "pixoo_id": {
      "hint": "The ID of the pixoo display to control."
    },
    "pl_const": {
      "hint": "A constant derived from the physical characteristics of your measurement setup."
    },
    "placeholder": {
      "hint": "ID of another image to display while the downloaded image is not yet ready."
    },
    "platform_version": {
      "hint": "The version of the platformio/espressif8266 package to use."
    },
    "platformio_options": {
      "hint": "Additional options to pass over to PlatformIO in the platformio.ini file."
    },
    "pm_0_3um": {
      "hint": "Use the number of particles with diameter beyond 0.3um in 0.1L of air."
    },
    "pm_0_5um": {
      "hint": "Use the number of particles with diameter beyond 0.5um in 0.1L of air."
    },
    "pm_1_0": {
      "hint": "Mass of particles with a diameter of 1 micrometres or less (μg/m^3)."
    },
    "pm_1_0_std": {
      "hint": "Use the concentration of particulates of size less than 1.0µm in µg per cubic meter at standard particle."
    },
    "pm_1_0um": {
      "hint": "Use the number of particles with diameter beyond 1.0um in 0.1L of air."
    },
    "pm_10_0": {
      "hint": "The sensor providing PM10 concentration readings in µg/m³."
    },
    "pm_10_0_std": {
      "hint": "Use the concentration of particulates of size less than 10.0µm in µg per cubic meter at standard particle."
    },
    "pm_10_0um": {
      "hint": "Use the number of particles with diameter beyond 10.0um in 0.1L of air."
    },
    "pm_2_5": {
      "hint": "The sensor providing PM2.5 concentration readings in µg/m³."
    },
    "pm_2_5_std": {
      "hint": "Use the concentration of particulates of size less than 2.5µm in µg per cubic meter at standard particle."
    },
    "pm_2_5um": {
      "hint": "Use the number of particles with diameter beyond 2.5um in 0.1L of air."
    },
    "pm_4_0": {
      "hint": "The information for the Weight Concentration sensor for coarse particles up to 4μm."
    },
    "pm_5_0um": {
      "hint": "Use the number of particles with diameter beyond 5.0um in 0.1L of air."
    },
    "pm_size": {
      "hint": "Typical particle size in μm."
    },
    "pmc_0_3": {
      "hint": "Count of particles with diameter > 0.3 um in 0.1 L of air (#/0.1L)."
    },
    "pmc_0_5": {
      "hint": "Count of particles with diameter > 0.5 um in 0.1 L of air (#/0.1L)."
    },
    "pmc_1_0": {
      "hint": "Count of particles with diameter > 1 um in 0.1 L of air (#/0.1L)."
    },
    "pmc_10_0": {
      "hint": "Count of particles with diameter > 10 um in 0.1 L of air (#/0.1L)."
    },
    "pmc_2_5": {
      "hint": "Count of particles with diameter > 2.5 um in 0.1 L of air (#/0.1L)."
    },
    "pmc_4_0": {
      "hint": "The information for the Number Concentration sensor for coarse particles up to 4μm."
    },
    "pmc_5_0": {
      "hint": "Count of particles with diameter > 5 um in 0.1 L of air (#/0.1L)."
    },
    "point_cloud": {
      "hint": "Enable or disable point-cloud reporting, which is what point_count reads."
    },
    "point_count": {
      "hint": "Total number of point-cloud points reported by the radar."
    },
    "polarity": {
      "hint": "Polarity of the alert pin, either active_high or active_low."
    },
    "poll_period": {
      "hint": "The poll period to apply, e.g."
    },
    "polling_interval": {
      "hint": "If interrupt_pin is not set, set the time interval for periodic polling."
    },
    "port": {
      "hint": "The port to run the API server on."
    },
    "port_type": {
      "hint": "The electrical type of the serial port."
    },
    "position": {
      "hint": "The position of the stop point."
    },
    "position_action": {
      "hint": "The action that should be performed when the remote (like Home Assistant's frontend) requests the cover be set to a specific position."
    },
    "position_command_topic": {
      "hint": "The topic to receive cover position commands on."
    },
    "position_state_topic": {
      "hint": "The topic to publish cover position changes to."
    },
    "positive_output": {
      "hint": "The positive output power to drive the heat output at."
    },
    "possible_values": {
      "hint": "a list of possible values default: 48.0,49.0,50.0,51.0"
    },
    "post_connect_roaming": {
      "hint": "Enable basic post-connect roaming for stationary devices."
    },
    "power": {
      "hint": "The power consumption of the pump in W."
    },
    "power_calibration": {
      "hint": "Enables power calibration configuration."
    },
    "power_consumption": {
      "hint": "Any of the possible power profile (48, 56, 63, 70, 77, 91, 105, 115, 40, 44, 47, 51, 54, 61, 68, 78) in µA"
    },
    "power_down": {
      "hint": "Chose the power down mode."
    },
    "power_down_pin": {
      "hint": "The ESP pin to power down the camera."
    },
    "power_factor": {
      "hint": "Report the power factor value of this phase as a percentage (%)."
    },
    "power_factor_a": {
      "hint": "Use the power factor of the A channel in percentage."
    },
    "power_factor_b": {
      "hint": "Use the power factor of the B channel in percentage."
    },
    "power_gain": {
      "hint": "The value for the APGAIN calibration register."
    },
    "power_id": {
      "hint": "The ID of the power sensor to integrate over time."
    },
    "power_mode": {
      "hint": "The power mode to run the sensor."
    },
    "power_pin": {
      "hint": "The PHY reset/power pin."
    },
    "power_reference": {
      "hint": "The calibration parameter for power readings."
    },
    "power_save_mode": {
      "hint": "Should the device be powered down between update intervals?"
    },
    "power_saving": {
      "hint": "power saving"
    },
    "power_source": {
      "hint": "Indicates what kind of power the device uses."
    },
    "power_supply": {
      "hint": "The Power Supply to connect to this light."
    },
    "power_usage": {
      "hint": "The information for the current power consumption sensor."
    },
    "poweron_selfcheck_time": {
      "hint": "The delay to perform self check and calibration on power on."
    },
    "powerup_pin": {
      "hint": "The Powerup pin for the Inkplate display."
    },
    "preamble_detect": {
      "hint": "Minimum length of the preamble in bytes required by the receiver."
    },
    "preamble_errors": {
      "hint": "Number of chip errors tolerated in the receiver."
    },
    "preamble_polarity": {
      "hint": "Polarity of the preamble, either 0xAA or 0x55."
    },
    "preamble_size": {
      "hint": "Length of the preamble in bytes to be sent by the transmitter."
    },
    "precision": {
      "hint": "This is for Nextion float components."
    },
    "prefix": {
      "hint": "The prefix to automatically prepend every metric with."
    },
    "presence_timeout": {
      "hint": "The time in seconds during which the occupied state (presence) will persist after presence is no longer detected."
    },
    "preset": {
      "hint": "Set the preset of the climate device."
    },
    "preset_change": {
      "hint": "(Optional, Action): The action to call when the preset is changed."
    },
    "preset_command_topic": {
      "hint": "The topic to receive preset commands on."
    },
    "preset_modes": {
      "hint": "A list of preset modes for this fan."
    },
    "preset_state_topic": {
      "hint": "The topic to publish preset changes to."
    },
    "pressure": {
      "hint": "The information for the pressure sensor."
    },
    "pressure_range_bar": {
      "hint": "The maximum pressure range of your sensor model in bar."
    },
    "prev": {
      "hint": "The ID of a Binary Sensor , to be used as PREV key."
    },
    "prior": {
      "hint": "The prior probability of the event."
    },
    "priority": {
      "hint": "The priority to execute your custom initialization code."
    },
    "private_key": {
      "hint": "The private key of the device."
    },
    "prob_given_false": {
      "hint": "Assuming the event is false, the probability this observation is on."
    },
    "prob_given_true": {
      "hint": "Assuming the event is true, the probability this observation is on."
    },
    "process_std_dev": {
      "hint": "only for KALMAN type: The standard deviation of the measurement's change per second (e.g."
    },
    "product_id": {
      "hint": "The product ID."
    },
    "product_model": {
      "hint": "The product model."
    },
    "project": {
      "hint": "ESPHome Creator's Project information."
    },
    "pronto": {
      "hint": "Trigger on a Pronto remote code with the given code."
    },
    "propane_butane_mix": {
      "hint": "Mixture of propane and butane."
    },
    "protect_time": {
      "hint": "The delay after an end-of-trigger event where the detection will not trigger anymore."
    },
    "protocol": {
      "hint": "Protocol of service (_udp or _tcp)."
    },
    "protocol_version": {
      "hint": "Version of the protocol used by your inverter."
    },
    "provider": {
      "hint": "The name of the provider node."
    },
    "proximity_gain": {
      "hint": "The proximity gain level."
    },
    "ps_cooldown": {
      "hint": "The \"cooldown\" period after the proximity sensor is triggered."
    },
    "ps_counts": {
      "hint": "Raw 11-bit reading from proximity sensor, counts."
    },
    "ps_gain": {
      "hint": "The gain the device will use for proximity sensor."
    },
    "ps_high_threshold": {
      "hint": "The threshold for the proximity sensor to trigger on object getting closer."
    },
    "ps_low_threshold": {
      "hint": "The threshold for the proximity sensor to trigger on object getting further away."
    },
    "pskc": {
      "hint": "PSKc is used to authenticate an external Thread Commissioner to a Thread network."
    },
    "publish_initial_value": {
      "hint": "Controls whether the value is published upon start of ESPHome."
    },
    "publish_nan_as_none": {
      "hint": "Publish None instead of NaN to handle Unknown/Unavailable sensor states in Home Assistant."
    },
    "publish_state": {
      "hint": "Publish new state to Home Assistant."
    },
    "pulse_energy": {
      "hint": "Sets the energy (in Watt-hours) per individual pulse on the CS5460A's EOUT pin that can be used to driver external counters / meters or a LED."
    },
    "pulse_length": {
      "hint": "The length in milliseconds of the pulse sent on on_pin and off_pin to change switch state."
    },
    "pulse_time": {
      "hint": "The duration for which the trigger pin will be active."
    },
    "pump_start_pump_delay": {
      "hint": "The delay in seconds from when a distribution valve is opened to when the associated pump is activated."
    },
    "pump_start_valve_delay": {
      "hint": "The delay in seconds from when a pump is started to when the associated distribution valve is opened."
    },
    "pump_stop_pump_delay": {
      "hint": "The delay in seconds from when a distribution valve is closed to when the respective pump is deactivated."
    },
    "pump_stop_valve_delay": {
      "hint": "The delay in seconds from when a pump is deactivated to when the respective distribution valve is closed."
    },
    "pump_switch_id": {
      "hint": "Switch: This is the switch component to be used to control the valve's pump or upstream electric valve."
    },
    "pump_switch_off_during_valve_open_delay": {
      "hint": "If set to true, the pump will be switched off during the valve_open_delay interval; otherwise, it remains on."
    },
    "pv_active_power": {
      "hint": "Use the (active) power value of PVs in total in watts."
    },
    "pv_charging_power": {
      "hint": "pc charging power"
    },
    "pv_input_current_for_battery": {
      "hint": "pv input current for battery"
    },
    "pv_input_voltage": {
      "hint": "pv input voltage"
    },
    "pv_ok_condition_for_parallel": {
      "hint": "PV OK condition for parallel"
    },
    "pv_power_balance": {
      "hint": "PV power balance mode"
    },
    "pv1": {
      "hint": "The group of exposed sensors for Photo Voltaic 1."
    },
    "pv2": {
      "hint": "The group of exposed sensors for Photo Voltaic 2."
    },
    "pwm": {
      "hint": "Enable PWM output."
    },
    "pylontech_id": {
      "hint": "Manually specify the ID of the pylontech instance if there are multiple."
    },
    "qos": {
      "hint": "The Quality of"
    },
    "quality": {
      "hint": "Sets JPEG compression quality."
    },
    "query_params": {
      "hint": "Refresh all sensors values of the device."
    },
    "queue_enable_switch": {
      "hint": "The name for the sprinkler controller's queue enable switch as it will appear in the front end."
    },
    "queue_mode": {
      "hint": "Enables queue mode."
    },
    "quiet_mode": {
      "hint": "(supported only by hOn) A switch that enables or disables Haier climate quiet mode."
    },
    "r_int": {
      "hint": "Current rain intensity in mm/h."
    },
    "r1_pin": {
      "hint": "Red data pin for top half."
    },
    "r2_pin": {
      "hint": "Red data pin for bottom half."
    },
    "radiation_dose_per_10m": {
      "hint": "Radiation average dose per 10 minutes."
    },
    "radiation_dose_per_1m": {
      "hint": "Radiation average dose per 1 minute."
    },
    "radius": {
      "hint": "Corner radius in pixels. A large value on a square widget makes it a circle."
    },
    "range": {
      "hint": "The allowable rotation range from the start_position."
    },
    "raw": {
      "hint": "Allows to read the raw ADC output without any conversion or calibration."
    },
    "raw_position": {
      "hint": "Reports the raw (un-scaled, un-filtered) position of the magnet."
    },
    "raw_pressure": {
      "hint": "The information for the pressure sensor."
    },
    "rc_code_1": {
      "hint": "The remote control code to trigger on, see dumper output for more details."
    },
    "rc_code_2": {
      "hint": "The second part of the remote control code to trigger on, see dumper output for more details."
    },
    "rc_switch_raw": {
      "hint": "Trigger on a decoded RC Switch raw remote code with the given data."
    },
    "rc_switch_type_a": {
      "hint": "Trigger on a decoded RC Switch Type A remote code with the given data."
    },
    "rc_switch_type_b": {
      "hint": "Trigger on a decoded RC Switch Type B remote code with the given data."
    },
    "rc_switch_type_c": {
      "hint": "Trigger on a decoded RC Switch Type C remote code with the given data."
    },
    "rc_switch_type_d": {
      "hint": "Trigger on a decoded RC Switch Type D remote code with the given data."
    },
    "rc5": {
      "hint": "Trigger on a decoded RC5 remote code with the given data."
    },
    "rc6": {
      "hint": "Trigger on a decoded RC6 remote code with the given data."
    },
    "rd03d_id": {
      "hint": "Manually specify the ID of the RD-03D component."
    },
    "reactive_power": {
      "hint": "Use the reactive power value on this phase."
    },
    "reactive_power_a": {
      "hint": "Use the reactive power value of the A channel in volt amperes."
    },
    "reactive_power_b": {
      "hint": "Use the reactive power value of the B channel in volt amperes."
    },
    "read_address": {
      "hint": "Address of the first holding register to read back."
    },
    "read_command": {
      "hint": "The byte used for the read commands when communicating with the BL0940."
    },
    "read_count": {
      "hint": "1-125 registers to read back."
    },
    "read_lambda": {
      "hint": "lambda: Lambda that returns the value of this bit, as a bool."
    },
    "read_pin": {
      "hint": "The pin that the receiver is connected to"
    },
    "reboot_timeout": {
      "hint": "The amount of time to wait before rebooting when no client connects to the API."
    },
    "receive_symbols": {
      "hint": "Maximum receive length in symbols."
    },
    "receive_timeout": {
      "hint": "The timeout for receiving a complete frame."
    },
    "receiver_id": {
      "hint": "The remote receiver to receive the remote code with."
    },
    "recipient": {
      "hint": "The number to dial."
    },
    "recolor": {
      "hint": "Color to mix with the widget."
    },
    "recolor_opa": {
      "hint": "Sets the intensity of color mixing."
    },
    "recovery_time": {
      "hint": "Adjust the recovery time (tREC0)."
    },
    "red": {
      "hint": "percentage: The red channel of the light."
    },
    "red_channel": {
      "hint": "Value of the red color channel relative to the clear channel, as a percentage."
    },
    "redirect_limit": {
      "hint": "Maximum amount of redirects to follow when enabled."
    },
    "redox": {
      "hint": "Measured Redox value."
    },
    "ref": {
      "hint": "Git ref (branch or tag)."
    },
    "reference_current": {
      "hint": "Fine grained gain calibration for a known current of the circuit a CT is measuring."
    },
    "reference_resistance": {
      "hint": "Reference resistor on the PCB."
    },
    "reference_temperature": {
      "hint": "Access the internal temperature sensor of the MAX31855."
    },
    "reference_voltage": {
      "hint": "Fine grained gain calibration of a known voltage that a voltage transformer is connected to."
    },
    "register_last_address": {
      "hint": "The highest Modbus register address (inclusive) up to which undefined registers are allowed to be read and will be padded with a default value."
    },
    "register_value": {
      "hint": "The 16-bit value (range: 0–65535) to return for undefined registers within the address range defined by register_last_address."
    },
    "registered": {
      "hint": "Indicates if the SIM800L has successfully registered in the cellular network."
    },
    "registers": {
      "hint": "A list of registers that are responded to when acting as a server."
    },
    "relabel": {
      "hint": "Override metric labels."
    },
    "release_debounce": {
      "hint": "The minimum length that no touch is recognized before a release event is created."
    },
    "release_threshold": {
      "hint": "The release defines the sensitivity for touch detection and should be between 5 and 30."
    },
    "remaining_capacity": {
      "hint": "The capacity in Ah left in the battery."
    },
    "remote_id": {
      "hint": "The ID of the original binary sensor in the provider device."
    },
    "repeat": {
      "hint": "Specifies how many times the loop will run."
    },
    "repeat_number": {
      "hint": "The name of the number component that should be presented to the front end (Home Assistant) to enable control of the sprinkler controller's repeat value."
    },
    "request_headers": {
      "hint": "Map of HTTP headers."
    },
    "require_connection_to_proceed": {
      "hint": "Set to true to wait for the remote peer to be up before continuing to boot the device."
    },
    "requires_code_to_arm": {
      "hint": "Code required for arming the alarm, codes must be provided."
    },
    "reset": {
      "hint": "Whether to reset the BL0942 chip on startup, resetting all internal counters."
    },
    "reset_detection_area": {
      "hint": "Reset detection areas to defaults (4 m by 4 m)."
    },
    "reset_duration": {
      "hint": "Duration for the display reset operation."
    },
    "reset_high": {
      "hint": "The time to hold the data line high after writing the state."
    },
    "reset_low": {
      "hint": "The time to hold the data line low after writing the state."
    },
    "reset_low_time": {
      "hint": "Adjust the reset low time (tRSTL)."
    },
    "reset_on_boot": {
      "hint": "Whether or not to reset the device configuration (including counters) on component initialization."
    },
    "reset_pin": {
      "hint": "The GPIO pin that the ADE7880's RESET input is connected to, if any."
    },
    "reset_unattended": {
      "hint": "Reset the module's unattended (no-person) state used by its low-power logic."
    },
    "resets_required": {
      "hint": "The number of power cycles after which the device will be reset."
    },
    "resistance": {
      "hint": "The initial value of the sensor resistance."
    },
    "resistor": {
      "hint": "The value of the resistor with a constant value."
    },
    "resistor_one": {
      "hint": "The calibration parameter for R1."
    },
    "resistor_shunt": {
      "hint": "The calibration parameter for RL."
    },
    "resistor_two": {
      "hint": "The calibration parameter for R2."
    },
    "resizable": {
      "hint": "Whether the display window can be manually resized"
    },
    "resize": {
      "hint": "If set, this will resize the image to fit inside the given dimensions WIDTHxHEIGHT and preserve the aspect ratio."
    },
    "resolution": {
      "hint": "the resolution of this sensor."
    },
    "response_template": {
      "hint": "Optional Jinja template to process the action response data."
    },
    "restart": {
      "hint": "Restart the device."
    },
    "restart_cycle_on_state_change": {
      "hint": "Restart a timer of a cycle when new state is set."
    },
    "restart_module": {
      "hint": "Reboots the LD2420 modules."
    },
    "restore": {
      "hint": "Whether to store the intermediate result on the device so that the value can be restored upon power cycle or reboot."
    },
    "restore_from_flash": {
      "hint": "Whether to store some persistent preferences in flash memory."
    },
    "restore_mode": {
      "hint": "Control how the fan attempts to restore state on boot."
    },
    "restore_value": {
      "hint": "Whether to try to restore the state on boot up."
    },
    "resume_on_input": {
      "hint": "If LVGL is paused and the user interacts with the screen, resume the activity of LVGL."
    },
    "retain": {
      "hint": "If the published message should"
    },
    "retransmission": {
      "hint": "true if the message was re-transmitted."
    },
    "reverse_active_energy": {
      "hint": "Report the reverse active energy value of this phase in volt-ampere-reactive-hours (VARh)."
    },
    "reverse_enable": {
      "hint": "For some displays the order of the chips is reversed so you'll see \"56781234\" instead of \"12345678\"."
    },
    "reverse_switch": {
      "hint": "The name for the sprinkler controller's reverse switch as it will appear in the front end."
    },
    "reversed": {
      "hint": "Whether to reverse the order of LEDs in this segment."
    },
    "revert_config": {
      "hint": "Undoes in-progress edits prior to their application via the apply_config button."
    },
    "rf_switch": {
      "hint": "Used to indicate if DIO2 controls an external RF switch."
    },
    "rgb_current": {
      "hint": "current: The current used for the RGB channel."
    },
    "rgb_order": {
      "hint": "The order of the RGB channels."
    },
    "right": {
      "hint": "The ID of a Binary Sensor , to be used as RIGHT key."
    },
    "right_button": {
      "hint": "The ID of a Binary Sensor , to be used as RIGHT key."
    },
    "rising_edge": {
      "hint": "What to do when a rising edge is detected."
    },
    "rloc16": {
      "hint": "Expose the Router Locator (RLOC16) address as a text sensor."
    },
    "rms": {
      "hint": "The information for the Root Mean Square loudness sensor."
    },
    "rmt_symbols": {
      "hint": "When use_dma is enabled, this sets the size of the driver's internal DMA buffer."
    },
    "role": {
      "hint": "Expose the current device role in the Thread network (Leader, Router, Child, Detached, etc.) as a text sensor."
    },
    "roomba": {
      "hint": "Trigger on a decoded Roomba remote code with the given data."
    },
    "rotary_sensitivity": {
      "hint": "Adjust sensitivity for rotary encoders in 1/256 unit."
    },
    "rotate_chip": {
      "hint": "Rotates every 8x8 chip."
    },
    "rotation": {
      "hint": "Rotate the display content by a specified angle."
    },
    "router": {
      "hint": "If true the Zigbee role will be router instead of end device and it will forward Zigbee packets from other devices."
    },
    "row": {
      "hint": "The row of the key."
    },
    "row_start": {
      "hint": "The starting row offset."
    },
    "rows": {
      "hint": "A list of pins where the horrizontal matrix lines are connected, in order from top to bottom."
    },
    "rp2040_ble_id": {
      "hint": "Manually specify the ID of the RP2040 BLE component this tracker uses."
    },
    "rs_pin": {
      "hint": "The pin you have RS (04 ) hooked up to."
    },
    "rssi": {
      "hint": "The informed Received signal strength indication (RSSI) in dBm."
    },
    "rtc_storage": {
      "hint": "Only useful on ESP32."
    },
    "rtd_nominal_resistance": {
      "hint": "Nominal resistance of the RTD at 0°C."
    },
    "rtd_wires": {
      "hint": "The number of RTD wires."
    },
    "rts_pin": {
      "hint": "GPIO pin to use as the RTS (Request to Send) modem control output."
    },
    "rtttl": {
      "hint": "The RTTTL string."
    },
    "run_duration": {
      "hint": "The time duration the node should be active, i.e."
    },
    "run_duration_number": {
      "hint": "The name of the number component that should be presented to the front end (Home Assistant) to enable control of the valve's run_duration value."
    },
    "run_gain_calibration": {
      "hint": "A button to run the gain calibration."
    },
    "run_offset_calibration": {
      "hint": "A button to run the offset calibration."
    },
    "run_power_offset_calibration": {
      "hint": "A button to run the power offset calibration."
    },
    "runtime_tag_levels": {
      "hint": "Enable runtime per-tag log level changes."
    },
    "rw_pin": {
      "hint": "Optionally set the pin you have R/W (05 ) hooked up to."
    },
    "rx_attenuation": {
      "hint": "Internal RX attenuation."
    },
    "rx_buffer_size": {
      "hint": "Size of the receive buffer in bytes."
    },
    "rx_err_fcs": {
      "hint": "Total MAC RX FCS (Frame Check Sequence) errors since boot."
    },
    "rx_floor": {
      "hint": "When receiving OOK rx_floor should be set appropriately for your environment."
    },
    "rx_only": {
      "hint": "Whether the sensor is connected only by RX pin to the UART bus."
    },
    "rx_pin": {
      "hint": "Receive pin."
    },
    "rx_queue_len": {
      "hint": "Length of RX queue."
    },
    "rx_start": {
      "hint": "Start the receiver automatically on boot or after transmitting."
    },
    "rx_total": {
      "hint": "Total MAC RX frames received since boot."
    },
    "sample_duration": {
      "hint": "The time duration to sample the current clamp with."
    },
    "sample_rate": {
      "hint": "The sample rate of this sensor."
    },
    "samples": {
      "hint": "The amount of ADC readings to take per sensor update."
    },
    "samples_per_second": {
      "hint": "Sets the internal chip sample rate"
    },
    "sampling_mode": {
      "hint": "Sampling method to use when multiple samples are taken."
    },
    "samsung": {
      "hint": "Trigger on a decoded Samsung remote code with the given data."
    },
    "samsung36": {
      "hint": "Trigger on a decoded Samsung36 remote code with the given data."
    },
    "satellites": {
      "hint": "Include the number of tracking satellites being used as a sensor"
    },
    "saturation": {
      "hint": "The saturation to apply to the picture, from -2 to 2."
    },
    "save": {
      "hint": "If set to true, the SSID and password will be saved in persistent memory."
    },
    "scan": {
      "hint": "If ESPHome should do a search of the I²C address space on startup."
    },
    "scan_parameters": {
      "hint": "Advanced parameters for configuring the scan behavior."
    },
    "scan_results": {
      "hint": "Expose the latest networks found during the latest scan."
    },
    "scan_time": {
      "hint": "Scan time per row (must be set above debounce time)."
    },
    "scan_wiring": {
      "hint": "Panel scan wiring pattern."
    },
    "scc_charging_status": {
      "hint": "scc charging status"
    },
    "scc_firmware_version": {
      "hint": "scc firmware version"
    },
    "scene_mode": {
      "hint": "Valid only in standard mode."
    },
    "scl": {
      "hint": "The pin for the clock line of the I²C bus."
    },
    "scl_pullup_enabled": {
      "hint": "Enable the internal pullup resistor for the SCL pin."
    },
    "scroll_delay": {
      "hint": "Set delay time before scroll starts."
    },
    "scroll_dwell": {
      "hint": "Sets the wait time at the end of the scroll before starting over."
    },
    "scroll_enable": {
      "hint": "Turn scroll mode on when content does not fit."
    },
    "scroll_mode": {
      "hint": "Set the scroll mode."
    },
    "scroll_speed": {
      "hint": "Set scroll speed."
    },
    "sda": {
      "hint": "The pin for the data line of the I²C bus."
    },
    "sda_pullup_enabled": {
      "hint": "Enable the internal pullup resistor for the SDA pin."
    },
    "sdl_options": {
      "hint": "Build arguments if required to specify include or library paths."
    },
    "second": {
      "hint": "uint32_t: The second 24-bit Coolix code to trigger on, see dumper output for more info."
    },
    "seconds": {
      "hint": "The string to use for the seconds element."
    },
    "segments": {
      "hint": "A list of segments included in this partition."
    },
    "sel_pin": {
      "hint": "The pin SEL is connected to."
    },
    "self_cleaning": {
      "hint": "A button that starts Haier climate self cleaning."
    },
    "send_every": {
      "hint": "How often a sensor value should be pushed out."
    },
    "send_first_at": {
      "hint": "By default, the very first raw value on boot is immediately published."
    },
    "send_to_nextion": {
      "hint": "Publish new state to Nextion display which will update component."
    },
    "send_wait_time": {
      "hint": "Time in milliseconds before the next ModBUS command is sent when an answer from a previous command has not yet started (i.e."
    },
    "sensing_distance": {
      "hint": "A unitless number, in range 0-1023, specifying the maximum distance to detect motion"
    },
    "sensing_pin": {
      "hint": "Pin connected to the reader's finger detection signal (WAKEUP) output."
    },
    "sensitivity": {
      "hint": "Set the sensitivity of the sensor."
    },
    "sensor": {
      "hint": "The ID of a Rotary Encoder ; or a list with buttons for left/right interaction with the widgets: left_button ( Optional , ID ): The ID of a Binary Sensor , to be used as LEFT key."
    },
    "sensor_datapoint": {
      "hint": "The datapoint id number of the sensor."
    },
    "sensor_id": {
      "hint": "The ID of the source sensor."
    },
    "sensor_power_pin": {
      "hint": "Output pin responsible for toogling the sensor power on and off."
    },
    "sensors": {
      "hint": "A list of Sensor definitions that include a lambda to do the decoding and return a float value."
    },
    "sensors_packet_size": {
      "hint": "(supported only by hOn) Define the size of the sensor packet of the status message."
    },
    "separate_modes": {
      "hint": "Use separate RGB/CW modes instead of writing all 5 values as RGB."
    },
    "separator": {
      "hint": "The separator to use between the uptime values."
    },
    "serial_number": {
      "hint": "The inverter's serial number."
    },
    "server_id": {
      "hint": "Specify the device's server_id to retrieve the OBIS code from."
    },
    "servers": {
      "hint": "Choose up to 3 NTP servers that are used for the clock source."
    },
    "service": {
      "hint": "Name of extra service."
    },
    "service_uuid": {
      "hint": "16-, 32- or 128-bit BLE service UUID in hex."
    },
    "services": {
      "hint": "A list of services to expose on the BLE GATT server."
    },
    "set_action": {
      "hint": "The action that should be performed when the remote (like Home Assistant's frontend) requests to set the dateime value."
    },
    "set_point_minimum_differential": {
      "hint": "For dual-point/dual-function systems, the minimum required temperature difference between the heat and cool set points."
    },
    "setup_mode": {
      "hint": "Whether debug messages with the touch pad value should"
    },
    "shadow_color": {
      "hint": "Color used to create a shadow under the widget rectangle."
    },
    "shadow_offset_x": {
      "hint": "Horizontal offset of the shadow, in pixels."
    },
    "shadow_offset_y": {
      "hint": "Vertical offset of the shadow, in pixels."
    },
    "shadow_opa": {
      "hint": "Opacity of the shadow."
    },
    "shadow_spread": {
      "hint": "Spread of the shadow, in pixels."
    },
    "shadow_width": {
      "hint": "Blur radius of the drop shadow in pixels. 0 disables the shadow."
    },
    "shaping": {
      "hint": "Transmitter data shaping can be GAUSSIAN_BT_0_3, GAUSSIAN_BT_0_5, GAUSSIAN_BT_0_7, GAUSSIAN_BT_1_0 or NONE."
    },
    "shift_driver": {
      "hint": "LED shift register driver chip type."
    },
    "show_snow": {
      "hint": "When paused, display random colored pixels across the entire screen in order to minimize screen burn-in, to relief the tension put on each individual pixel."
    },
    "shunt_resistance": {
      "hint": "The value of the shunt resistor on the board for current calculation."
    },
    "shunt_voltage": {
      "hint": "Use the shunt voltage (voltage across the shunt resistor) value of the sensor in V."
    },
    "shutdown_message": {
      "hint": "MQTTMessage: The message to send when the node shuts down and the connection is closed cleanly."
    },
    "signal_rate_limit": {
      "hint": "Set the return signal rate limit in units of MCPS (mega counts per second)."
    },
    "signal_strength": {
      "hint": "The information for the received signal strength sensor in dBm."
    },
    "silence_buzzer_open_buzzer": {
      "hint": "silence buzzer open buzzer"
    },
    "silence_detection": {
      "hint": "Enable silence detection."
    },
    "single_light_id": {
      "hint": "The ID of a single addressable or non-addressable light."
    },
    "size": {
      "hint": "The desired size of the font."
    },
    "skip": {
      "hint": "Option to skip this page when navigating between them with lvgl.page.next , lvgl.page.previous ."
    },
    "skip_cert_cn_check": {
      "hint": "Only on ESP32."
    },
    "skip_connection_handshake": {
      "hint": "Sets whether the initial display connection handshake process is skipped."
    },
    "skip_crc": {
      "hint": "Skip CRC check."
    },
    "skip_probe": {
      "hint": "Skip reading the chip ID on startup."
    },
    "skip_taskbar": {
      "hint": "Whether to skip adding a taskbar icon for the display window or not"
    },
    "sleep_duration": {
      "hint": "The time duration to stay in deep sleep mode."
    },
    "sleep_pin": {
      "hint": "Optionally also use the SLEEP pin of the A4988 stepper driver."
    },
    "sleep_time": {
      "hint": "No key press within this time will set keypad engine to sleep."
    },
    "sleep_when_done": {
      "hint": "Whether to turn off all coils when the stepper has reached the target position"
    },
    "sleepy": {
      "hint": "Disable RX during idle to reduce power consumption for battery-powered devices."
    },
    "slow_filter": {
      "hint": "See datasheet."
    },
    "sm16716_id": {
      "hint": "Manually specify the ID of the Component/Hub."
    },
    "sm2135_id": {
      "hint": "Manually specify the ID of the Component/Hub."
    },
    "sm2235_id": {
      "hint": "Manually specify the ID of the Component/Hub."
    },
    "sm2335_id": {
      "hint": "Manually specify the ID of the Component/Hub."
    },
    "sml_id": {
      "hint": "The ID of the SML platform"
    },
    "smooth_mode": {
      "hint": "Level of filter applied on the original data against large noise interference."
    },
    "software_coexistence": {
      "hint": "When enabled, software coexistence will briefly prioritize Bluetooth over Wi-Fi during the initial establishment of BLE connections, which can improve reliability."
    },
    "solution": {
      "hint": "Solution reference EC value."
    },
    "sony": {
      "hint": "Trigger on a decoded Sony remote code with the given data."
    },
    "sorting_groups": {
      "hint": "Available only on version: 3."
    },
    "sorting_weight": {
      "hint": "A float representing the weight of the group."
    },
    "source": {
      "hint": "The sensor id that is used as sample source."
    },
    "source_address": {
      "hint": "The source address to trigger on."
    },
    "source_id": {
      "hint": "The binary sensor that should be mirrored."
    },
    "source_speakers": {
      "hint": "A list of source speaker inputs."
    },
    "sources": {
      "hint": "A list of sensors to use as source."
    },
    "speaker": {
      "hint": "The id of the Speaker to play the song on."
    },
    "special_effect": {
      "hint": "The effect to apply to the picture."
    },
    "speed": {
      "hint": "the speed of the pump rotor in RPM."
    },
    "speed_command_topic": {
      "hint": "The topic to receive speed commands on (options: LOW, MEDIUM, HIGH)."
    },
    "speed_count": {
      "hint": "Set the number of supported discrete speed levels."
    },
    "speed_level_command_topic": {
      "hint": "The topic to receive numeric speed commands on (range: 0 to speed count)."
    },
    "speed_level_state_topic": {
      "hint": "The topic to publish numeric fan speed state changes to (range: 0 to speed count)."
    },
    "speed_state_topic": {
      "hint": "The topic to publish fan speed state changes to (options: LOW, MEDIUM, HIGH)."
    },
    "sph_pin": {
      "hint": "The SPH pin for the Inkplate display."
    },
    "spi_16": {
      "hint": "Set to true on boards where single bit SPI is used but drives the display in parallel via a 16 bit shift register."
    },
    "spi_id": {
      "hint": "Manually specify the ID of the SPI Component if you want to use multiple SPI buses."
    },
    "spi_mode": {
      "hint": "The SPI mode."
    },
    "spike_rejection": {
      "hint": "Helps to differentiate between false events and actual lightning."
    },
    "spreading_factor": {
      "hint": "Spreading factor, values range from 6 to 12."
    },
    "spv_pin": {
      "hint": "The SPV pin for the Inkplate display."
    },
    "sr_count": {
      "hint": "Number of daisy-chained shift registers, up-to 256."
    },
    "ssid": {
      "hint": "The name of the WiFi access point."
    },
    "stage_gain": {
      "hint": "The analog gain to use for threshold test."
    },
    "standard_units": {
      "hint": "True to use standard units or False to use environmental units."
    },
    "standby_switch": {
      "hint": "The name for the sprinkler controller's standby switch as it will appear in the front end."
    },
    "start_dynamic_background_correction": {
      "hint": "Start the Dynamic Background Correction All options from Button."
    },
    "start_frame": {
      "hint": "The frame to loop back to when end_frame is reached."
    },
    "start_keys": {
      "hint": "Keys used to start the key sequence; when set, no keys will be accepted until one of the start keys is pressed."
    },
    "start_position": {
      "hint": "The raw position that should be considered the start (i.e."
    },
    "start_sensing_delay": {
      "hint": "The amount of time the current sensing will be disabled when the movement starts."
    },
    "start_up_page": {
      "hint": "Sets the page to display when ESPHome connects to the Nextion."
    },
    "starting_integral_term": {
      "hint": "Set the initial output, by priming the integral term."
    },
    "startup_delay": {
      "hint": "An optional startup delay -"
    },
    "startup_override_ms": {
      "hint": "Time in milliseconds to wait before forcing the display to be marked as ready if it hasn't responded to the connection handshake."
    },
    "state": {
      "hint": "The state to set the cover to - one of OPEN or CLOSE."
    },
    "state_change_action": {
      "hint": "An automation to perform when the load is switched."
    },
    "state_class": {
      "hint": "The state class for the sensor."
    },
    "state_save_interval": {
      "hint": "The minimum interval at which to save calibrated BSEC algorithm state to flash so that calibration doesn't have to start from zero on device restart."
    },
    "state_subscription_only": {
      "hint": "If enabled, only counts clients that have subscribed to entity state updates."
    },
    "static_ip": {
      "hint": "IPv4 address: The static IP of your node."
    },
    "status": {
      "hint": "Information about the magnet status."
    },
    "status_indicator": {
      "hint": "An output to display feedback to the user."
    },
    "status_message_header_size": {
      "hint": "(supported only by hOn) Define the header size of the status message."
    },
    "status_pin": {
      "hint": "Some Tuya devices support WiFi status reporting ONLY through gpio pin."
    },
    "stb_pin": {
      "hint": "The pin you have the STB line hooked up to."
    },
    "std_dev": {
      "hint": "only for KALMAN type: A sensor that publishes the current standard deviation of the state with each update."
    },
    "std_initial": {
      "hint": "The initial estimate for standard deviation parameter has no impact for NOx."
    },
    "step": {
      "hint": "The granularity with which the number can be set."
    },
    "step_delay": {
      "hint": "Manually specify the delay between steps (in microseconds) between 1us and 100us."
    },
    "step_mode": {
      "hint": "The step mode to operate the motor with."
    },
    "step_pin": {
      "hint": "The STEP pin of the A4988 stepper driver."
    },
    "steri_cleaning": {
      "hint": "A button that starts Haier climate 56°C Steri-Clean."
    },
    "still_distance": {
      "hint": "Distance in cm of detected still target."
    },
    "still_energy": {
      "hint": "Energy for still target."
    },
    "still_target_count": {
      "hint": "Count of stationary targets."
    },
    "stop": {
      "hint": "Whether to stop the cover."
    },
    "stop_action": {
      "hint": "The action that should be performed to stop the cover."
    },
    "stop_after_detection": {
      "hint": "Whether to stop the component after detecting a wake word."
    },
    "stop_bits": {
      "hint": "The number of stop bits to send."
    },
    "stops": {
      "hint": "A list of at least 2 color stop points."
    },
    "storage": {
      "hint": "Where to persist the boot-attempt counter."
    },
    "store_baseline": {
      "hint": "Stores and retrieves the baseline VOC and NOx information for quicker startups."
    },
    "store_in_eeprom": {
      "hint": "Use SEQ_WRITE mode to also write to EEPROM sequentially."
    },
    "style_definitions": {
      "label": "Style definitions",
      "hint": "Reusable named style blocks that widgets can reference by ID instead of repeating properties."
    },
    "subnet": {
      "hint": "IPv4 address: The subnet of the local network."
    },
    "success": {
      "hint": "Whether the action succeeded."
    },
    "sun_gtil2_id": {
      "hint": "Manually specify the ID of the sun_gtil2 instance if there are multiple."
    },
    "supplemental_cooling_action": {
      "hint": "The action to call when the climate device should activate supplemental cooling to (more aggressively) decrease the current temperature."
    },
    "supplemental_cooling_delta": {
      "hint": "When the temperature difference between the upper set point and the current temperature exceeds this value, supplemental_cooling_action will be called immediately."
    },
    "supplemental_heating_action": {
      "hint": "The action to call when the climate device should activate supplemental heating to (more aggressively) increase the current temperature."
    },
    "supplemental_heating_delta": {
      "hint": "When the temperature difference between the lower set point and the current temperature exceeds this value, supplemental_heating_action will be called immediately."
    },
    "supply_voltage": {
      "hint": "Supply voltage of the sensor."
    },
    "supported_modes": {
      "hint": "Can be used to disable some of AC modes."
    },
    "supported_presets": {
      "hint": "Can be used to disable some presets."
    },
    "supported_swing_modes": {
      "hint": "Can be used to disable some swing modes if your AC does not support it."
    },
    "swap_xy": {
      "hint": "If true, exchange the x and y axes."
    },
    "swing_both_action": {
      "hint": "The action to call when the fan should oscillate in horizontal and vertical directions."
    },
    "swing_horizontal_action": {
      "hint": "The action to call when the fan should oscillate in a horizontal direction."
    },
    "swing_mode": {
      "hint": "Set the swing mode of the climate device."
    },
    "swing_mode_command_topic": {
      "hint": "The topic to receive swing mode commands on."
    },
    "swing_mode_state_topic": {
      "hint": "The topic to publish swing mode changes to."
    },
    "swing_off_action": {
      "hint": "The action to call when the fan should remain in a stationary position."
    },
    "swing_vertical_action": {
      "hint": "The action to call when the fan should oscillate in a vertical direction."
    },
    "symbol_rate": {
      "hint": "The symbol rate in Baud."
    },
    "symphony": {
      "hint": "Trigger on a decoded Symphony remote code with the given data."
    },
    "sync_mode": {
      "hint": "Sync word detection mode."
    },
    "sync_value": {
      "hint": "Synchronization bytes, list of 1 to 8 bytes, found after the preamble and before the payload."
    },
    "sync0": {
      "hint": "hex: Lower sync word byte."
    },
    "sync1": {
      "hint": "hex: Upper sync word byte."
    },
    "tag": {
      "hint": "The id of the scanned tag"
    },
    "tag_id": {
      "hint": "A string that identifies the tag; in effect, its name."
    },
    "tag_ttl": {
      "hint": "The duration that must elapse after the PN7160 is no longer able to \"see\" a tag before it is considered to have been removed from the reader."
    },
    "tank_type": {
      "hint": "The tank type the sensor is measuring."
    },
    "tap": {
      "hint": "Single tap detection."
    },
    "taps": {
      "hint": "positive integer: The number of taps per windowed sinc interpolation filter."
    },
    "target": {
      "hint": "True if any target is detected or any detection area reports presence."
    },
    "target_count": {
      "hint": "Total targets detected, whether they are stationary or in motion."
    },
    "target_display": {
      "hint": "Enable or disable target reporting (0x0A04)."
    },
    "target_humidity": {
      "hint": "Set the target humidity of a climate device."
    },
    "target_humidity_change_action": {
      "hint": "The action to call when the thermostat's target humidity is changed."
    },
    "target_humidity_command_topic": {
      "hint": "The topic to receive target humidity commands on."
    },
    "target_humidity_state_topic": {
      "hint": "The topic to publish target humidity changes to."
    },
    "target_mac_address": {
      "hint": "MAC Address: The MAC Address of the target computer."
    },
    "target_temperature": {
      "hint": "Set the target temperature of a climate device."
    },
    "target_temperature_change_action": {
      "hint": "The action to call when the thermostat's target temperature(s) is/are changed."
    },
    "target_temperature_command_topic": {
      "hint": "The topic to receive target temperature commands on."
    },
    "target_temperature_high": {
      "hint": "Set the higher target temperature of a climate device with a two-point target temperature."
    },
    "target_temperature_high_command_topic": {
      "hint": "The topic to receive higher target temperature commands on."
    },
    "target_temperature_high_state_topic": {
      "hint": "The topic to publish higher target temperature changes to."
    },
    "target_temperature_low": {
      "hint": "Set the lower target temperature of a climate device with a two-point target temperature."
    },
    "target_temperature_low_command_topic": {
      "hint": "The topic to receive lower target temperature commands on."
    },
    "target_temperature_low_state_topic": {
      "hint": "The topic to publish lower target temperature changes to."
    },
    "target_temperature_state_topic": {
      "hint": "The topic to publish target temperature changes to."
    },
    "target_temperature_step": {
      "hint": "Override the temperature steps shown in the frontend."
    },
    "task_log_buffer_size": {
      "hint": "ESP32, LibreTiny and nRF52 only: The size of the internal thread-safe ring buffer for task log messages."
    },
    "task_stack_in_psram": {
      "hint": "If true, allocate the inference task stack in PSRAM instead of internal RAM."
    },
    "tcxo_delay": {
      "hint": "The time needed for the TCXO to stabilize."
    },
    "tcxo_voltage": {
      "hint": "Reference voltage of the external TCXO controlled by DIO3."
    },
    "telemetry_request_min_interval": {
      "hint": "Minimum time between consecutive telemetry reads."
    },
    "temp_diff": {
      "hint": "Temperature difference between the 2 sensors."
    },
    "temp1": {
      "hint": "Temperatue of sensor 1."
    },
    "temp2": {
      "hint": "Temperatue of sensor 2."
    },
    "temperature": {
      "hint": "The sensor that is used to measure the current temperature, in °C."
    },
    "temperature_1": {
      "hint": "The first temperature sensor."
    },
    "temperature_2": {
      "hint": "The second temperature sensor."
    },
    "temperature_coefficient": {
      "hint": "Temperature coefficient (ppm/°C) of the shunt for temperature compensation correction."
    },
    "temperature_compensation": {
      "hint": "On-chip temperature compensation."
    },
    "temperature_high": {
      "hint": "Historic maximum temperature."
    },
    "temperature_low": {
      "hint": "Historic minimum temperature."
    },
    "temperature_offset": {
      "hint": "Temperature offset if device is in enclosure and reads too high."
    },
    "temperature_sensor": {
      "hint": "Set the ID of the temperature sensor."
    },
    "temperature_source": {
      "hint": "The temperature that should be used as the climate entity's current temperature:"
    },
    "temperature_state": {
      "hint": "Temperature state."
    },
    "temperature_step": {
      "hint": "The granularity with which the target temperature can be controlled."
    },
    "terminal_a": {
      "hint": "Set to false if terminal \"A\" shall be disabled on boot."
    },
    "terminal_b": {
      "hint": "Set to false if terminal \"B\" shall be disabled on boot."
    },
    "terminal_w": {
      "hint": "Set to false if terminal \"W\" shall be disabled on boot."
    },
    "test_pattern": {
      "hint": "For tests purposes, it's possible to replace picture get from sensor by a test color pattern."
    },
    "text_color": {
      "hint": "Color of the widget's text."
    },
    "text_font": {
      "label": "Text font",
      "hint": "ID of the font: component used for this widget's text. Falls back to the LVGL default_font."
    },
    "tft_upload_http_retries": {
      "hint": "Number of retries for transient HTTP failures during TFT upload."
    },
    "tft_upload_http_timeout": {
      "hint": "HTTP request timeout for TFT upload transfers."
    },
    "tft_upload_watchdog_timeout": {
      "hint": "Temporarily adjusts the system watchdog timeout for the duration of the TFT transfer process."
    },
    "tft_url": {
      "hint": "The URL from which to download the TFT file for display firmware updates (Nextion OTA)."
    },
    "theme": {
      "label": "Theme",
      "hint": "Global style defaults per widget type. Keyed by widget type, optionally nested by part and state."
    },
    "then": {
      "hint": "The action to perform if the condition evaluates to true."
    },
    "thermocouple_type": {
      "hint": "The type of thermocouple used."
    },
    "three_byte_address": {
      "hint": "The length of the source and destination address."
    },
    "threshold": {
      "hint": "Configures the reference for comparison."
    },
    "throttle": {
      "hint": "Minimum time between sensor updates."
    },
    "tilt": {
      "hint": "The tilt position to set."
    },
    "tilt_action": {
      "hint": "The action that should be performed when the remote (like Home Assistant's frontend) requests the cover be set to a specific tilt position."
    },
    "tilt_command_topic": {
      "hint": "The topic to receive cover tilt commands on."
    },
    "tilt_lambda": {
      "hint": "lambda: Lambda to be evaluated repeatedly to get the current tilt position of the cover."
    },
    "tilt_state_topic": {
      "hint": "The topic to publish cover cover tilt state changes to."
    },
    "time": {
      "hint": "Duration of the page change animation."
    },
    "time_constant": {
      "hint": "Time constant in seconds."
    },
    "time_device_address": {
      "hint": "The 32 bit device address of the thermostat that keeps the system time."
    },
    "time_id": {
      "hint": "The ID of a Time which can be used to set the time on the BedJet device."
    },
    "time_off": {
      "hint": "Interval to hold the output at OFF."
    },
    "time_on": {
      "hint": "Interval to hold the output at ON."
    },
    "time_unit": {
      "hint": "The time unit to integrate with, one of ms, s, min, h or d."
    },
    "timeout": {
      "hint": "Amount of time expected since the last touch event."
    },
    "timezone": {
      "hint": "Manually tell ESPHome what time zone to use with this format (warning: the format is quite complicated, see examples) or the simpler TZ database name in the form <Region>/<City>."
    },
    "timing": {
      "hint": "The timing of the multi click."
    },
    "timing_budget": {
      "hint": "Set the timing budget the sensor will use for a single range measurement."
    },
    "tlc59208f_id": {
      "hint": "Manually specify the ID of the TLC59208F chip."
    },
    "tlc5947_id": {
      "hint": "Manually specify the ID of the TLC5947-component."
    },
    "tlc5971_id": {
      "hint": "Manually specify the ID of the TLC5971-component."
    },
    "tls_buffer_size_rx": {
      "hint": "Change TLS receive buffer size."
    },
    "tls_buffer_size_tx": {
      "hint": "Change TLS transmit buffer size."
    },
    "tlv": {
      "hint": "The Thread dataset as a hex-encoded TLV string, taken from the Thread information in Home Assistant."
    },
    "tm1637_id": {
      "hint": ":ID: The id of the tm1637 that should be used to scan the keys in case you are using multiple devices."
    },
    "to": {
      "hint": "The type of values in the map."
    },
    "today_generation_time": {
      "hint": "Use the day generation time value for same day of the sensor in minutes."
    },
    "toggle_action": {
      "hint": "The action that should be performed when the remote requests to toggle the cover."
    },
    "tolerance": {
      "hint": "The percentage or time that the remote signal lengths can deviate in the decoding process."
    },
    "too_cold": {
      "hint": "true if the sensor reports being too cold."
    },
    "toolchain": {
      "hint": "Toolchain: Toolchain used to build the firmware."
    },
    "top_layer": {
      "hint": "A special kind of Always on Top page, which acts as a parent for widgets placed on it."
    },
    "topic": {
      "hint": "The MQTT topic to subscribe to and listen for MQTT messages on."
    },
    "topic_prefix": {
      "hint": "The prefix used for all MQTT messages."
    },
    "toshiba_ac": {
      "hint": "Trigger on a decoded Toshiba AC remote code with the given data."
    },
    "total": {
      "hint": "Report the total number of pulses."
    },
    "total_acc": {
      "hint": "Total amount of rain this sensor has ever measured, in mm."
    },
    "total_active_energy": {
      "hint": "Use the total active energy value of the sensor in kilo watt hours."
    },
    "total_energy": {
      "hint": "The total energy value of all channels in kWh."
    },
    "total_energy_production": {
      "hint": "Use the total exported energy value of the sensor in kilo watt hours."
    },
    "total_generation_time": {
      "hint": "Use the total generation time value of the sensor in hours."
    },
    "total_power": {
      "hint": "The total power value of all channels in watts."
    },
    "total_reactive_energy": {
      "hint": "Use the total reactive energy value of the sensor in kilo volt amps reactive hours."
    },
    "toto": {
      "hint": "Trigger on a decoded Toto remote code with the given data."
    },
    "touch_debounce": {
      "hint": "The minimum length before a touch is recognized."
    },
    "touch_sleep_timeout": {
      "hint": "Sets internal No-touch-then-sleep timer in seconds."
    },
    "touch_threshold": {
      "hint": "The touch threshold for all channels."
    },
    "touch_timeout": {
      "hint": "A timeout for touchscreens that do not report the end of touch."
    },
    "touch_wakeup": {
      "hint": "Only on ESP32."
    },
    "touch_wakeup_reason": {
      "hint": "run duration if woken up by touch."
    },
    "touchscreen_id": {
      "hint": "ID of a touchscreen configuration related to a display."
    },
    "touchscreens": {
      "hint": "A list of touchscreens interacting with the LVGL widgets on the display."
    },
    "traces": {
      "hint": "Use this to specify more than a single trace."
    },
    "tracking_mode": {
      "hint": "The tracking mode to configure."
    },
    "transfer_function": {
      "hint": "\"A\" or \"B\": Transfer function used by the pressure sensor."
    },
    "transform": {
      "hint": "If rotation is not sufficient, use this to transform the display."
    },
    "transform_height": {
      "hint": "Stretch widget higher on both sides with this value."
    },
    "transform_pivot_x": {
      "hint": "Horizontal anchor point of the transformation."
    },
    "transform_pivot_y": {
      "hint": "Vertical anchor point of the transformation."
    },
    "transform_rotation": {
      "hint": "Transformation rotation angle of the widget."
    },
    "transform_scale": {
      "hint": "Transformation scale of the widget (uniform scaling)."
    },
    "transform_scale_x": {
      "hint": "Zoom widget horizontally."
    },
    "transform_scale_y": {
      "hint": "Zoom widget vertically."
    },
    "transform_skew_x": {
      "hint": "Skew widget horizontally by an angle."
    },
    "transform_skew_y": {
      "hint": "Skew widget vertically by an angle."
    },
    "transform_width": {
      "hint": "Stretch widget wider on both sides with this value."
    },
    "transition_length": {
      "hint": "The time needed for a full movement (-1.0 to 1.0)."
    },
    "translate_radial": {
      "hint": "Movement of the widget object the centre of the parent object (e.g."
    },
    "translate_x": {
      "hint": "Movement of the widget with this value in horizontal direction."
    },
    "translate_y": {
      "hint": "Movement of the widget with this value in vertical direction."
    },
    "transmitter_id": {
      "hint": "Defined and used automatically when using Remote Transmitter component for IR commands transmit."
    },
    "transparency": {
      "hint": "If set the alpha channel of the input image will be taken into account."
    },
    "transparency_key": {
      "label": "Transparency key",
      "hint": "Color treated as fully transparent when the display buffer is composited (chroma key)."
    },
    "transport": {
      "hint": "Specifies the transport method used by Zephyr MCUmgr for OTA updates."
    },
    "trigger_base": {
      "hint": "The delay while a detection must be active to change the state of the sensor."
    },
    "trigger_keep": {
      "hint": "The delay that the output will stay high after a detection event."
    },
    "trigger_mode": {
      "hint": "Sets the trigger mode for this sensor."
    },
    "trigger_on_initial_state": {
      "hint": "If true, any applicable triggers will be fired when the binary sensor state changes from unknown to a valid state."
    },
    "trigger_pin": {
      "hint": "The output pin to periodically send the trigger pulse to."
    },
    "trigger_speed": {
      "hint": "fast (~0.5 s), medium (~1 s), or slow (~5 s), the time from detecting a person to triggering."
    },
    "trigger_time": {
      "hint": "The time after a triggered alarm before resetting to previous state if the sensors are cleared/off."
    },
    "tt21100_id": {
      "hint": "Manually specify the ID of the touchscreen."
    },
    "tune_antenna": {
      "hint": "Start sensor in antenna tuning mode."
    },
    "turn_off_action": {
      "hint": "An automation to perform when the load is turned off."
    },
    "turn_on_action": {
      "hint": "An automation to perform when the load is turned on."
    },
    "turnaround_time": {
      "hint": "Time in milliseconds before the next ModBUS command is sent after last response is received."
    },
    "tvoc": {
      "hint": "The information for the total Volatile Organic Compounds sensor."
    },
    "tvoc_baseline": {
      "hint": "The information for the TVOC baseline value sensor."
    },
    "tx_buffer_size": {
      "hint": "Size of the transmit buffer in bytes."
    },
    "tx_enqueue_timeout": {
      "hint": "Maximum time to wait when the TX queue is full before dropping the message (by default, this is set to the time it takes to send 10 CAN messages at the given bit rate)."
    },
    "tx_err_abort": {
      "hint": "Total MAC TX abort errors since boot."
    },
    "tx_err_cca": {
      "hint": "Total MAC TX CCA (Clear Channel Assessment) errors since boot."
    },
    "tx_pin": {
      "hint": "Transmit pin."
    },
    "tx_power": {
      "hint": "The transmit power of the iBeacon in dBm."
    },
    "tx_queue_len": {
      "hint": "Length of TX queue, 0 to disable."
    },
    "tx_retries": {
      "hint": "Total MAC TX frame retransmissions since boot."
    },
    "tx_total": {
      "hint": "Total MAC TX frame transmissions since boot."
    },
    "type": {
      "hint": "The type of sensor measurement."
    },
    "uart_id": {
      "hint": "The ID of the UART bus you wish to use for this sensor."
    },
    "udp_id": {
      "hint": "The ID of the UDP client to use for sending logs."
    },
    "ufc_chip_error": {
      "hint": "Reports if there is an error with the UFC chip (ultrasonic flow converter; datasheet ST2 bit 5)."
    },
    "ufm01_id": {
      "hint": "The ID of the ufm01 component this binary sensor belongs to."
    },
    "uid": {
      "hint": "The unique ID of the NFC tag."
    },
    "underlying_open_function": {
      "hint": "Enable/disable underlying open function."
    },
    "unit_of_measurement": {
      "hint": "Unit shown next to the value in the frontend."
    },
    "unlock_action": {
      "hint": "The action that should be performed when the remote (like Home Assistant's frontend) requests the lock to be unlocked."
    },
    "unman_time": {
      "hint": "Valid only in standard mode."
    },
    "until": {
      "hint": "The time of day to wake up."
    },
    "up": {
      "hint": "The ID of a Binary Sensor , to be used as UP key."
    },
    "update": {
      "hint": "If true, the image will be updated (fetched) immediately after setting the new URL."
    },
    "update_interval": {
      "hint": "The interval to check the sensor."
    },
    "update_when_display_idle": {
      "hint": "When using the Epaper Spi or other displays with long update times, setting this option"
    },
    "uponor_smatrix_id": {
      "hint": "Manually specify the ID of the uponor_smatrix hub component if you want to use multiple hub components on one ESPHome device."
    },
    "upper": {
      "hint": "Upper threshold, that needs to be crossed to transition from low to high states."
    },
    "url": {
      "hint": "url: Git repository url."
    },
    "usb_lang_id": {
      "hint": "USB language identifier."
    },
    "usb_manufacturer_str": {
      "hint": "Manufacturer string descriptor."
    },
    "usb_product_id": {
      "hint": "USB product identifier."
    },
    "usb_product_str": {
      "hint": "Product name string descriptor."
    },
    "usb_serial_str": {
      "hint": "Serial number string descriptor."
    },
    "usb_vendor_id": {
      "hint": "USB vendor identifier."
    },
    "use_abbreviations": {
      "hint": "Whether to use Abbreviations in discovery messages."
    },
    "use_accumulated_energy_registers": {
      "hint": "Use ADE7935 accumulated energy registers instead of instant power registers."
    },
    "use_address": {
      "hint": "Manually override what address to use to connect to the ESP."
    },
    "use_axis_flips": {
      "hint": "If true, the driver will use alternate bits in the MADCTL register to implement x and y mirroring."
    },
    "use_bgr": {
      "hint": "Use BGR mode."
    },
    "use_dma": {
      "hint": "Enable DMA on variants that support it."
    },
    "use_fahrenheit": {
      "hint": "Specifies if the temperature value is in Fahrenheit."
    },
    "use_interrupt": {
      "hint": "Use hardware interrupts instead of polling for better performance and lower CPU usage."
    },
    "use_mclk": {
      "hint": "Use the MCLK signal to control the clock."
    },
    "use_microphone": {
      "hint": "Configure the codec's ADC to use PDM microphone input instead of analog."
    },
    "use_pcnt": {
      "hint": "Use hardware PCNT pulse counter."
    },
    "use_psram": {
      "hint": "Requests that the Bluetooth stack allocate its memory buffers from PSRAM instead of internal RAM, freeing approximately 40 kB for other uses."
    },
    "use_rm433_remote": {
      "hint": "Set to True if your setup uses Sonoff RM433 or any other radio remote control."
    },
    "use_wake_word": {
      "hint": "Enable wake word on the assist pipeline."
    },
    "useragent": {
      "hint": "User-Agent header for requests."
    },
    "username": {
      "hint": "Username for the Git server, if one is required"
    },
    "uuid": {
      "hint": "The universally unique identifier to identify the beacon."
    },
    "uv": {
      "hint": "Sensor counts for the UV sensor (#)."
    },
    "uv_index": {
      "hint": "UV index (UVI)."
    },
    "validity_period": {
      "hint": "The time periode for which the pvvx device should display the information."
    },
    "value": {
      "hint": "The value to use."
    },
    "value_type": {
      "hint": "data type of the Modbus register data."
    },
    "values": {
      "hint": "Registers to write, write_address first."
    },
    "valve_open_delay": {
      "hint": "The minimum delay in seconds that should be inserted between (distribution) valve switching -- in other words, the amount of time that must elapse between one valve switching off and the next one swi…"
    },
    "valve_overlap": {
      "hint": "The amount of time in seconds that the current valve and the next valve should run simultaneously as the next valve/zone starts up."
    },
    "valve_switch": {
      "hint": "The name for the switch component to be used to control the valve for this part of the sprinkler system (often referred to as a \"zone\")."
    },
    "valve_switch_id": {
      "hint": "Switch: This is the switch component to be used to control the valve that operates the given section or zone of the sprinkler system."
    },
    "valves": {
      "hint": "A list of valves the controller should use."
    },
    "vane": {
      "hint": "Vane state automation configuration."
    },
    "variable_name": {
      "hint": "The name of the Nextion variable."
    },
    "variables": {
      "hint": "Optional variables that can be used in the data_template."
    },
    "variant": {
      "hint": "Set the variant of the device in use."
    },
    "vcom_pin": {
      "hint": "The VCOM pin for the Inkplate display."
    },
    "ven_pin": {
      "hint": "The pin connected to the PN7160's VEN line."
    },
    "verify_ssl": {
      "hint": "When set to true (default), SSL/TLS certificates will be validated upon connection; if invalid, the connection will be aborted."
    },
    "version": {
      "hint": "The firmware version of the sensor."
    },
    "vertical": {
      "hint": "Vertical vane settings."
    },
    "vertical_airflow": {
      "hint": "The vertical airflow direction."
    },
    "vertical_flip": {
      "hint": "Whether to flip the image vertically."
    },
    "vibrations": {
      "hint": "Vibration status."
    },
    "vid": {
      "hint": "The vendor ID of the device."
    },
    "visible": {
      "hint": "Visible or not"
    },
    "visual": {
      "hint": "Visual settings for the climate device - these do not affect operation and are solely for controlling how the climate device shows up in the frontend."
    },
    "voltage": {
      "hint": "Report the RMS voltage value of this phase in volts (V)."
    },
    "voltage_attenuation": {
      "hint": "ESP32 only: The voltage attenuation to use for the charge cycles."
    },
    "voltage_calibration": {
      "hint": "Enables voltage calibration configuration."
    },
    "voltage_divider": {
      "hint": "The value of the voltage divider on the board as (R_upstream + R_downstream) / R_downstream."
    },
    "voltage_gain": {
      "hint": "The value for the AVGAIN calibration register."
    },
    "voltage_high": {
      "hint": "Voltage of the highest cell."
    },
    "voltage_hpf": {
      "hint": "Enables and disables the High-pass Filter on the voltage processing path in the CS5460A."
    },
    "voltage_low": {
      "hint": "Voltage of the lowest cell."
    },
    "voltage_pga_gain": {
      "hint": "Set the input PGA voltage amplification."
    },
    "voltage_reference": {
      "hint": "The calibration parameter for voltage readings."
    },
    "voltage_sampled_by_secondary_cpu": {
      "hint": "Use the photo voltiac's voltage sampled by slave CPU value of the sensor in volts."
    },
    "voltage_state": {
      "hint": "Voltage state."
    },
    "volume": {
      "hint": "The volume value."
    },
    "volume_increment": {
      "hint": "percentage: Increment amount that the media_player.volume_up and media_player.volume_down actions will increase or decrease volume by."
    },
    "volume_initial": {
      "hint": "percentage: The default volume that mediaplayer uses for first boot where a volume has not been previously saved."
    },
    "volume_max": {
      "hint": "percentage: The maximum volume allowed."
    },
    "volume_max_db": {
      "hint": "dB: The volume level that maps to the maximum volume (fully turned up)."
    },
    "volume_min": {
      "hint": "percentage: The minimum volume allowed."
    },
    "volume_min_db": {
      "hint": "dB: The volume level that maps to the minimum volume (fully turned down)."
    },
    "volume_multiplier": {
      "hint": "Volume multiplier to apply to the assist pipeline."
    },
    "volume_per_minute": {
      "hint": "The volume to dose in milliliters every minute."
    },
    "vref": {
      "hint": "Chose the VREF source."
    },
    "vsync_back_porch": {
      "hint": "The vertical back porch length."
    },
    "vsync_front_porch": {
      "hint": "The vertical front porch length."
    },
    "vsync_pin": {
      "hint": "The pin the VSYNC line of the camera is connected to."
    },
    "vsync_pulse_width": {
      "hint": "The vertical sync pulse width."
    },
    "vwc": {
      "hint": "Volumetric Water Content in cm3cm−3."
    },
    "wait_for_cdc": {
      "hint": "Waits for the CDC port before starting setup (10-second timeout)."
    },
    "wait_for_connection": {
      "hint": "Blocks other components from starting until the MQTT connection is established."
    },
    "wait_for_sent": {
      "hint": "If true, the automation will wait for the underlying send operation to complete (and for any on_sent or on_error actions to finish) before continuing with the next action."
    },
    "wait_time": {
      "hint": "Time to wait after a gain change before allowing another adjustment."
    },
    "wake": {
      "hint": "Pulse the wakeup_pin to wake the radar."
    },
    "wake_up_page": {
      "hint": "Sets the page to display after waking up"
    },
    "wake_word": {
      "hint": "The wake word that was used to trigger the voice assistant when using on-device wake word such as Micro Wake Word."
    },
    "wakeup_pin": {
      "hint": "Only on ESP32/BK72xx."
    },
    "wakeup_pin_mode": {
      "hint": "Only on ESP32/BK72xx."
    },
    "wakeup_pulse": {
      "hint": "Pulse length for the wakeup pin."
    },
    "wakeup_threshold": {
      "hint": "The threshold to use to detect touch events to wake-up from deep sleep."
    },
    "wand_id": {
      "hint": "The MagiQuest wand ID to trigger on, see dumper output for more info."
    },
    "warm_white": {
      "hint": "The id of the float Output Component to use for the warm white channel."
    },
    "warm_white_color_temperature": {
      "hint": "The warmest color temperature supported by this light."
    },
    "warmup_brightness": {
      "hint": "Brightness threshold below which the dimmer switches on later in mains current cycle."
    },
    "warmup_time": {
      "hint": "The sensor has a warmup period during which it returns inaccurate readings (e.g., 500ppm, 505ppm)."
    },
    "warning_battery_derating": {
      "hint": "warning battery derating"
    },
    "warning_battery_equalization": {
      "hint": "warning battery equalization"
    },
    "warning_battery_low_alarm": {
      "hint": "warning battery low alarm"
    },
    "warning_battery_too_low_to_charge": {
      "hint": "warning battery too low to charge"
    },
    "warning_battery_under_shutdown": {
      "hint": "warning battery under shutdown"
    },
    "warning_battery_voltage_high": {
      "hint": "warning battery voltage high"
    },
    "warning_eeprom_failed": {
      "hint": "warning eeprom failed"
    },
    "warning_fan_lock": {
      "hint": "warning fan lock"
    },
    "warning_high_ac_input_during_bus_soft_start": {
      "hint": "warning high ac input during bus soft start"
    },
    "warning_line_fail": {
      "hint": "warning line fail"
    },
    "warning_low_pv_energy": {
      "hint": "warning low pv energy"
    },
    "warning_mppt_overload": {
      "hint": "warning mppt overload"
    },
    "warning_over_load": {
      "hint": "warning over load"
    },
    "warning_over_temperature": {
      "hint": "warning over temperature"
    },
    "warning_power_limit": {
      "hint": "warning power limit"
    },
    "warning_power_loss": {
      "hint": "warning power loss"
    },
    "warning_pv_voltage_high": {
      "hint": "warning pv voltage high"
    },
    "warnings_present": {
      "hint": "warnings present"
    },
    "watchdog": {
      "hint": "Whether to enable the watchdog that puts the chip into low power mode 3."
    },
    "watchdog_threshold": {
      "hint": "Determines the threshold for events that trigger the IRQ pin."
    },
    "watchdog_timeout": {
      "hint": "The timeout applied to the ESP-IDF task watchdog."
    },
    "waterproof_guard_ring": {
      "hint": "Sets the touch channel to use for the guard pad."
    },
    "waterproof_shield_driver": {
      "hint": "Shield channel drive capability configuration; the larger the parasitic capacitance on the shielding channel, the higher the drive capability needs to be set."
    },
    "wave_channel_id": {
      "hint": "The waveform ID in a range of 0-3"
    },
    "wave_max_length": {
      "hint": "How many data points to store."
    },
    "wave_max_value": {
      "hint": "The max value."
    },
    "waveform_send_last_value": {
      "hint": "This will send the last value set during an update interval."
    },
    "wb_mode": {
      "hint": "The mode of white balace module."
    },
    "weight": {
      "hint": "The information for the weight sensor."
    },
    "white": {
      "hint": "percentage: The brightness of the white channel."
    },
    "white_brightness": {
      "hint": "The id of the float Output Component to use for the brightness of the white leds."
    },
    "whitening": {
      "hint": "Enable data whitening to improve DC balance."
    },
    "whitening_enable": {
      "hint": "Enables CCITT data whitening, with fixed polynomial 0x21."
    },
    "whitening_initial": {
      "hint": "Initial value of whitening shift register."
    },
    "widgets": {
      "hint": "A list of Widgets to be drawn on the root display."
    },
    "width": {
      "hint": "Width in pixels."
    },
    "wifi_signal": {
      "hint": "If true - send wifi signal level to AC."
    },
    "wifi_timeout": {
      "hint": "The amount of time to wait before starting the Improv service after Wi-Fi is no longer connected."
    },
    "will_message": {
      "hint": "MQTTMessage: The message to send when the MQTT connection is dropped."
    },
    "wind_direction_degrees": {
      "hint": "The information for the direction in degrees sensor."
    },
    "wind_speed": {
      "hint": "The information for the wind speed sensor."
    },
    "window": {
      "hint": "The time the receiver is actively listening for packets on a channel during each scan interval."
    },
    "window_correction_factor": {
      "hint": "Window correction factor."
    },
    "window_options": {
      "hint": "Options that affect how the display renders on the host system."
    },
    "window_size": {
      "hint": "The number of values over which to calculate the min/max when pushing out a value."
    },
    "wipe_on_boot": {
      "hint": "Erases all non-volatile memory data on boot, including Zigbee network pairing and preferences (e.g., last switch state)."
    },
    "wkup_req_pin": {
      "hint": "The pin connected to the PN7160's WKUP_REQ line."
    },
    "work_mode": {
      "hint": "normal or low_power based on the radar work mode."
    },
    "write_0_low_time": {
      "hint": "Adjust the write-0 low time (tW0L)."
    },
    "write_action": {
      "hint": "An automation to perform when the state of the output is updated."
    },
    "write_address": {
      "hint": "Address of the first holding register to write."
    },
    "write_command": {
      "hint": "byte used for the write commands when communicating with the BL0940."
    },
    "write_lambda": {
      "hint": "lambda: Lambda that sets the value of this bit."
    },
    "write_pin": {
      "hint": "The pin that the transmitter is connected to"
    },
    "x": {
      "hint": "X position of the display window in pixels"
    },
    "x_axis": {
      "hint": "The information for the x-axis."
    },
    "x_grid": {
      "hint": "Specifies the time per division."
    },
    "x_max": {
      "hint": "The raw value corresponding to the right"
    },
    "x_min": {
      "hint": "The raw value corresponding to the left"
    },
    "y": {
      "hint": "Y position of the display window in pixels"
    },
    "y_axis": {
      "hint": "The information for the y-axis."
    },
    "y_grid": {
      "hint": "Specifies the number of units per division."
    },
    "y_max": {
      "hint": "The raw value corresponding to the bottom"
    },
    "y_min": {
      "hint": "The raw value corresponding to the top"
    },
    "z_axis": {
      "hint": "The information for the z-axis."
    },
    "zero_cross_interrupt_type": {
      "hint": "Set the interrupt type for the zero crossing interrupt, can be:"
    },
    "zero_cross_pin": {
      "hint": "The pin used to sense the AC Zero cross event, you can have several dimmers controlled with the same zero cross detector, in such case duplicate the zero_cross_pin config on each output."
    },
    "zone_type": {
      "hint": "Control the zone detection modes."
    }
  }
}
